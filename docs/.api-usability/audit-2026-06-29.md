# FastSense Public API Usability Audit — 2026-06-29 (re-audit)

**Type:** report-only. **Runtime:** warm live MATLAB R2025b, worktree `laughing-swanson-be52cf`.
**Compares against:** [audit-2026-06-24.md](audit-2026-06-24.md) (baseline, 66/100).

## Usability Score: **66 / 100** — +2 new findings from expanded coverage; B3 resolved

> **Update 2026-06-29 (`/api-polish`):** 🔴 **B3 resolved** — `MonitorTag` now validates `ConditionFn`
> arity at construction and throws the namespaced, actionable `MonitorTag:invalidConditionArity`
> instead of a raw `MATLAB:TooManyInputs` deep in `getXY`. Additive, backward-compatible (every
> existing condition handle in the repo is 2-arg; 108 Tag tests pass). The next audit will reflect
> the score bump; remaining top blockers are B1 and B2.
>
> **Update 2026-06-29 (`/api-polish`, 2nd fix):** 🔴 **B1 resolved** — `FastSense(x, y)` (the
> `plot(x,y)` reflex) now throws the namespaced `FastSense:positionalData` pointing at the existing
> `FastSense.plot(x, y)` one-liner and the add-then-render builder, instead of a cryptic
> `MATLAB:mustBeFieldName`. Additive guard before option parsing; only catches calls that already
> errored (no repo call passes positional data; `FastSense.plot` forwards options only). Proven: 18
> FastSense/integration tests pass. **Remaining top blocker: B2** (`addWidget` string Tag key).

Headline score held at 66 for trend continuity (**source unchanged** — see evidence below). A later
run **expanded coverage** into serialization / `exportData` / the other Tag constructors and surfaced
**2 new findings** (S1, S2) that the baseline never measured. These are *not* a regression — they were
always present; the audit just hadn't probed that corner. Weighted into Error-quality + Discoverability,
a fresh baseline would land at **~64**. See "Expanded coverage" below.

**Nothing has been fixed since the baseline.** Evidence the public surface is identical:
- `git log --since=2026-06-24 -- libs/FastSense libs/Dashboard libs/SensorThreshold` → **no commits**.
- No uncommitted changes to those libs; HEAD still `9fdc13d5`.
- `functionSignatures.json` still absent.

| Dimension | Baseline | Now | Δ |
|---|---|---|---|
| Core-task ergonomics | 95 | 95 | 0 |
| Dashboard-task ergonomics | 55 | 55 | 0 |
| Error-message quality | 58 | 58 | 0 |
| Discoverability | 55 | 55 | 0 |
| Consistency | 70 | 70 | 0 |

## Live re-confirmation (all baseline findings still fire)

| Finding | Probe result this run | Status |
|---|---|---|
| 🔴 B1 `FastSense(x,y)` positional | now `FastSense:positionalData` (points to `FastSense.plot(x,y)` + builder) | ✅ RESOLVED 2026-06-29 (`/api-polish`) |
| 🔴 B2 `addWidget(...,'Tag','<key>')` | `MATLAB:structRefFromNonStruct` | OPEN |
| 🔴 B3 `MonitorTag(...,@()true)` arity | now `MonitorTag:invalidConditionArity` (namespaced + fix hint) | ✅ RESOLVED 2026-06-29 (`/api-polish`) |
| 🟠 F1 `TagRegistry.keys()` | `classHasNoPropertyOrMethod` | OPEN |
| 🟠 F2 `help addWidget` | still *"Accept a pre-constructed widget object directly"* | OPEN |
| 🟠 F3 `help DashboardEngine/save` | still falls through to built-in `save` docs | OPEN |
| 🟠 F4 `addWidget` unknown type | `DashboardEngine:unknownType` (still no valid-type list) | OPEN |
| 🟠 F6a `SensorTag` X/Y len mismatch | silently accepted | OPEN |
| 🟠 F6b `addThreshold('high')` | silently accepted | OPEN |
| 🟢 P2 `DerivedTag` help "FastPlot" | `contains('FastPlot') == 1` | OPEN |
| ✅ gold `FastSense:unknownOption` | still lists all valid options | (good, unchanged) |
| ✅ gold `TagRegistry:unknownKey` | still suggests `list()` | (good, unchanged) |

Full finding write-ups (where / what a user hits / why / additive-vs-breaking) are in the
baseline report — they are unchanged and not duplicated here. All 13 findings remain **additive /
backward-compatible**, safe for `/api-polish`.

## Expanded coverage (this run): serialization, exportData, Tag constructors

Probed corners of the public scope the baseline only lightly touched. Two new findings; the rest clean.

**🟠 S1 — `SensorTag` `toStruct`→`fromStruct` silently produces a data-less tag.**
*Where:* `SensorTag.toStruct` / `SensorTag.fromStruct` (`libs/SensorThreshold/SensorTag.m`).
*Measured:* round-trip of a 2000-point tag → `getXY()` returns **0 points, no error/warning**. `toStruct`
intentionally omits X/Y (documented in *its* help), but `help SensorTag/fromStruct` is a single line
("Reconstruct SensorTag from a toStruct output.") and never says the reloaded tag has no data and that
data must be re-attached (`load`/MatFile/DataStore). Cross-session dashboards built from registered tags
therefore lose their series silently.
*Why it matters:* "save → load" is the natural persistence mental model; a silent empty reload is a trap.
*Fix:* **additive** — warn when a reconstructed tag is queried with no data, and document the
re-attach step prominently in `fromStruct`/`loadFromStructs` help.

**🟠 S2 — `DerivedTag` accepts a wrong-arity `ComputeFn`, then throws raw `MATLAB:minrhs` at `getXY`.**
*Where:* `DerivedTag` constructor / recompute (`libs/SensorThreshold/DerivedTag.m`).
*Measured:* `DerivedTag('sum',{a,b},@(x,Y) Y{1}+Y{2})` (a plausible wrong guess) constructs fine — the
`DerivedTag:invalidCompute` guard only checks *is-a-function-handle*, not arity — then fails at `getXY`
with raw `MATLAB:minrhs :: Not enough input arguments`, **not** the advertised namespaced error. The
correct contract (`@(parents)->[X,Y]`, e.g. `@(p) deal(p{1}.X, p{1}.Y+p{2}.Y)`) *is* documented, so this
is a wrong-guess→raw-error trap, same class as B3 (`MonitorTag` arity).
*Fix:* **additive** — validate `nargin(ComputeFn)` (and output shape) at construction → reuse
`DerivedTag:invalidCompute` with a message stating the `@(parents)->[X,Y]` contract.

**Confirmed clean (positive coverage):**
- ✅ `exportData` — CSV (113 KB) and MAT (52 KB) both written correctly.
- ✅ `StateTag` — numeric **and** cellstr `Y` both construct and `valueAt` correctly (45→`1` / `'run'`).
- ✅ `DashboardEngine.save`→`DashboardEngine.load` — JSON round-trips, reloads, and renders (in-session).
- ✅ `CompositeTag` — `AggregateMode 'and'|'or'|'majority'|…` and `CompositeTag:invalidAggregateMode`
  are documented with namespaced errors; well-formed API (an earlier "OR" surprise was a caller omission,
  not an API gap).

## Expanded coverage round 2: theme/export errors, annotation methods, lifecycle

Error-graded common methods the earlier rounds skipped. The core validation is **mostly gold**; three
small new findings.

**🟠 V1 — `DashboardEngine` Theme name not validated at construction (inconsistent with `FastSense`).**
*Measured:* `FastSense('Theme','sunburst_neon')` → gold `FastSenseTheme:unknownPreset` ("Use 'light' or
'dark'."), but `DashboardEngine('x','Theme','bogus')` **constructs with no error**. A dashboard theme typo
gets no feedback at the point of the mistake.
*Fix:* **additive** — validate the dashboard Theme name eagerly, reusing `FastSenseTheme:unknownPreset`.

**🟠 V2 — `addFill(x,y,0)` (Baseline passed positionally) → raw `MATLAB:badsubscript`.**
*Where:* `FastSense.addFill`. Real signature is `addFill(x, y, 'Baseline', val)` — Baseline is a
name-value pair. Passing it positionally (a very natural guess, fails identically before and after render)
throws raw `Index exceeds the number of array elements`. **Same root-cause family as B1** (`FastSense(x,y)`):
a numeric in a name-value *key* slot yields a cryptic raw error instead of the gold `unknownOption`.
*Fix:* **additive** — detect a numeric 3rd positional and accept it as Baseline (or throw a helpful error).

**🟢 P5 — widget `DashboardWidget:unknownOption` list is gold but noisy.**
*Measured:* `addWidget('number',...,'Value',1)` → `DashboardWidget:unknownOption` correctly lists valid
options, but the list mixes in internal handle/state props (`hPanel`, `Dirty`, `Realized`, `hCellPanel`,
`hValueText`…) alongside real options (`StaticValue`, `ValueFcn`, `Units`, `Format`…), burying the
user-facing ones. *Fix:* **additive** — filter the suggested list to user-facing options.

**Confirmed gold this round (no action):**
- ✅ `FastSense` bad Theme → `FastSenseTheme:unknownPreset` (lists valid presets).
- ✅ `exportData` bad format → `FastSense:exportData:unknownFormat` (lists valid formats).
- ✅ `addBand`/`addMarker`/`addShaded` after `render()` → `FastSense:alreadyRendered` (specific messages).
- ✅ `render()` called twice → `FastSense:alreadyRendered`.
- ✅ `TagRegistry.loadFromStructs` round-trip (SensorTag + MonitorTag) → restores both tags.

**Cumulative note:** headline stays **66** (source unchanged). Expanded coverage across two rounds added
S1, S2, V1, V2 (🟠) + P5 (🟢) — all **additive**, all localized. A fresh baseline weighting all of them
would sit near **~63**. The dominant *pattern* is now clear: validation is gold **except** when a numeric
is passed where a name-value key is expected (B1, V2) or a callback has the wrong arity (B3, S2) — those
slip the type-checks and surface as raw MATLAB errors. One fix family closes four findings.

## Recommended next action

No drift to investigate — the score is static because **no fix has landed**. The lever is `/api-polish`,
not another audit. Highest friction-removed per change (unchanged from baseline):
1. **B3 → B1 → B2** — kill the three cryptic errors on the most common first guesses.
2. **F1** (`TagRegistry.keys`) + **F4** (list valid widget types).
3. **F2 / F3** (addWidget + save/load help).
4. **F5** (explicit widget threshold) + **F6** (consistent validation).
5. **F8** (`functionSignatures.json`), then **P1–P4** (help cleanup).

## Method

- git used to prove the public-API source is byte-identical to the baseline run.
- Headline findings re-triggered live in MATLAB R2025b (error IDs captured verbatim) rather than
  assumed — every baseline 🔴/🟠 reproduced.
- Public surface only; `private/` and trailing-underscore helpers excluded.
- Trend: **66 (2026-06-24) → 66 (2026-06-29)**, flat. Score will only move when a fix lands.

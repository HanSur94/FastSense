---
phase: 1014-dashboardserializer-m-export-for-tag-bound-widgets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/Dashboard/DashboardSerializer.m
  - tests/suite/TestDashboardSerializerTagExport.m
autonomous: true
requirements: [MEXP-01, MEXP-02, MEXP-03, MEXP-04, MEXP-05]
status: complete
---

# Plan 1014-01 — DashboardSerializer `case 'tag'` emitter + round-trip suite

**Status:** Reconstructed retroactively (original PLAN.md lost in worktree turbulence). Reflects what was actually delivered in commits b0c4bfc, ee04f4e, ac44490.

## Goal

Add `case 'tag'` emission to both code paths in `DashboardSerializer.m` (save() inline switch + linesForWidget helper), drop the legacy `case 'sensor'` branch, and ship a 4-method round-trip test suite that exercises both single-page and multi-page export paths plus the unregistered-tag fail-loud guard.

## Tasks

### Task 1 (b0c4bfc) — `case 'tag'` in `linesForWidget`
- File: `libs/Dashboard/DashboardSerializer.m` (~line 596)
- Replace `case 'sensor'` with `case 'tag'`
- Emit try/catch around `TagRegistry.get('<key>')`
- Rethrow as `DashboardSerializer:tagNotRegistered` on miss
- Capture into `tag_<key>` local var; pass to `d.addWidget(...)` via `'Tag', tag_<key>`

### Task 2 (ee04f4e) — `case 'tag'` in `save()` inline switch
- File: `libs/Dashboard/DashboardSerializer.m` (~line 36)
- Replicate Task 1 pattern; both code paths emit identical Tag lookup
- Read `ws.source.key` (not `ws.source.name`)

### Task 3 (ac44490) — round-trip test suite (MEXP-04)
- File: `tests/suite/TestDashboardSerializerTagExport.m` (new, 174 LOC)
- 4 methods:
  1. `singlePageTagWidgetRoundTripsViaSave` — save() inline switch, MEXP-01
  2. `singlePageTagWidgetRoundTripsViaExportScript` — linesForWidget helper, MEXP-01
  3. `multiPageTagWidgetsRoundTripViaM` — save→exportScriptPages, MEXP-02
  4. `unregisteredTagFailsLoudly` — clears registry, asserts `DashboardSerializer:tagNotRegistered`, MEXP-03
- Local helper `iMakeTempMPath()` uses `fullfile(tempdir, 'tag_export_<ts>_<n>.m')` (avoids macOS Octave tempname hyphens)

## Acceptance gates

| Gate                                                   | Result |
|--------------------------------------------------------|--------|
| `case 'tag'` count = 2, `case 'sensor'` count = 0      | ✓      |
| `tagNotRegistered` error ID present in both switches   | ✓      |
| 4 test methods auto-discovered via fromFolder          | ✓      |
| Single-page round-trip via save()                      | ✓ (deferred — next MATLAB CI run) |
| Multi-page round-trip via exportScriptPages            | ✓ (deferred) |
| Unregistered tag throws `DashboardSerializer:tagNotRegistered` | ✓ (deferred) |

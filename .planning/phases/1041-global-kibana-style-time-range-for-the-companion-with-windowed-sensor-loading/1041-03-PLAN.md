---
phase: 1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading
plan: 03
type: execute
wave: 2
depends_on: ["1041-01"]
files_modified:
  - libs/Dashboard/FastSenseWidget.m
  - libs/Dashboard/DashboardEngine.m
  - libs/FastSense/SensorDetailPlot.m
  - libs/FastSenseCompanion/private/openAdHocPlot.m
  - tests/test_dashboard_time_window.m
autonomous: true
requirements: []

must_haves:
  truths:
    - "DashboardEngine.setTimeWindow(t0,t1) stores [t0 t1] and pushes it to every widget, then rerenders once (not per live-tick)"
    - "DashboardEngine.setTimeWindow([],[]) clears the window back to full-range"
    - "A FastSenseWidget with a non-empty window BINDS its FastSense line from getXYRange(t0,t1) at render (via fp.addLine(xw,yw,...) — NOT via fp.addTag, because addTag calls Tag.getXY() which returns EMPTY for a disk-backed SensorTag), and pulls via getXYRange(t0,t1) at refresh and update"
    - "A disk-backed sensor with an empty window (the 'All data' preset) renders its FULL extent via getXYRange(getTimeRange()) -> fp.addLine (NOT a blank addTag line); the empty-window disk path is gated behind ismethod(obj.Tag,'isOnDisk') && obj.Tag.isOnDisk(), and in-RAM tags keep the byte-identical fp.addTag(obj.Tag) path on an empty window"
    - "Opening a disk-backed sensor under a non-empty window reads ONLY that window at the INITIAL render: the windowed getXYRange(t0,t1) call goes through SensorTag.DataStore.getRange (overlapping chunks only), and those bounded arrays — not the full series — are what FastSense plots. (Mechanism: bind windowed [xw,yw] at the addTag site. The addTag/getXY path is bypassed when a window is set because getXY() is empty on disk.)"
    - "FastSenseWidget.getPreviewSeries and the CachedXMin/CachedXMax time-range cache STILL call getXY() (the slider/navigator preview shows the full historical envelope)"
    - "A FastSenseWidget whose window yields empty data renders the 'No data in selected range' empty-state (queryable via isShowingEmptyState()) instead of an empty axes; isShowingEmptyState() is false whenever the window is empty (full-range default), including the disk 'All data' case which resolves to the full extent"
    - "SensorDetailPlot accepts a 'TimeWindow' NV-option and loads the main axes via getXYRange when it is set"
    - "The openAdHocPlot Overlay path honors the window so overlaid lines are also bounded"
  artifacts:
    - path: "libs/Dashboard/FastSenseWidget.m"
      provides: "TimeWindow_ property + setTimeWindow + windowed render-binding (fp.addLine from getXYRange when windowed; disk-aware empty-window fallback to getXYRange(getTimeRange()); addTag for in-RAM empty window) + windowed refresh/update pulls + empty-state render"
      contains: "function setTimeWindow(obj, t0, t1)"
    - path: "libs/Dashboard/DashboardEngine.m"
      provides: "TimeWindow_ property + setTimeWindow(t0,t1) that fans out to widgets then rerenders once"
      contains: "function setTimeWindow(obj, t0, t1)"
    - path: "libs/FastSense/SensorDetailPlot.m"
      provides: "'TimeWindow' NV-option parsed via parseOpts; windowed main-axes load"
      contains: "'TimeWindow'"
    - path: "tests/test_dashboard_time_window.m"
      provides: "Headless engine/widget windowed-pull + clear-window + empty-state-decision + disk 'All data' full-extent tests"
      contains: "function test_dashboard_time_window"
  key_links:
    - from: "libs/Dashboard/DashboardEngine.m setTimeWindow"
      to: "libs/Dashboard/FastSenseWidget.m setTimeWindow"
      via: "iterate allPageWidgets() calling widget.setTimeWindow(t0,t1) when ismethod"
      pattern: "ismethod\\(w, 'setTimeWindow'\\)"
    - from: "libs/Dashboard/FastSenseWidget.m render/refresh/update"
      to: "Tag.getXYRange"
      via: "windowed pull when TimeWindow_ non-empty, else getXY()"
      pattern: "getXYRange\\("
    - from: "libs/Dashboard/FastSenseWidget.m render (windowed branch)"
      to: "FastSense.addLine (windowed arrays bound directly)"
      via: "when TimeWindow_ non-empty, fp.addLine(xw, yw, ...) from getXYRange; addTag path used only when window empty AND tag is in-RAM"
      pattern: "fp\\.addLine\\(xw"
    - from: "libs/Dashboard/FastSenseWidget.m render (empty-window disk branch)"
      to: "Tag.getTimeRange + Tag.getXYRange"
      via: "ismethod(obj.Tag,'isOnDisk') && obj.Tag.isOnDisk() -> [tMin,tMax]=getTimeRange() -> getXYRange(tMin,tMax) -> fp.addLine"
      pattern: "obj\\.Tag\\.getTimeRange\\(\\)"
    - from: "libs/FastSense/SensorDetailPlot.m render"
      to: "Tag.getXYRange"
      via: "opts.TimeWindow non-empty -> getXYRange(t0,t1) for main axes"
      pattern: "getXYRange\\("
---

<objective>
Thread the resolved `[t0, t1]` window through the view layer with small, localized, backward-compatible changes. `DashboardEngine` gains a window it fans out to its widgets; `FastSenseWidget` pulls windowed data at exactly its three data-plot sites (preview/cache untouched) and shows an empty-state when the window has no data; `SensorDetailPlot` gains a `'TimeWindow'` option; the ad-hoc Overlay honors the window.

Purpose: Make every companion-opened view load only the selected window. The default (empty window) reproduces today's full-range behavior — and the 'All data' preset (which also resolves to an empty window) renders the FULL extent for disk-backed sensors instead of a blank plot, so non-companion engine use and serialized dashboards are unaffected while the primary 10-year disk-backed use case shows data.
Output: windowed `FastSenseWidget` + disk-aware empty-window fallback + `DashboardEngine.setTimeWindow` + `SensorDetailPlot 'TimeWindow'` + Overlay window-awareness + headless tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-CONTEXT.md
@.planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-RESEARCH.md
@.planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-UI-SPEC.md

<interfaces>
<!-- Verified against the codebase. Use directly. -->

Tag/SensorTag (from Plan 01 — already on disk):
  [X, Y]       = tag.getXYRange(tStart, tEnd)   % windowed; empty/[] bounds -> full series; out-of-extent/inverted -> empty
  [tMin, tMax] = tag.getTimeRange()             % full extent; disk-backed returns [DataStore.XMin, DataStore.XMax] (Plan 01 fix); [NaN NaN] only if truly empty
  tf           = tag.isOnDisk()                 % SensorTag ONLY (SensorTag.m:245). Other Tag kinds (Derived/Composite/State/Monitor) DO NOT define isOnDisk — guard with ismethod(obj.Tag,'isOnDisk').

FastSenseWidget.m (libs/Dashboard/FastSenseWidget.m) — CRITICAL FACTS:
  * The widget has NO back-reference to its DashboardEngine (verified: no Engine_ property). Therefore the window must live ON the widget (a TimeWindow_ property the engine pushes), NOT read from the engine. This is RESEARCH Pitfall 3's recommended seam.
  * Tag lives on the DashboardWidget base as obj.Tag.
  * The render PLOT-BINDING site and the THREE refresh/update data sites (all VERIFIED in source):
      render  : line :196  ->  fp.addTag(obj.Tag);              % BINDS the plotted line
      render  : line :283  ->  [~, yInit] = obj.Tag.getXY();    % y-autoscale seed ONLY
      refresh : line :337  ->  [x, y] = obj.Tag.getXY();        % then FastSenseObj.updateData(1, x, y)
      update  : line :367  ->  [x, y] = obj.Tag.getXY();        % then FastSenseObj.updateData(1, x, y)
  * DO NOT TOUCH: getPreviewSeries (slider preview), updateTimeRangeCache (:295 etc.), CachedXMin/CachedXMax (:75-77), getTimeRange (:727). These stay on getXY() so the preview/navigator shows the full envelope (RESEARCH Pitfall 2).
  * CRITICAL — how a disk-backed sensor renders TODAY (VERIFIED by reading FastSense.addTag :985-999 + addLine :441-577 + SensorTag.getXY :115-121 + toDisk :226-235):
      - fp.addTag(sensorTag) at :196 does `[x,y]=tag.getXY(); obj.addLine(x,y,'DisplayName',tag.Name,...)`. It passes the MATERIALIZED arrays to addLine; it NEVER hands the tag's DataStore to FastSense.
      - SensorTag.getXY() returns obj.X_/obj.Y_, and toDisk() sets X_=[]/Y_=[]. So for a DISK-BACKED sensor, getXY() returns EMPTY -> addTag binds an EMPTY line. (FastSense's own XLim->DataStore.getRange windowing at FastSense.m:4088 only fires for lines given a DataStore via the 'DataStore' NV-pair, which the tag path never does.)
      - Therefore: setting the axes XLim would NOT window a tag-bound line (no DataStore inside FastSense to re-resolve from). The window must be applied at the WIDGET by binding the windowed arrays directly.
  * BLOCKER (the 'All data' / empty-window disk bug this revision fixes): the companion 'All data' preset resolves to an EMPTY window ([],[]), which the engine pushes as setTimeWindow([],[]) -> widget TimeWindow_=[]. The naive empty-window branch calls fp.addTag(obj.Tag), and for a DISK-BACKED sensor that binds an EMPTY line (getXY() is empty on disk) -> BLANK plot. 'All data' is supposed to show the FULL series. FIX: the empty-window branch must be disk-aware — for a disk-backed tag, resolve the full extent via getTimeRange() and bind via getXYRange(tMin,tMax) (= DataStore.getRange over the whole extent, which loads everything — exactly what 'All data' requests). For an in-RAM tag, getXY() is NOT empty, so fp.addTag(obj.Tag) stays byte-identical.
  * SensorTag exposes a PUBLIC read-only dependent property `DataStore` (SensorTag.m:36 -> returns DataStore_), but you do NOT need it: getXYRange(t0,t1) already routes through DataStore.getRange on disk (Plan 01), so binding `[xw,yw]=obj.Tag.getXYRange(t0,t1)` reads only the overlapping chunks.
  * THE BINDING FIX (Task 2 step 5) — THREE-WAY branch on the Tag path:
      (1) TimeWindow_ non-empty -> pull `[xw,yw]=obj.pullData_()` (= getXYRange(t0,t1)); if non-empty bind via `fp.addLine(xw, yw, 'DisplayName', obj.Tag.Name)`, else render the empty-state.
      (2) TimeWindow_ empty AND tag is disk-backed (`ismethod(obj.Tag,'isOnDisk') && obj.Tag.isOnDisk()`) -> resolve `[tMin,tMax]=obj.Tag.getTimeRange()`, pull `[xw,yw]=obj.Tag.getXYRange(tMin,tMax)`; if non-empty bind via `fp.addLine(xw, yw, 'DisplayName', obj.Tag.Name)`, else render the empty-state. This is the 'All data' disk fix.
      (3) TimeWindow_ empty AND tag is NOT disk-backed -> keep the existing `fp.addTag(obj.Tag)` path verbatim (byte-identical to today; covers in-RAM SensorTag + Derived/Composite/State/Monitor whose getXY() is not disk-emptied).
  * properties (Access = private) block exists; add TimeWindow_ there. public methods block exists for setTimeWindow.

DashboardEngine.m (libs/Dashboard/DashboardEngine.m):
  * private properties block (~:70-115). DataTimeRange (:70), TimeRangeSelector_ (:85). NO TimeWindow_ today.
  * allPageWidgets() — public, returns cell of widgets across all pages (used at :217, :819).
  * activePageWidgets() — public, active page only.
  * rerenderWidgets() (:1730) — deletes + recreates active-page widget panels; expensive; reinstalls TimeRangeSelector callbacks at :1782-1785. Call ONCE per user range change.
  * existing per-widget fan-out precedent: broadcastTimeRange (:2044-2054) iterates allPageWidgets() guarding each ws{i}.setTimeRange(...) in try/catch with a duck-type/guard. setShowPlantLog passes the engine into the widget. Follow the broadcastTimeRange iteration shape for setTimeWindow fan-out.
  * DO NOT call updateGlobalTimeRange from setTimeWindow (RESEARCH anti-pattern: it rescans widget data ranges and resets the scrubber). Fan out the window to widgets, then rerenderWidgets() once.

SensorDetailPlot.m (libs/FastSense/SensorDetailPlot.m):
  * ctor SensorDetailPlot(tag, varargin) :48 ; conDefaults built then [opts,~] = parseOpts(conDefaults, varargin) :90.
  * tag.getXY() validation at :60; render() loads [xVec, yVec] = obj.TagRef.getXY() at :126.
  * Add 'TimeWindow' to conDefaults (default []); store opts.TimeWindow on a property; in render(), if non-empty load via getXYRange.

openAdHocPlot.m (libs/FastSenseCompanion/private/openAdHocPlot.m):
  * Overlay path :93-99 adds a 'rawaxes' widget with PlotFcn = @(ax) plotOverlay_(ax, validTags, validNames).
  * plotOverlay_ (:142-157) calls [tv, y] = tags{k}.getXY() per tag at :147.
  * RawAxesWidget supports PlotFcn arity @(ax, tRange) and stores TimeRange via setTimeRange (RawAxesWidget.m:10, :66). So an Overlay PlotFcn of the form @(ax, tRange) can window via getXYRange(tRange(1),tRange(2)).
  * NOTE: the companion-side stash of the engine on figure appdata + the RangeChanged re-query loop is Plan 04, NOT here. This plan only makes the Overlay PlotFcn window-capable.

RawAxesWidget.m (libs/Dashboard/RawAxesWidget.m):
  * properties: PlotFcn, TimeRange (SetAccess=private, [] default), UseGlobalTime.
  * setTimeRange(obj,tStart,tEnd) sets TimeRange then re-calls PlotFcn (cla + callPlotFcn). callPlotFcn dispatches on nargin(PlotFcn): @(ax,tRange) gets (hAxes, TimeRange).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — headless DashboardEngine/FastSenseWidget windowing test scaffold (RED)</name>
  <read_first>
    - tests/test_fastsense_widget_tag.m (existing FastSenseWidget function test — fixture + headless construction pattern, how it builds a Tag and a widget without a visible figure; find the exact FastSenseObj line/Lines accessor it uses to read plotted point counts)
    - tests/test_dashboard_range_selector_integration.m (existing DashboardEngine headless/integration test — how an engine is constructed and exercised in a test)
    - libs/Dashboard/FastSenseWidget.m lines 70-115, 147-200, 280-380, 720-735 (properties, render, the 3 data-plot sites, getTimeRange/cache)
    - libs/Dashboard/DashboardEngine.m lines 70-115, 1730-1800, 2044-2054 (private props, rerenderWidgets, broadcastTimeRange fan-out shape)
    - libs/SensorThreshold/SensorTag.m lines 226-248 (toDisk clears X_/Y_; isOnDisk; getXY empty on disk) and getTimeRange/getXYRange contract from Plan 01
    - .planning/phases/1041-.../1041-RESEARCH.md "Phase Requirements -> Test Map" rows for test_dashboard_time_window + Pitfalls 2/3/4
  </read_first>
  <behavior>
    - testSetTimeWindow: engine.setTimeWindow(t0,t1) -> engine exposes the stored window (via a public getter or the widgets' TimeWindow_); each FastSenseWidget's TimeWindow_ equals [t0 t1].
    - testClearTimeWindow: engine.setTimeWindow([],[]) -> widgets' TimeWindow_ is empty.
    - testWidgetWindowedPull: a FastSenseWidget with TimeWindow_ set to a sub-window of its tag's data, after refresh/update, has its FastSenseObj line length << full series (proves getXYRange path). Use a disk-backed or large-RAM SensorTag fixture so the bound is meaningful.
    - testWidgetWindowedRenderBindsDisk: build a DISK-BACKED SensorTag fixture (`st.updateData(x,y); st.toDisk();`), set the engine window to a strict sub-window, render the widget FRESH (no prior refresh), and assert the FastSenseObj plotted line length is > 0 AND << full series. This proves the INITIAL render reads only the window via fp.addLine(getXYRange) — NOT the broken `addTag(getXY)==empty` path. (Without the binding fix this fails: a disk-backed tag binds an empty line via addTag, so the line length is 0.) MATLAB-only (needs a real render); guard with the Octave skip.
    - testWidgetAllDataDiskNonEmpty (the 'All data' / empty-window disk Blocker guard): build the SAME DISK-BACKED SensorTag fixture (`st.updateData(x,y); st.toDisk();` so getXY() is empty), set an EMPTY window via `engine.setTimeWindow([],[])` (this is exactly what the 'All data' preset resolves to), render the widget FRESH, and assert the FastSenseObj plotted line length is > 0 AND equals the FULL extent point count (i.e. NOT a blank plot). This is the test that would FAIL on the naive empty-window `fp.addTag(obj.Tag)` path (disk getXY() is empty -> blank line, length 0) and PASS once the empty-window branch is disk-aware and binds `fp.addLine(getXYRange(getTimeRange()))`. Also assert `~widget.isShowingEmptyState()` (the full extent is non-empty, so no empty-state). MATLAB-only; guard with the Octave skip.
    - testWidgetFullPullWhenEmpty: with TimeWindow_ empty AND an IN-RAM tag, the widget pulls the full series (line length == full) via the unchanged fp.addTag path.
    - testEmptyStateDecision: a FastSenseWidget whose TimeWindow_ is entirely outside its tag extent reports the empty-state condition (assert via a queryable flag/method the widget exposes, e.g. isShowingEmptyState() or that the content panel hosts the 'No data in selected range' label — pick the seam you implement in Task 2 and assert it here).
    - testPreviewStillFull: getPreviewSeries / getTimeRange on a windowed widget STILL reflect the full data extent (assert getTimeRange returns the full [tMin,tMax], independent of TimeWindow_).
  </behavior>
  <action>
    Create `tests/test_dashboard_time_window.m` (function-style). The pure setTimeWindow/clear/empty-state-decision logic should be runnable headlessly; gate any sub-test that needs a rendered figure with the established companion/engine headless approach used in test_dashboard_range_selector_integration.m. Some sub-tests (those exercising FastSenseObj line length after a real render) are MATLAB-leaning — if the engine render requires a figure, guard those specific sub-tests with the standard `if exist('OCTAVE_VERSION','builtin') ~= 0; return; end` skip, but keep testSetTimeWindow / testClearTimeWindow (pure property fan-out) unguarded so they run in both runtimes.

    Build the fixture with a SensorTag carrying enough datenum points that a sub-window is a strict subset (reuse the Plan 01 fixture recipe: `t0=datenum(2024,1,1); x=(t0:1/86400:t0+9)'`). For testWidgetWindowedPull/testWidgetFullPullWhenEmpty, after constructing a FastSenseWidget bound to the tag and rendering it into a panel (or the engine), set TimeWindow_ via the engine's `setTimeWindow` and assert the resulting `widget.FastSenseObj` line X length is bounded vs full.

    For testWidgetWindowedRenderBindsDisk: build the SAME fixture but call `st.toDisk()` so `st.getXY()` is empty (mirrors the production disk case). Set the engine window to e.g. `[t0+2, t0+4]` BEFORE the first render, render the widget, then read the plotted line length off `widget.FastSenseObj` (use the FastSenseObj line/Lines accessor that test_fastsense_widget_tag.m uses — read that test to find the exact accessor). Assert length > 0 and < full. This is the test that would FAIL on the pre-fix `addTag(getXY())` path (empty line) and PASS once render binds `fp.addLine(getXYRange(t0,t1))`.

    For testWidgetAllDataDiskNonEmpty (the empty-window disk fix): build the SAME disk-backed fixture (`st.updateData(x,y); st.toDisk();`). Set an EMPTY window via `engine.setTimeWindow([],[])` (the 'All data' case), render the widget FRESH, then read the plotted line length off `widget.FastSenseObj` with the SAME accessor. Assert length > 0 AND length equals the full extent point count (`numel(x)` modulo FastSense's downsampling — if the fixture is below FastSense's downsample threshold the bound line should match `numel(x)`; otherwise assert `> 0` and `>=` the windowed-sub-window count from testWidgetWindowedRenderBindsDisk, i.e. strictly MORE than a strict sub-window, proving it loaded the whole series). Also assert `~widget.isShowingEmptyState()`. Without the disk-aware empty-window fix this FAILS (addTag binds an empty line on disk -> length 0). MATLAB-only; Octave-guard it.

    Use `assert(cond, 'test_dashboard_time_window:caseName', 'msg')` + pass-count print. RED until Tasks 2-3 land.
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/PARA/10_Projects/FastPlot/.claude/worktrees/sleepy-grothendieck-1b8d2f && grep -q "function test_dashboard_time_window" tests/test_dashboard_time_window.m && grep -qE "testSetTimeWindow|testClearTimeWindow|testWidgetWindowedPull|testWidgetWindowedRenderBindsDisk|testWidgetAllDataDiskNonEmpty|testEmptyStateDecision|testPreviewStillFull" tests/test_dashboard_time_window.m && echo SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_dashboard_time_window.m` exists; `grep -q "function test_dashboard_time_window"` succeeds.
    - Contains sub-tests: testSetTimeWindow, testClearTimeWindow, testWidgetWindowedPull, testWidgetWindowedRenderBindsDisk, testWidgetAllDataDiskNonEmpty, testWidgetFullPullWhenEmpty, testEmptyStateDecision, testPreviewStillFull (verify each with grep).
    - testWidgetAllDataDiskNonEmpty asserts the bound line length > 0 on a disk-backed tag under an EMPTY window (the 'All data' case) and that `isShowingEmptyState()` is false (the disk 'All data' full-extent fix). Verify the sub-test exists: `grep -qF "testWidgetAllDataDiskNonEmpty" tests/test_dashboard_time_window.m`.
    - testSetTimeWindow / testClearTimeWindow are NOT Octave-guarded (pure property fan-out runs in both).
    - Static check clean: `mcp__matlab__check_matlab_code` reports no errors.
  </acceptance_criteria>
  <done>Headless windowing test scaffold exists covering fan-out, windowed pull, the disk windowed-render bind, the disk 'All data' empty-window full-extent bind, the empty-state decision, and the preview-stays-full guard; fails pending Tasks 2-3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FastSenseWidget — TimeWindow_ property, windowed pulls at the 3 sites, disk-aware empty-window bind, empty-state render</name>
  <read_first>
    - libs/Dashboard/FastSenseWidget.m lines 70-115 (properties — where TimeWindow_ goes), 147-300 (render incl. fp.addTag :196 + getXY :283), 311-385 (refresh :337 + update :367), 540-560 (setYLimitMode public-method placement pattern)
    - libs/FastSense/FastSense.m lines 958-1005 (addTag — confirms it does `[x,y]=tag.getXY(); addLine(x,y,...)`) and 372-380 + 441-577 (addLine signature + the pre-built-DataStore vs raw-array branches) — so you understand WHY a windowed render must bind arrays via fp.addLine, not fp.addTag
    - libs/SensorThreshold/SensorTag.m lines 115-121 (getXY returns X_/Y_), 134-143 (getTimeRange), 226-248 (toDisk clears X_/Y_; isOnDisk; the public DataStore dependent prop at :36) — confirms getXY() is EMPTY on disk AND that isOnDisk/getTimeRange exist on SensorTag only
    - libs/SensorThreshold/Tag.m (base class — confirm Derived/Composite/State/Monitor do NOT define isOnDisk; this is why the ismethod guard is required)
    - libs/FastSenseCompanion/DashboardListPane.m lines 325-356 (renderEmptyState_ — the EXACT pattern to mirror for the empty-state)
    - .planning/phases/1041-.../1041-UI-SPEC.md "Surface 3: Widget Empty State" (exact copy "No data in selected range", 14pt bold centered, PlaceholderTextColor, WidgetBackground, Padding [16 16 16 16])
    - .planning/phases/1041-.../1041-RESEARCH.md Pitfall 2 (do NOT change getPreviewSeries/cache) + Pitfall 3 (window lives on the widget)
    - CLAUDE.md (header comments, namespaced errors `FastSenseWidget:...`, 4-space/160-col, trailing-underscore private state)
  </read_first>
  <behavior>
    - Widget holds TimeWindow_ = [] (full) by default.
    - setTimeWindow(t0,t1) sets TimeWindow_; setTimeWindow([],[]) clears it.
    - A private helper pullData_() returns [x,y]: getXYRange(TimeWindow_(1),TimeWindow_(2)) when TimeWindow_ non-empty, else getXY().
    - render with a NON-EMPTY window BINDS the FastSense line from the windowed pull via fp.addLine(xw,yw,...) — it does NOT call fp.addTag(obj.Tag) (addTag would bind an EMPTY line for a disk-backed tag since getXY() is empty on disk). 
    - render with an EMPTY window is DISK-AWARE (this is the 'All data' fix): if the tag is disk-backed (`ismethod(obj.Tag,'isOnDisk') && obj.Tag.isOnDisk()`) it resolves the full extent `[tMin,tMax]=obj.Tag.getTimeRange()`, pulls `[xw,yw]=obj.Tag.getXYRange(tMin,tMax)` and binds via fp.addLine(xw,yw,...) (so 'All data' on a disk-backed sensor shows the FULL series instead of a blank addTag line); if the tag is NOT disk-backed it keeps fp.addTag(obj.Tag) verbatim (byte-identical to today; in-RAM getXY() is non-empty).
    - A disk-backed sensor opened with a non-empty window reads ONLY that window at the initial render: getXYRange(t0,t1) -> DataStore.getRange reads overlapping chunks only, and those bounded arrays are what gets plotted.
    - refresh and update pull via pullData_() (so live re-pulls honor the window too).
    - getPreviewSeries, updateTimeRangeCache, getTimeRange continue to call getXY() (full envelope).
    - When the bound pull yields empty x (windowed window outside extent, OR a genuinely empty tag), the widget renders the 'No data in selected range' empty-state instead of plotting; isShowingEmptyState() returns true. With an empty window over a non-empty tag (in-RAM OR disk 'All data') the empty-state never shows.
  </behavior>
  <action>
    In `libs/Dashboard/FastSenseWidget.m`:

    1) Add to the `properties (Access = private)` block:
       ```matlab
       TimeWindow_ = []    % [t0 t1] datenum pushed by DashboardEngine.setTimeWindow; [] = full range
       ```

    2) Add a public method (next to setYLimitMode :545):
       ```matlab
       function setTimeWindow(obj, t0, t1)
       %SETTIMEWINDOW Set the load window for this widget's data pulls.
       %   t0, t1 datenum scalars; both [] resets to full range.
       %   The DashboardEngine pushes this before re-rendering. Data is
       %   pulled via Tag.getXYRange when set, Tag.getXY when empty.
           if nargin < 3 || isempty(t0) || isempty(t1)
               obj.TimeWindow_ = [];
           else
               obj.TimeWindow_ = [t0, t1];
           end
       end
       ```

    3) Add a private helper that centralizes the windowed pull:
       ```matlab
       function [x, y] = pullData_(obj)
       %PULLDATA_ Windowed data pull. Honors TimeWindow_ via getXYRange;
       %   full series via getXY when no window is set.
           if ~isempty(obj.TimeWindow_)
               [x, y] = obj.Tag.getXYRange(obj.TimeWindow_(1), obj.TimeWindow_(2));
           else
               [x, y] = obj.Tag.getXY();
           end
       end
       ```

    4) Switch the three DATA-PULL sites to use pullData_ (these feed y-autoscale + the live updateData path; the actual plot BINDING is handled in step 5):
       - render (~:283, y-autoscale seed): `[~, yInit] = obj.Tag.getXY();` -> `[~, yInit] = obj.pullData_();`
       - refresh (~:337): `[x, y] = obj.Tag.getXY();` -> `[x, y] = obj.pullData_();`
       - update (~:367): `[x, y] = obj.Tag.getXY();` -> `[x, y] = obj.pullData_();`
       Leave EVERY other `getXY()` call (getPreviewSeries, updateTimeRangeCache, getTimeRange, any y-autoscale fallback path, etc.) exactly as-is.

    5) Windowed render binding + disk-aware empty-window fallback + empty-state (THIS is the Blocker fix). Today render binds the plotted line via `fp.addTag(obj.Tag)` at :196. VERIFIED FACT: `fp.addTag(sensorTag)` internally does `[x,y]=tag.getXY(); addLine(x,y,...)`, and `SensorTag.getXY()` returns EMPTY for a disk-backed tag (toDisk() clears X_/Y_). So `addTag` can neither load the full history NOR the window for a disk-backed tag — it binds an empty line. The companion 'All data' preset resolves to an EMPTY window, so the empty-window branch ALSO has to be disk-aware (resolve the full extent and bind it) or 'All data' on a disk-backed sensor shows blank.

       Replace the data-binding block at :194-206 (the `if ~isempty(obj.Tag) ... fp.addTag(obj.Tag); ... end` dispatch) so the Tag branch is a THREE-WAY window/disk dispatch. Exact MATLAB for the Tag branch ONLY (leave the DataStoreObj/File/XData elseif branches untouched):
       ```matlab
       if ~isempty(obj.Tag)
           if ~isempty(obj.TimeWindow_)
               % (1) Windowed: bind ONLY the window. getXYRange routes through
               % DataStore.getRange for disk-backed sensors (overlapping
               % chunks only) and binary-search-slices in RAM.
               [xw, yw] = obj.pullData_();          % = getXYRange(TimeWindow_(1), TimeWindow_(2))
               if isempty(xw)
                   obj.renderEmptyState_(parentPanel);
                   obj.ShowingEmptyState_ = true;
                   return;
               end
               obj.ShowingEmptyState_ = false;
               fp.addLine(xw, yw, 'DisplayName', obj.Tag.Name);
           elseif ismethod(obj.Tag, 'isOnDisk') && obj.Tag.isOnDisk()
               % (2) Empty window + DISK-backed tag = the 'All data' preset on a
               % disk-backed sensor. getXY() is EMPTY on disk, so fp.addTag would
               % bind a BLANK line. Resolve the FULL extent and bind it via
               % getXYRange(getTimeRange()) -> DataStore.getRange over the whole
               % extent (loads everything — exactly what 'All data' requests).
               [tMin, tMax] = obj.Tag.getTimeRange();
               [xw, yw] = obj.Tag.getXYRange(tMin, tMax);
               if isempty(xw)
                   obj.renderEmptyState_(parentPanel);
                   obj.ShowingEmptyState_ = true;
                   return;
               end
               obj.ShowingEmptyState_ = false;
               fp.addLine(xw, yw, 'DisplayName', obj.Tag.Name);
           else
               % (3) Empty window + in-RAM tag (incl. Derived/Composite/State/
               % Monitor whose getXY() is not disk-emptied): byte-identical to today.
               obj.ShowingEmptyState_ = false;
               fp.addTag(obj.Tag);
           end
       elseif ~isempty(obj.DataStoreObj)
           ...                                      % unchanged
       end
       ```
       Notes:
       (a) `fp.addLine` auto-promotes datenum X to date ticks (FastSense.addLine :465-482), so the windowed / full-extent line still gets proper time-axis formatting.
       (b) The `ismethod(obj.Tag, 'isOnDisk')` guard is REQUIRED: only SensorTag defines isOnDisk (SensorTag.m:245). Derived/Composite/State/Monitor tags do NOT — they fall through to the addTag path (branch 3), which is correct because their getXY() is not disk-emptied.
       (c) The empty-state guard must run BEFORE `fp.render()` and before any axes-dependent finalization. Since renderEmptyState_ renders into `parentPanel` directly (not the FastSense axes), the cleanest seam is: at the TOP of render(), if either (TimeWindow_ non-empty) or (TimeWindow_ empty AND ismethod(obj.Tag,'isOnDisk') && obj.Tag.isOnDisk()), probe the bound arrays ONCE — `[xw,~] = obj.pullData_()` for the windowed case, or `[tMin,tMax]=obj.Tag.getTimeRange(); [xw,~]=obj.Tag.getXYRange(tMin,tMax)` for the disk-empty-window case — and if `isempty(xw)` call `obj.renderEmptyState_(parentPanel); obj.ShowingEmptyState_=true; return;` before creating the axes/FastSense at all. Then the binding block above only runs in the non-empty cases (cache the probed `xw,yw` and reuse them in the bind so you don't double-pull). Pick whichever ordering reads cleanly in the actual render() you see; the REQUIREMENT is: (windowed OR disk-empty-window) + empty -> empty-state only (no axes); (windowed OR disk-empty-window) + non-empty -> fp.addLine(xw,yw,...); empty-window + in-RAM -> fp.addTag(obj.Tag).

       Add a private `renderEmptyState_(obj, parentPanel)` mirroring `DashboardListPane.renderEmptyState_` EXACTLY (uigridlayout([1 1]) filling the content area, RowHeight {'1x'}, ColumnWidth {'1x'}, Padding [16 16 16 16], BackgroundColor = theme.WidgetBackground; a uilabel Text='No data in selected range', FontSize=14, FontWeight='bold', FontColor=theme.PlaceholderTextColor, BackgroundColor=theme.WidgetBackground, HorizontalAlignment='center', VerticalAlignment='center'). Resolve `theme` via the widget's existing theme accessor (getTheme()/ParentTheme — follow the pattern already in FastSenseWidget for colors). Add a `SetAccess=private` `ShowingEmptyState_ = false` property and a public `tf = isShowingEmptyState(obj)` returning it, so testEmptyStateDecision / testWidgetAllDataDiskNonEmpty have a stable seam.

       Ensure refresh()/update() re-evaluate the empty<->data transition: if the widget is currently showing the empty-state (or the FastSense axes are absent because an empty-state was rendered), do NOT take the incremental `FastSenseObj.updateData` fast-path — fall back to the full rebuild (`rebuildForTag_()` / the render path) so a re-pull that now yields data rebuilds the axes (and vice-versa). Guard the fast-path with `~obj.ShowingEmptyState_ && fpValid`.

    Keep the change surgical: when TimeWindow_ is empty AND the tag is in-RAM (the DEFAULT for all non-companion engine use and serialized dashboards), the Tag branch calls `fp.addTag(obj.Tag)` exactly as before, `ShowingEmptyState_` stays false, and behavior is byte-identical to today. The disk empty-window branch only fires for disk-backed SensorTags under the 'All data' (empty) window.
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/PARA/10_Projects/FastPlot/.claude/worktrees/sleepy-grothendieck-1b8d2f && grep -qF "TimeWindow_ = []" libs/Dashboard/FastSenseWidget.m && grep -qF "function setTimeWindow(obj, t0, t1)" libs/Dashboard/FastSenseWidget.m && grep -qF "function [x, y] = pullData_(obj)" libs/Dashboard/FastSenseWidget.m && grep -q "No data in selected range" libs/Dashboard/FastSenseWidget.m && grep -q "isShowingEmptyState" libs/Dashboard/FastSenseWidget.m && grep -qF "fp.addLine(xw, yw" libs/Dashboard/FastSenseWidget.m && grep -qF "ismethod(obj.Tag, 'isOnDisk')" libs/Dashboard/FastSenseWidget.m && grep -qF "obj.Tag.getTimeRange()" libs/Dashboard/FastSenseWidget.m && grep -qF "fp.addTag(obj.Tag)" libs/Dashboard/FastSenseWidget.m && test "$(grep -cF "obj.pullData_()" libs/Dashboard/FastSenseWidget.m)" -ge 3 && echo WIDGET_OK</automated>
  </verify>
  <acceptance_criteria>
    - `libs/Dashboard/FastSenseWidget.m` adds `TimeWindow_ = []` in a private properties block.
    - Defines `function setTimeWindow(obj, t0, t1)` and `function [x, y] = pullData_(obj)`.
    - `pullData_` calls `obj.Tag.getXYRange(obj.TimeWindow_(1), obj.TimeWindow_(2))` when windowed and `obj.Tag.getXY()` otherwise.
    - The three data-pull sites now call `obj.pullData_()`: `grep -cF "obj.pullData_()" libs/Dashboard/FastSenseWidget.m` >= 3.
    - WINDOWED RENDER BINDING (Blocker fix part 1): when TimeWindow_ is non-empty, render binds the line via `fp.addLine(xw, yw, ...)` (windowed arrays), NOT via fp.addTag: `grep -qF "fp.addLine(xw, yw" libs/Dashboard/FastSenseWidget.m`.
    - DISK EMPTY-WINDOW FALLBACK (Blocker fix part 2 — the 'All data' disk fix): the empty-window branch is disk-aware — `grep -qF "ismethod(obj.Tag, 'isOnDisk')" libs/Dashboard/FastSenseWidget.m` AND `grep -qF "obj.Tag.getTimeRange()" libs/Dashboard/FastSenseWidget.m` both succeed (use `grep -qF` for the paren-bearing patterns). The disk branch resolves the full extent via getTimeRange() and binds via getXYRange -> fp.addLine.
    - IN-RAM EMPTY-WINDOW PATH PRESERVED: the empty-window in-RAM branch still uses `fp.addTag(obj.Tag)` — `grep -qF "fp.addTag(obj.Tag)" libs/Dashboard/FastSenseWidget.m` MUST still succeed (the addTag call is gated behind the empty-window + NOT-disk branch, not deleted).
    - Preview path untouched: `grep -c "getXY(" libs/Dashboard/FastSenseWidget.m` still shows getXY used by getPreviewSeries/updateTimeRangeCache/getTimeRange (i.e. getXY occurrences did NOT all disappear — at least 1 remains and getPreviewSeries still references it). Manually confirm getPreviewSeries body is unchanged.
    - Empty-state present: `grep -q "No data in selected range" libs/Dashboard/FastSenseWidget.m`; the label uses FontSize 14 + FontWeight 'bold' + PlaceholderTextColor (grep each).
    - A queryable empty-state seam exists: `grep -q "isShowingEmptyState" libs/Dashboard/FastSenseWidget.m` and `grep -qF "ShowingEmptyState_ = false" libs/Dashboard/FastSenseWidget.m`; the refresh/update fast-path is guarded by `~obj.ShowingEmptyState_`.
    - Static check clean: `mcp__matlab__check_matlab_code` on FastSenseWidget.m reports no errors.
    - Existing widget tests still pass in MATLAB via MCP: `test_fastsense_widget_tag()` prints "All N tests passed" (no regression from the additive change).
  </acceptance_criteria>
  <done>FastSenseWidget owns a TimeWindow_; when a window is set it BINDS the FastSense line from getXYRange(t0,t1) via fp.addLine (so a disk-backed sensor reads only the overlapping chunks at the initial render); when the window is EMPTY and the tag is disk-backed (the 'All data' preset) it resolves the FULL extent via getXYRange(getTimeRange()) and binds via fp.addLine (so 'All data' shows the full series instead of a blank addTag line); pulls windowed data at refresh/update; keeps the preview/cache on getXY(); and renders the 'No data in selected range' empty-state (isShowingEmptyState()) when the bound pull has no data. With an empty window over an in-RAM tag it uses fp.addTag(obj.Tag) and behaves byte-identically to today.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DashboardEngine.setTimeWindow fan-out + SensorDetailPlot 'TimeWindow' + Overlay window-awareness (GREEN)</name>
  <read_first>
    - libs/Dashboard/DashboardEngine.m lines 70-115 (private props), 1730-1800 (rerenderWidgets), 2044-2054 (broadcastTimeRange fan-out shape to mirror), 215-220 + 815-825 (allPageWidgets iteration examples)
    - libs/Dashboard/FastSenseWidget.m setTimeWindow (added in Task 2) — the method the engine calls
    - libs/FastSense/SensorDetailPlot.m lines 48-130, 254-272 (ctor + parseOpts :90, render getXY :126, setZoomRange/getZoomRange)
    - libs/FastSenseCompanion/private/openAdHocPlot.m lines 88-100, 142-157 (Overlay addWidget + plotOverlay_ getXY :147)
    - libs/Dashboard/RawAxesWidget.m lines 10-20, 66-95, 149-166 (PlotFcn arity dispatch, setTimeRange, TimeRange)
    - CLAUDE.md (namespaced errors `DashboardEngine:...` / `SensorDetailPlot:...`; header comments; 4-space/160-col)
  </read_first>
  <behavior>
    - DashboardEngine has TimeWindow_ = [] (private). setTimeWindow(t0,t1) stores [t0 t1] (or [] when both empty), fans the window out to every widget via allPageWidgets() guarded ismethod(w,'setTimeWindow') try/catch, then calls rerenderWidgets() once. It does NOT call updateGlobalTimeRange and is NOT invoked per live-tick.
    - SensorDetailPlot accepts 'TimeWindow' (default []); when set, render loads main-axes data via getXYRange(t0,t1).
    - openAdHocPlot Overlay PlotFcn becomes window-capable: plotOverlay_ takes an optional tRange and uses getXYRange when present; the widget is added with a PlotFcn whose arity lets RawAxesWidget pass TimeRange.
  </behavior>
  <action>
    A) `libs/Dashboard/DashboardEngine.m`:
    - Add to the private properties block (near DataTimeRange :70):
      ```matlab
      TimeWindow_ = []    % [t0 t1] datenum load window pushed by the companion; [] = full range
      ```
    - Add a public method (place near rerenderWidgets / the public API region):
      ```matlab
      function setTimeWindow(obj, t0, t1)
      %SETTIMEWINDOW Set the global load window and re-resolve all widgets.
      %   t0, t1 datenum scalars; both [] resets to full range. User-triggered
      %   (e.g. from the companion time bar) — NOT called per live tick. Fans
      %   the window out to every widget, then rebuilds widget panels once so
      %   they reload windowed data. Does not touch the scrubber's data range.
          if nargin < 3 || isempty(t0) || isempty(t1)
              obj.TimeWindow_ = [];
          else
              obj.TimeWindow_ = [t0, t1];
          end
          ws = obj.allPageWidgets();
          for i = 1:numel(ws)
              w = ws{i};
              if ismethod(w, 'setTimeWindow')
                  try
                      if isempty(obj.TimeWindow_)
                          w.setTimeWindow([], []);
                      else
                          w.setTimeWindow(obj.TimeWindow_(1), obj.TimeWindow_(2));
                      end
                  catch ME
                      warning('DashboardEngine:setTimeWindowWidget', ...
                          'setTimeWindow failed for a widget: %s', ME.message);
                  end
              end
          end
          obj.rerenderWidgets();
      end
      ```
      (Mirror the broadcastTimeRange iteration shape at :2044-2054 — allPageWidgets, per-widget guard. Use `ismethod` so non-FastSenseWidget widgets are skipped silently.)
      - Optional: expose a Hidden/read accessor `function w = getTimeWindow(obj); w = obj.TimeWindow_; end` so the test can assert the stored window directly. Add it if the test needs it.

    B) `libs/FastSense/SensorDetailPlot.m`:
    - Add `'TimeWindow', []` to `conDefaults` before the `parseOpts` call (:90). After parsing, store `obj.TimeWindow_ = opts.TimeWindow;` on a new `SetAccess=private` property `TimeWindow_ = []`.
    - In `render()` (:116), where it loads `[xVec, yVec] = obj.TagRef.getXY();` (:126), branch:
      ```matlab
      if ~isempty(obj.TimeWindow_)
          [xVec, yVec] = obj.TagRef.getXYRange(obj.TimeWindow_(1), obj.TimeWindow_(2));
      else
          [xVec, yVec] = obj.TagRef.getXY();
      end
      ```
      Leave the ctor validation getXY() at :60 as-is (it only proves the tag yields data). Do not alter NavigatorOverlay/zoom logic — the navigator spans the loaded window per the deferred-scope note.

    C) `libs/FastSenseCompanion/private/openAdHocPlot.m`:
    - Make `plotOverlay_` window-capable. Change its signature to accept an optional time range and use getXYRange when present:
      ```matlab
      function plotOverlay_(ax, tags, names, tRange)
          ...
          for k = 1:numel(tags)
              if nargin >= 4 && ~isempty(tRange)
                  [tv, y] = tags{k}.getXYRange(tRange(1), tRange(2));
              else
                  [tv, y] = tags{k}.getXY();
              end
              ...
          end
      ```
    - Change the Overlay widget registration (:95-99) so the PlotFcn has arity that lets RawAxesWidget pass its TimeRange. Register with a two-arg PlotFcn:
      ```matlab
      engine.addWidget('rawaxes', ...
          'PlotFcn', @(ax, tRange) plotOverlay_(ax, validTags, validNames, tRange), ...
          ...);
      ```
      RawAxesWidget.callPlotFcn dispatches `@(ax, tRange)` -> `PlotFcn(hAxes, obj.TimeRange)` (RawAxesWidget.m:162). On first render TimeRange is [] so plotOverlay_ falls back to getXY() (full); when the engine later calls setTimeWindow -> rerenderWidgets, the companion's RangeChanged loop (Plan 04) will also drive the RawAxesWidget's TimeRange via the engine. For THIS plan, the only requirement is that the Overlay PlotFcn correctly windows when given a non-empty tRange (assert in test by directly calling plotOverlay_ with a tRange, or by setting the RawAxesWidget TimeRange and re-rendering). Keep `findEventStoreFor_` and the `isa(tt,'MonitorTag')` check at :165 untouched (Approach-3 pitfall).

    After all three, run `test_dashboard_time_window()` in MATLAB — all sub-tests green. Run `test_sensor_tag_range()` to confirm no data-layer regression.
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/PARA/10_Projects/FastPlot/.claude/worktrees/sleepy-grothendieck-1b8d2f && grep -qF "function setTimeWindow(obj, t0, t1)" libs/Dashboard/DashboardEngine.m && grep -qF "ismethod(w, 'setTimeWindow')" libs/Dashboard/DashboardEngine.m && grep -qF "TimeWindow_ = []" libs/Dashboard/DashboardEngine.m && grep -qF "'TimeWindow'" libs/FastSense/SensorDetailPlot.m && grep -q "getXYRange" libs/FastSense/SensorDetailPlot.m && grep -qF "getXYRange(tRange(1), tRange(2))" libs/FastSenseCompanion/private/openAdHocPlot.m && grep -qF "engine.addWidget('rawaxes'" libs/FastSenseCompanion/private/openAdHocPlot.m && echo VIEW_OK</automated>
  </verify>
  <acceptance_criteria>
    - `libs/Dashboard/DashboardEngine.m` adds `TimeWindow_ = []` (private) and `function setTimeWindow(obj, t0, t1)`.
    - setTimeWindow iterates `obj.allPageWidgets()`, guards `ismethod(w, 'setTimeWindow')`, and calls `obj.rerenderWidgets()` once: `grep -qF "obj.rerenderWidgets()" libs/Dashboard/DashboardEngine.m` (fixed-string — parens) and confirm it is within the setTimeWindow body region.
    - setTimeWindow does NOT call updateGlobalTimeRange: confirm no `updateGlobalTimeRange` call inside the setTimeWindow body (read the method).
    - `libs/FastSense/SensorDetailPlot.m` contains `'TimeWindow'` in conDefaults and a `getXYRange` call in render (`grep -q "getXYRange" libs/FastSense/SensorDetailPlot.m`); the ctor-validation `getXY()` at ~:60 is unchanged.
    - `libs/FastSenseCompanion/private/openAdHocPlot.m` plotOverlay_ accepts a tRange and calls `getXYRange(tRange(1), tRange(2))` — verify with FIXED-STRING grep (parens are regex metacharacters): `grep -qF "getXYRange(tRange(1), tRange(2))" libs/FastSenseCompanion/private/openAdHocPlot.m`.
    - The Overlay registration uses a two-arg PlotFcn: `grep -qF "engine.addWidget('rawaxes'" libs/FastSenseCompanion/private/openAdHocPlot.m` (and the PlotFcn is `@(ax, tRange) plotOverlay_(...)`).
    - The `isa(tt,'MonitorTag')` check is unchanged: `grep -qF "isa(tt, 'MonitorTag')" libs/FastSenseCompanion/private/openAdHocPlot.m` (or the exact existing form still present — read to confirm).
    - Static checks clean: `mcp__matlab__check_matlab_code` on DashboardEngine.m, SensorDetailPlot.m, openAdHocPlot.m report no errors.
    - In MATLAB via MCP: `test_dashboard_time_window()` prints "All N tests passed"; `test_sensor_tag_range()` still green.
  </acceptance_criteria>
  <done>DashboardEngine fans a load window out to its widgets and rerenders once (never per tick, never via updateGlobalTimeRange); SensorDetailPlot honors a 'TimeWindow' option; the ad-hoc Overlay windows its lines when given a range — all backward-compatible when the window is empty.</done>
</task>

</tasks>

<verification>
- `test_dashboard_time_window()` prints "All N tests passed" in MATLAB; the pure fan-out sub-tests also run in Octave.
- `test_fastsense_widget_tag()` and `test_sensor_tag_range()` still green (no regression).
- The disk 'All data' (empty-window) case binds the FULL extent via getXYRange(getTimeRange()) -> fp.addLine, NOT a blank addTag line (testWidgetAllDataDiskNonEmpty).
- FastSenseWidget preview path (getPreviewSeries / getTimeRange / CachedXMin/CachedXMax) still uses getXY() — confirmed by reading the bodies.
- `DashboardEngine.setTimeWindow` calls rerenderWidgets exactly once and never updateGlobalTimeRange.
- Static analysis clean on all four modified source files.
</verification>

<success_criteria>
- A windowed FastSenseWidget loads only the window via getXYRange at render/refresh/update; an empty window over an out-of-extent / empty result yields the 'No data in selected range' empty-state.
- The 'All data' preset (empty window) on a disk-backed sensor renders its FULL extent (not a blank plot); in-RAM tags keep the byte-identical fp.addTag path on an empty window.
- The slider/navigator preview keeps showing the full historical envelope.
- DashboardEngine.setTimeWindow fans out + rerenders once; clearing restores full-range.
- SensorDetailPlot 'TimeWindow' loads bounded main-axes data; the Overlay path honors a range.
- Default-empty behavior is byte-identical to pre-phase for non-companion engine use with in-RAM tags.
</success_criteria>

<output>
After completion, create `.planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-03-SUMMARY.md`.
</output>

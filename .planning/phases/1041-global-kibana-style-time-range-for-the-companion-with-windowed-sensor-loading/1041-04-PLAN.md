---
phase: 1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading
plan: 04
type: execute
wave: 3
depends_on: ["1041-02", "1041-03"]
files_modified:
  - libs/FastSenseCompanion/CompanionTimeBar.m
  - libs/FastSenseCompanion/FastSenseCompanion.m
  - libs/FastSenseCompanion/private/openAdHocPlot.m
  - libs/FastSenseCompanion/companionPrefs.m
autonomous: true
requirements: []

must_haves:
  truths:
    - "The companion toolbar shows a range button (Tag 'CompanionTimeRangeBtn') in the col-9 spacer whose text is the current CompanionTimeRange label"
    - "Clicking the range button opens a single Time Range picker popup (400x280 uifigure); opening again focuses the existing popup"
    - "The picker has Quick / Relative / Absolute modes; quick presets apply on one click and close the popup; Relative/Absolute apply on the Apply button"
    - "Committing a range fires RangeChanged exactly once, the button label updates, and the button background switches to Accent when the spec is non-default"
    - "On RangeChanged the companion resolves [t0,t1] and calls setTimeWindow on every managed Engines_ entry AND every OpenedFigures_ engine recovered via getappdata(hFig,'DashboardEngine')"
    - "openAdHocPlot stashes its engine on the figure via setappdata(hFig,'DashboardEngine',engine) so the companion can re-query it"
    - "Newly opened views (ad-hoc, dashboards, sensor detail) receive the current resolved window at open time"
    - "FastSenseCompanion exposes a public currentTimeWindow() helper returning the resolved [t0,t1]; InspectorPane uses it to window the sensor-detail open site"
    - "The default range is Last 7 days, loaded from companionPrefs; changing it persists via companionPrefs save"
  artifacts:
    - path: "libs/FastSenseCompanion/CompanionTimeBar.m"
      provides: "uifigure-native toolbar picker: range button + popup (3 modes) + Apply/Cancel, themed"
      contains: "classdef CompanionTimeBar < handle"
    - path: "libs/FastSenseCompanion/FastSenseCompanion.m"
      provides: "Instantiate CompanionTimeRange + CompanionTimeBar in toolbar; onRangeChanged_ re-query; pass window at open sites; prefs default"
      contains: "onRangeChanged_"
    - path: "libs/FastSenseCompanion/private/openAdHocPlot.m"
      provides: "Engine stash on figure appdata for companion re-query"
      contains: "setappdata(hFig, 'DashboardEngine', engine)"
    - path: "libs/FastSenseCompanion/companionPrefs.m"
      provides: "Default time-range spec persistence (timeRange field)"
      contains: "timeRange"
  key_links:
    - from: "libs/FastSenseCompanion/FastSenseCompanion.m onRangeChanged_"
      to: "DashboardEngine.setTimeWindow"
      via: "iterate Engines_ + OpenedFigures_ (getappdata DashboardEngine), call setTimeWindow(t0,t1)"
      pattern: "setTimeWindow\\(t0, t1\\)"
    - from: "libs/FastSenseCompanion/private/openAdHocPlot.m"
      to: "figure appdata"
      via: "setappdata(hFig, 'DashboardEngine', engine) after engine.render()"
      pattern: "setappdata\\(hFig, 'DashboardEngine', engine\\)"
    - from: "libs/FastSenseCompanion/CompanionTimeBar.m"
      to: "CompanionTimeRange RangeChanged"
      via: "preset/Apply calls CompanionTimeRange.setRelative/setAbsolute/setAll"
      pattern: "set(Relative|Absolute|All)\\("
    - from: "libs/FastSenseCompanion/FastSenseCompanion.m"
      to: "companionPrefs"
      via: "load default timeRange spec at ctor; save on change"
      pattern: "companionPrefs\\("
---

<objective>
Build the `CompanionTimeBar` uifigure picker and wire it into `FastSenseCompanion`: a toolbar range button + a 3-mode popup that edits the shared `CompanionTimeRange`; a `RangeChanged` listener that pushes the resolved window to every managed and ad-hoc engine; an engine stash so ad-hoc figures are re-queryable; the window passed at every open site; and a Last-7-days default persisted via `companionPrefs`.

Purpose: This is the user-facing surface. It turns the data + view plumbing from Plans 01-03 into "pick a window in the toolbar; every companion-opened view loads only that window and re-queries when you change it."
Output: `CompanionTimeBar.m` + companion integration + `openAdHocPlot` stash + prefs default.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-CONTEXT.md
@.planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-RESEARCH.md
@.planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-UI-SPEC.md

<interfaces>
<!-- Verified against the codebase + Plan 02/03 contracts. Use directly. -->

CompanionTimeRange (Plan 02 — on disk):
  events RangeChanged
  [t0,t1]=resolve()  setRelative(N,unit)  setAbsolute(t0,t1)  setAll()
  lbl=label()  tf=isDefault()  s=toStruct()  CompanionTimeRange.fromStruct(s)

DashboardEngine (Plan 03 — on disk):
  setTimeWindow(t0, t1)   % [] [] resets to full; fans out to widgets + rerenders once

SensorDetailPlot (Plan 03 — on disk):
  SensorDetailPlot(tag, 'TimeWindow', [t0 t1], ...)   % windowed main axes

FastSenseCompanion.m (libs/FastSenseCompanion/FastSenseCompanion.m) — VERIFIED:
  * Toolbar: hToolbarGrid = uigridlayout(hToolbarPanel_, [1 10]) at :326; ColumnWidth = {110,110,110,130,70,90,70,70,'1x',36} at :327 — COL 9 is the '1x' spacer (currently empty), COL 10 (36px) is the gear. Phase 1041 puts the range button in col 9.
  * Theme_ resolved CompanionTheme struct (:83). Listeners_ cell (:84) — append every addlistener; deleted on close (:688-698).
  * Engines_ cell of DashboardEngine handles (:88). OpenedFigures_ column vector of figure handles (:116).
  * syncOpenedFigures_(obj) (:2120) pulls every alive Engines_{k}.hFigure into OpenedFigures_ + prunes dead; pruneOpenedFigures_ exists.
  * onOpenAdHocPlotRequested_ (:~2150) calls openAdHocPlot(tags, mode, Theme) — does NOT track the spawned figure (lifecycle-independent by design).
  * InspectorPane.onOpenDetail_ (InspectorPane.m:843) opens detail inline and calls obj.Orchestrator_.trackOpenedFigure(hFig) (:861) — so detail figures DO land in OpenedFigures_ via the public trackOpenedFigure hook (:1194).
  * Singleton dialog precedent: SettingsDlg_ (:65) + openSettings (:1049) — `if isvalid(SettingsDlg_.hFig_) figure(hFig_); return; end; obj.SettingsDlg_ = CompanionSettingsDialog(obj);`. Mirror this for the picker.
  * gear ButtonPushedFcn wired at :449 (`obj.hSettingsBtn_.ButtonPushedFcn = @(~,~) obj.openSettings();`).
  * applyThemeToChildren_(hFig, theme) is the theme walker used by CompanionSettingsDialog (:99) — call it on the picker popup too.

openAdHocPlot.m (libs/FastSenseCompanion/private/openAdHocPlot.m) — VERIFIED:
  * engine = DashboardEngine(...) at :88; engine.render() :131; engine.startLive() :132; hFig = engine.hFigure :134; CloseRequestFcn set :135-136. Returns [hFig, skippedNames].
  * The engine is captured in the CloseRequestFcn closure but NOT stashed on hFig appdata — Plan 04 adds the stash.

CompanionSettingsDialog.m (reference popup):
  * uifigure('Name',...,'Position',[200 200 360 200],'Resize','off','AutoResizeChildren','off','Color',t.DashboardBackground) :50-55
  * uigridlayout(hFig_,[3 2]); Padding=[16 16 16 16]; RowSpacing=12 :58-64; button row gBtns ColumnSpacing=8 :80-87
  * applyThemeToChildren_(hFig_, t) :99 ; CloseRequestFcn = @(~,~) obj.close() :101 ; close() deletes hFig_, sets [] ; delete() cleans up.

companionPrefs.m (libs/FastSenseCompanion/companionPrefs.m) — VERIFIED:
  * out = companionPrefs('load')  -> persisted struct (theme, livePeriod, ...); missing file -> struct(); corrupt -> struct() + one warning.
  * companionPrefs('save', prefs)  -> validates prefs is struct, atomic write.
  * Forward-compatible: just add a `timeRange` field (a CompanionTimeRange.toStruct() struct) to the saved struct; absent field -> default.

UI-SPEC LOCKED VALUES (authority over RESEARCH for any conflict):
  * Range button: Tag='CompanionTimeRangeBtn', Layout.Column=9, FontSize=11, FontWeight='bold', FontColor=theme.ForegroundColor (=ToolbarFontColor), BackgroundColor=theme.WidgetBorderColor when default / theme.Accent when non-default, Tooltip='Set the global time range for all companion-opened views', Text=CompanionTimeRange.label().
  * Popup: uifigure Name='Time Range', Position [x y 400 280], Resize='off', AutoResizeChildren='off', Color=theme.DashboardBackground, CloseRequestFcn=@(~,~)obj.close(). Root uigridlayout([5 1])? -> use [4 1]: Row1 32 = mode tab strip [1x3]; Row2 '1x' = active mode panel; Row3 1 = separator uipanel (BackgroundColor=WidgetBorderColor); Row4 40 = Apply+Cancel row. Padding=[16 16 16 16], RowSpacing=12, ColumnSpacing=0.
  * Mode tabs (Quick/Relative/Absolute) uibuttons [1x3], ColumnSpacing=4; active tab BackgroundColor=Accent, inactive=WidgetBorderColor; FontSize=11 bold. Default active = Quick.
  * Quick panel: [6x1] uibuttons RowSpacing=4: 'Last 24 hours'(24,'hours'), 'Last 7 days'(7,'days') default, 'Last 30 days'(30,'days'), 'Last 90 days'(90,'days'), 'Last 1 year'(1,'years'), 'All data'(setAll). One-click apply -> commit + close. Active preset highlighted Accent.
  * Relative panel: uispinner N (Limits [1 9999], Step 1, Value 7, RoundFractionalValues 'on') + uidropdown unit (Items {'hours','days','weeks','months','years'}, Value 'days') + a read-only preview uilabel (resolved 'YYYY-MM-DD to YYYY-MM-DD', PlaceholderTextColor). Apply commits setRelative.
  * Absolute panel: uidatepicker Start + End (default start=datetime('today')-caldays(7), end=datetime('today')) + preview uilabel ('N days'). Apply: convert via datenum(picker.Value); if start>=end disable Apply + show 'Invalid: start must be before end' in StatusAlarmColor.
  * Action row: Apply (BackgroundColor=Accent) + Cancel (BackgroundColor=WidgetBorderColor), FontSize 11 bold. In Quick mode Apply.Visible='off' (presets are one-click); Cancel stays visible.
  * Labels: 'All data' (NOT 'All'); absolute uses the WORD 'to'. Empty-state copy (Plan 03) 'No data in selected range'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: CompanionTimeBar — range button + skeleton singleton popup + Quick-presets mode</name>
  <read_first>
    - libs/FastSenseCompanion/CompanionSettingsDialog.m (FULL — the exact popup pattern: owned uifigure, uigridlayout, applyThemeToChildren_, close/delete lifecycle, singleton focus)
    - libs/FastSenseCompanion/CompanionTheme.m (theme struct fields: DashboardBackground, WidgetBackground, WidgetBorderColor, Accent, ForegroundColor/ToolbarFontColor, PlaceholderTextColor, StatusAlarmColor)
    - libs/FastSenseCompanion/CompanionTimeRange.m (Plan 02 — the spec object this bar edits: setRelative/setAbsolute/setAll/label/isDefault)
    - .planning/phases/1041-.../1041-UI-SPEC.md "Surface 1" + "Surface 2" root grid + "Mode: Quick Presets" + "Copywriting Contract" (button spec, popup 400x280, the [4 1] root grid + tab strip, the 6 Quick presets, colors, fonts, spacing)
    - libs/FastSenseCompanion/FastSenseCompanion.m lines 326-345, 441-460, 1049-1060 (toolbar grid, a button wiring example, openSettings singleton)
    - CLAUDE.md (PascalCase class; camelCase public methods; trailing-underscore private state; namespaced errors CompanionTimeBar:...; Listeners_ cell + delete in CloseRequestFcn; every callback try/catch + uialert; 4-space/160-col)
  </read_first>
  <action>
    Create `libs/FastSenseCompanion/CompanionTimeBar.m` as a `classdef CompanionTimeBar < handle` that owns the toolbar range button AND the picker popup shell, and edits a `CompanionTimeRange` handle passed in at construction. MATLAB-only (uifigure). Build to the UI-SPEC LOCKED VALUES verbatim. This task delivers the button + the popup skeleton (root grid, tab strip, action row) + the FULLY WORKING Quick-presets mode. Task 1b adds the Relative + Absolute panels.

    Constructor: `CompanionTimeBar(parentGrid, col, timeRange, theme, app)` — `parentGrid` = companion `hToolbarGrid`, `col` = 9, `timeRange` = shared `CompanionTimeRange`, `theme` = resolved CompanionTheme struct, `app` = FastSenseCompanion (for `uialert` parent + `applyThemeToChildren_` access; pass the app handle as CompanionSettingsDialog does).

    Public API (full surface — Relative/Absolute commit logic is stubbed here and completed in 1b):
      - `obj = CompanionTimeBar(parentGrid, col, timeRange, theme, app)` — builds the range button into parentGrid col, Tag='CompanionTimeRangeBtn', FontSize 11, FontWeight 'bold', Tooltip 'Set the global time range for all companion-opened views', wires ButtonPushedFcn -> obj.openPicker().
      - `openPicker(obj)` — singleton: if the popup is already open (`~isempty(obj.hPopup_) && isvalid(obj.hPopup_)`), `figure(obj.hPopup_)` and return; else build the 400x280 popup shell via `buildPopup_` and show the Quick panel by default.
      - `refreshButton(obj)` — set button Text = obj.TimeRange_.label() and BackgroundColor = (obj.TimeRange_.isDefault() -> theme.WidgetBorderColor; else theme.Accent). Call on construction and after every commit.
      - `setTheme(obj, theme)` — restyle the button (and popup if open); mirror CompanionSettingsDialog.onThemeChanged_.
      - `close(obj)` — delete the popup if open, clear handle (does NOT delete the button).
      - `delete(obj)` — delete Listeners_, close popup, delete button if valid.

    Private state (Access=private, trailing underscore): hBtn_, hPopup_ (=[]), TimeRange_, Theme_, App_, Listeners_={}, ActiveMode_ ('Quick'|'Relative'|'Absolute', default 'Quick'), hTabQuick_/hTabRelative_/hTabAbsolute_, hPanelQuick_/hPanelRelative_/hPanelAbsolute_ (panel handles; Relative/Absolute panels created EMPTY here, populated in 1b), hApplyBtn_, hCancelBtn_.

    Private builders for THIS task:
      - `buildPopup_(obj)` — `uifigure('Name','Time Range','Position',[px py 400 280],'Resize','off','AutoResizeChildren','off','Color',theme.DashboardBackground)` positioned near the companion via `getpixelposition(obj.App_.hFig_,'recursive')` (offset + clamp to screen). Root `uigridlayout([4 1])` Padding [16 16 16 16], RowSpacing 12, ColumnSpacing 0; Row1(32)=tab strip, Row2('1x')=mode-panel host, Row3(1)=separator uipanel (WidgetBorderColor), Row4(40)=action row. Build the tab strip [1 3] (Quick/Relative/Absolute uibuttons, FontSize 11 bold, active=Accent / inactive=WidgetBorderColor). Create the three mode panels as children of the Row2 host with `Visible='off'` (Quick visible). Build the action row [1 2] Apply(Accent)+Cancel(WidgetBorderColor); Apply.Visible='off' in Quick mode. After building, `applyThemeToChildren_(obj.hPopup_, theme)` then re-assert Accent on the active tab; set `obj.hPopup_.CloseRequestFcn = @(~,~) obj.close();`.
      - `buildQuickPanel_(obj, parent)` — the [6 1] preset grid (RowHeight 32 each, RowSpacing 4): 'Last 24 hours'(24,'hours'), 'Last 7 days'(7,'days'), 'Last 30 days'(30,'days'), 'Last 90 days'(90,'days'), 'Last 1 year'(1,'years'), 'All data'(setAll). FontSize 11 bold, HorizontalAlignment 'left'. Active preset (matching current spec) highlighted Accent. Each preset ButtonPushedFcn (one-click): for the 5 relative -> `obj.TimeRange_.setRelative(N,unit)`; for All -> `obj.TimeRange_.setAll()`; then `obj.refreshButton(); obj.close();`.
      - `switchMode_(obj, mode)` — show/hide the three panels via Visible, repaint tab backgrounds (Accent active / WidgetBorderColor inactive), toggle Apply.Visible ('off' in Quick, 'on' otherwise). NEVER commits.
      - `buildRelativePanel_(obj, parent)` and `buildAbsolutePanel_(obj, parent)` — STUB in 1a: create the empty panel container only (Task 1b fills them). Leave a `% TODO(1b)` marker.

    Wrap every callback in try/catch; on error `uialert(obj.App_.hFig_, ME.message, 'Time Range Error')` (guarded). Do NOT fire RangeChanged directly — only the CompanionTimeRange setters fire it (Plan 02); the companion listens (Task 3).
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/PARA/10_Projects/FastPlot/.claude/worktrees/sleepy-grothendieck-1b8d2f && grep -qF "classdef CompanionTimeBar < handle" libs/FastSenseCompanion/CompanionTimeBar.m && grep -qF "'CompanionTimeRangeBtn'" libs/FastSenseCompanion/CompanionTimeBar.m && grep -qF "function openPicker(obj)" libs/FastSenseCompanion/CompanionTimeBar.m && grep -qF "400 280" libs/FastSenseCompanion/CompanionTimeBar.m && grep -qF "function buildQuickPanel_(obj" libs/FastSenseCompanion/CompanionTimeBar.m && grep -qE "setRelative\(|setAll\(" libs/FastSenseCompanion/CompanionTimeBar.m && echo BAR1A_OK</automated>
  </verify>
  <acceptance_criteria>
    - `libs/FastSenseCompanion/CompanionTimeBar.m` contains `classdef CompanionTimeBar < handle`.
    - Range button: Tag `'CompanionTimeRangeBtn'`, FontSize 11, FontWeight 'bold' (grep each), Tooltip 'Set the global time range for all companion-opened views'.
    - Defines public methods openPicker, refreshButton, setTheme, close, delete (grep each).
    - Defines private builders `buildPopup_`, `buildQuickPanel_`, `switchMode_` (grep each); Relative/Absolute panel builders exist as stubs (`buildRelativePanel_`, `buildAbsolutePanel_`).
    - Popup uses `uifigure` with `400 280` in Position, `Resize` 'off', `Name` 'Time Range'; root grid is `uigridlayout` with `[4 1]`; the three Quick presets+All are present (grep 'Last 7 days', 'All data').
    - Quick presets commit one-click via `setRelative`/`setAll` then `obj.close()` (a preset ButtonPushedFcn calls both a setter and `obj.close()`).
    - `applyThemeToChildren_` invoked on the popup; CloseRequestFcn wired to `obj.close()`; singleton openPicker brings an existing popup to front (`figure(obj.hPopup_)`).
    - Every callback try/catch with a guarded `uialert`.
    - Static check clean: `mcp__matlab__check_matlab_code` on CompanionTimeBar.m reports no errors.
  </acceptance_criteria>
  <done>CompanionTimeBar exists with the themed col-9 range button, a 400x280 singleton popup shell ([4 1] root grid + tab strip + action row), and a fully working Quick-presets mode (one-click commit+close); Relative/Absolute panels are stubbed for Task 1b.</done>
</task>

<task type="auto">
  <name>Task 1b: CompanionTimeBar — Relative panel + Absolute panel + Apply/Cancel + validation + live preview</name>
  <read_first>
    - libs/FastSenseCompanion/CompanionTimeBar.m (Task 1a — the class to extend: buildRelativePanel_/buildAbsolutePanel_ stubs, switchMode_, ActiveMode_, hApplyBtn_/hCancelBtn_, refreshButton/close)
    - libs/FastSenseCompanion/CompanionTimeRange.m (Plan 02 — setRelative/setAbsolute; the relative->days map to mirror for the live preview)
    - .planning/phases/1041-.../1041-UI-SPEC.md "Mode: Relative Builder" + "Mode: Absolute" + "Action Row" + "Copywriting Contract" (uispinner/uidropdown/uidatepicker specs, preview labels, validation string, Apply/Cancel behavior, 'until now', 'N days')
    - CLAUDE.md (Pitfall 6: uidatepicker.Value is datetime -> datenum(); namespaced errors; 4-space/160-col; callback try/catch + uialert)
  </read_first>
  <action>
    Extend `libs/FastSenseCompanion/CompanionTimeBar.m` — fill the two stubbed panel builders and wire the Apply/Cancel commit paths for Relative + Absolute. Each panel is built in its own named private method (NOT inline in buildPopup_).

      - `buildRelativePanel_(obj, parent)` — control row [1 3] (ColumnWidth {60,'1x',120}, ColumnSpacing 8): a `uispinner` N (`Limits [1 9999]`, `Step 1`, `Value 7`, `RoundFractionalValues 'on'`) + a `uidropdown` unit (`Items {'hours','days','weeks','months','years'}`, `Value 'days'`) + the 'until now' label; below it a read-only preview `uilabel` (FontColor PlaceholderTextColor) showing `datestr(now()-asDays,'yyyy-mm-dd')` to `datestr(now(),'yyyy-mm-dd')`. Recompute `asDays` from spinner+unit using the SAME map as CompanionTimeRange (hours=N/24, days=N, weeks=7N, months=30N, years=365N). Spinner/dropdown ValueChangedFcn -> update the preview live (no commit).
      - `buildAbsolutePanel_(obj, parent)` — [3 2] grid (ColumnWidth {80,'1x'}): Start `uilabel`+`uidatepicker` (default `datetime('today')-caldays(7)`), End `uilabel`+`uidatepicker` (default `datetime('today')`), and a preview `uilabel` showing 'N days' (PlaceholderTextColor). Picker ValueChangedFcn -> recompute the day count + run validation live.
      - Apply commit paths (the shared `hApplyBtn_` ButtonPushedFcn dispatches on `obj.ActiveMode_`): Relative -> read spinner+dropdown, `obj.TimeRange_.setRelative(N,unit)`, `obj.refreshButton(); obj.close();`. Absolute -> `t0=datenum(startPicker.Value); t1=datenum(endPicker.Value)`; if `t0>=t1` keep Apply disabled + show 'Invalid: start must be before end' in StatusAlarmColor (do NOT commit); else `obj.TimeRange_.setAbsolute(t0,t1)`, `obj.refreshButton(); obj.close();`.
      - Cancel (`hCancelBtn_`) -> `obj.close()` (no commit). Validation: in Absolute mode, set `hApplyBtn_.Enable` off whenever `t0>=t1` and replace the preview text with the validation string in StatusAlarmColor; restore Enable on + the 'N days' preview when valid.
      - After populating panels, re-run `applyThemeToChildren_(obj.hPopup_, obj.Theme_)` if needed and re-assert Accent on the Apply button (the walker may normalize it).

    Keep using `datenum(picker.Value)` for the absolute conversion (Pitfall 6). Do NOT fire RangeChanged directly. Every callback try/catch + guarded uialert.
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/PARA/10_Projects/FastPlot/.claude/worktrees/sleepy-grothendieck-1b8d2f && grep -qF "function buildRelativePanel_(obj" libs/FastSenseCompanion/CompanionTimeBar.m && grep -qF "function buildAbsolutePanel_(obj" libs/FastSenseCompanion/CompanionTimeBar.m && grep -qF "setAbsolute(" libs/FastSenseCompanion/CompanionTimeBar.m && grep -qF "datenum(" libs/FastSenseCompanion/CompanionTimeBar.m && grep -q "Invalid: start must be before end" libs/FastSenseCompanion/CompanionTimeBar.m && grep -qF "uidatepicker" libs/FastSenseCompanion/CompanionTimeBar.m && echo BAR1B_OK</automated>
  </verify>
  <acceptance_criteria>
    - `buildRelativePanel_` builds a `uispinner` (Limits [1 9999], Value 7) + a `uidropdown` (Items hours/days/weeks/months/years) + a live preview label; the relative->days map matches CompanionTimeRange.
    - `buildAbsolutePanel_` builds two `uidatepicker` + a 'N days' preview; Absolute Apply uses `datenum(` on the picker Value (`grep -qF "datenum(" libs/FastSenseCompanion/CompanionTimeBar.m`).
    - Relative/Absolute commit via the shared Apply button: `grep -qF "setRelative(" libs/FastSenseCompanion/CompanionTimeBar.m` AND `grep -qF "setAbsolute(" libs/FastSenseCompanion/CompanionTimeBar.m`.
    - Validation string present: `grep -q "Invalid: start must be before end" libs/FastSenseCompanion/CompanionTimeBar.m`; Apply is disabled when start>=end.
    - Cancel closes without committing; preview labels update live on control change.
    - Static check clean: `mcp__matlab__check_matlab_code` on CompanionTimeBar.m reports no errors.
    - In MATLAB via MCP: the bar still constructs and openPicker shows all three modes without error (spot-check; full UI assertions live in Plan 05 TestCompanionTimeBar).
  </acceptance_criteria>
  <done>CompanionTimeBar now has working Relative (spinner+dropdown+live preview) and Absolute (two uidatepicker+'N days' preview+validation) modes, each built in a named private method, committing via the shared Apply button (datenum conversion, start<end validation), Cancel dismissing without commit.</done>
</task>

<task type="auto">
  <name>Task 2: openAdHocPlot engine stash + companionPrefs timeRange default</name>
  <read_first>
    - libs/FastSenseCompanion/private/openAdHocPlot.m lines 85-140 (engine construction, render, startLive, hFig, CloseRequestFcn, return)
    - libs/FastSenseCompanion/companionPrefs.m (FULL — load/save shape, forward-compatible struct, atomic write)
    - tests/test_companion_open_ad_hoc_plot.m (existing openAdHocPlot test — the Octave-skip guard + how the helper is invoked; this is where the stash can be regression-checked)
    - CLAUDE.md (namespaced errors; 4-space/160-col; private helper header comments)
  </read_first>
  <action>
    A) `libs/FastSenseCompanion/private/openAdHocPlot.m`: immediately AFTER `hFig = engine.hFigure;` and the `ishandle(hFig)` guard (~:134-136), stash the engine on the figure so the companion can recover it for re-query:
       ```matlab
       if ~isempty(hFig) && ishandle(hFig)
           setappdata(hFig, 'DashboardEngine', engine);
       end
       ```
       Place it alongside the existing CloseRequestFcn assignment (the same `ishandle(hFig)` block is fine). This is purely additive — no behavior change to the figure. Do not alter plotOverlay_ further (Plan 03 already made it window-capable) or the MonitorTag check.

    B) `libs/FastSenseCompanion/companionPrefs.m`: no signature change. The struct is already forward-compatible (arbitrary fields persist). Confirm a `timeRange` field round-trips: the companion (Task 3) will write `prefs.timeRange = obj.TimeRange_.toStruct();` on change and read it at ctor. If companionPrefs has an explicit allow-list of fields on load/save (read the code), ADD `timeRange` to it so the field is not dropped; if it persists arbitrary fields, no change is needed — just confirm by reading. Document the `timeRange` field in the function header comment's field list.
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/PARA/10_Projects/FastPlot/.claude/worktrees/sleepy-grothendieck-1b8d2f && grep -qF "setappdata(hFig, 'DashboardEngine', engine)" libs/FastSenseCompanion/private/openAdHocPlot.m && grep -q "timeRange" libs/FastSenseCompanion/companionPrefs.m && echo STASH_OK</automated>
  </verify>
  <acceptance_criteria>
    - `libs/FastSenseCompanion/private/openAdHocPlot.m` contains `setappdata(hFig, 'DashboardEngine', engine)` inside an `ishandle(hFig)` guard, after `engine.render()`.
    - The existing CloseRequestFcn line and the `isa(...'MonitorTag')` check are unchanged.
    - `libs/FastSenseCompanion/companionPrefs.m` references `timeRange` (either in an allow-list or in the documented field list) such that a `timeRange` field survives a save/load round-trip.
    - Static checks clean: `mcp__matlab__check_matlab_code` on both files report no errors.
    - In MATLAB via MCP: `test_companion_open_ad_hoc_plot()` still passes (no regression).
  </acceptance_criteria>
  <done>Ad-hoc figures carry their DashboardEngine on appdata for re-query; companionPrefs persists a timeRange spec field.</done>
</task>

<task type="auto">
  <name>Task 3: FastSenseCompanion integration — instantiate range + bar, RangeChanged re-query, pass window at open sites, prefs default</name>
  <read_first>
    - libs/FastSenseCompanion/FastSenseCompanion.m lines 80-120 (private props: Theme_, Listeners_, Engines_, OpenedFigures_), 240-260 (ctor option resolution), 300-345 (root layout + toolbar grid), 530-548 (pane wiring + listener appends), 1049-1060 (openSettings singleton), 1194-1210 (trackOpenedFigure), 2120-2160 (syncOpenedFigures_, onOpenAdHocPlotRequested_)
    - libs/FastSenseCompanion/CompanionTimeRange.m + CompanionTimeBar.m (Plan 02 / this plan's Tasks 1a+1b — the objects to instantiate)
    - libs/FastSenseCompanion/InspectorPane.m lines 843-865 (onOpenDetail_ — where SensorDetailPlot is opened inline; this is an open site that should receive the window)
    - libs/FastSenseCompanion/CompanionSettingsDialog.m lines 134-150 (onThemeChanged_ — to extend the theme switch to also restyle the time bar)
    - .planning/phases/1041-.../1041-RESEARCH.md "Companion RangeChanged listener in FastSenseCompanion" code example + Pitfall 5 (prune OpenedFigures_ before iterating)
    - CLAUDE.md (Listeners_ cell + delete on close; every callback try/catch + uialert; namespaced errors; 4-space/160-col)
  </read_first>
  <action>
    In `libs/FastSenseCompanion/FastSenseCompanion.m`:

    1) Private properties: add
       ```matlab
       TimeRange_ = []     % CompanionTimeRange — global window source of truth
       TimeBar_   = []     % CompanionTimeBar — toolbar picker control
       ```

    2) Construct the range from prefs (in the ctor, after Theme_ is resolved and prefs are loaded — co-locate with the existing prefs/theme resolution near :240-255):
       ```matlab
       p = companionPrefs('load');
       if isfield(p, 'timeRange') && isstruct(p.timeRange)
           obj.TimeRange_ = CompanionTimeRange.fromStruct(p.timeRange);
       else
           obj.TimeRange_ = CompanionTimeRange();   % default Last 7 days
       end
       ```
       (CompanionTimeRange defaults to relative N=7 unit='days', i.e. Last 7 days — the locked default.)

    3) Build the toolbar bar AFTER hToolbarGrid exists (right after the gear button is created, ~:441-449):
       ```matlab
       obj.TimeBar_ = CompanionTimeBar(hToolbarGrid, 9, obj.TimeRange_, obj.Theme_, obj);
       ```
       Col 9 was the '1x' spacer (now occupied). Leave ColumnWidth as-is (col 9 stays '1x'; the button fills it). The gear stays col 10.

    4) Wire the RangeChanged listener (append to Listeners_, near the other listener appends ~:538-547):
       ```matlab
       obj.Listeners_{end+1} = addlistener(obj.TimeRange_, 'RangeChanged', @(~,~) obj.onRangeChanged_());
       ```

    5) Add the private re-query method (follow RESEARCH's "Companion RangeChanged listener" example + Pitfall 5):
       ```matlab
       function onRangeChanged_(obj)
       %ONRANGECHANGED_ Push the resolved window to all managed + ad-hoc engines.
           try
               [t0, t1] = obj.TimeRange_.resolve();
               % Persist the new default so the next session restores it.
               try
                   p = companionPrefs('load');
                   p.timeRange = obj.TimeRange_.toStruct();
                   companionPrefs('save', p);
               catch
               end
               % Managed dashboards (direct handles).
               for i = 1:numel(obj.Engines_)
                   e = obj.Engines_{i};
                   if ~isempty(e) && isvalid(e)
                       try
                           e.setTimeWindow(t0, t1);
                       catch
                       end
                   end
               end
               % Ad-hoc + detail figures (recover engine from appdata).
               obj.syncOpenedFigures_();   % prunes dead + pulls Engines_ figs
               for k = 1:numel(obj.OpenedFigures_)
                   hf = obj.OpenedFigures_(k);
                   if ~ishandle(hf); continue; end
                   try
                       eng = getappdata(hf, 'DashboardEngine');
                       if ~isempty(eng) && isvalid(eng)
                           eng.setTimeWindow(t0, t1);
                       end
                   catch
                   end
               end
           catch ME
               try
                   uialert(obj.hFig_, ME.message, 'Time Range Error');
               catch
               end
           end
       end
       ```
       Note: a managed dashboard's figure appears in BOTH Engines_ and (after syncOpenedFigures_) OpenedFigures_, so it may be set twice — that is harmless (setTimeWindow is idempotent: same window -> same rerender). Acceptable for v1.

    6) Add the public helper `currentTimeWindow` (used by InspectorPane to window the sensor-detail open site, and by the open-site wiring below). Place it in the public methods block near `trackOpenedFigure` (~:1194):
       ```matlab
       function [t0, t1] = currentTimeWindow(obj)
       %CURRENTTIMEWINDOW Resolve the global range to a concrete [t0, t1] datenum.
       %   'all' resolves to [] []. Callers (e.g. InspectorPane opening a
       %   SensorDetailPlot) pass this into the view so it starts windowed.
           [t0, t1] = obj.TimeRange_.resolve();
       end
       ```
       This is REQUIRED (not optional): InspectorPane.onOpenDetail_ calls it to window the detail open site (sub-step 7 below).

    7) Pass the window at OPEN sites so newly opened views start bounded:
       - onOpenAdHocPlotRequested_ (~:2150): after `openAdHocPlot(...)` returns the engine's figure, recover the engine and apply the current window. Since openAdHocPlot now stashes the engine (Task 2), do: after the call, `[t0,t1]=obj.TimeRange_.resolve(); eng=getappdata(hFig,'DashboardEngine'); if ~isempty(eng)&&isvalid(eng); try eng.setTimeWindow(t0,t1); catch; end; end`. (openAdHocPlot returns hFig — capture it.)
       - onOpenDashboardRequested_: after the managed engine renders (it is in Engines_), apply the window: resolve + `engine.setTimeWindow(t0,t1)` for the just-opened engine. Read how onOpenDashboardRequested_ obtains/render the engine and apply right after render.
       - InspectorPane SensorDetailPlot open (InspectorPane.m:843 onOpenDetail_): pass the window into the detail plot. The cleanest seam without over-coupling InspectorPane: when InspectorPane opens detail it already calls `obj.Orchestrator_.trackOpenedFigure(hFig)`. Add a companion public helper `[t0,t1] = currentTimeWindow(obj)` returning `obj.TimeRange_.resolve()`, and have InspectorPane pass `'TimeWindow', [t0 t1]` into `SensorDetailPlot(tag, ...)` (or into the openAdHocPlot/detail path it uses). If InspectorPane opens detail via openAdHocPlot LinkedGrid (single tag), the appdata-stash + post-open setTimeWindow already covers it — in that case add the post-open setTimeWindow in onOpenDetail_ using `Orchestrator_.currentTimeWindow()` rather than threading 'TimeWindow' into SensorDetailPlot. Pick whichever matches the actual onOpenDetail_ implementation you read; the REQUIREMENT is that a detail view opened from the companion starts windowed.

    8) Theme switch: in the companion's theme-apply path (where it restyles panes on theme change — near :980-990 where it calls InspectorPane_.setTheme), also call `if ~isempty(obj.TimeBar_) && isvalid(obj.TimeBar_); obj.TimeBar_.setTheme(obj.Theme_); end`.

    9) Cleanup: in the close/delete path (~:688-707 where Listeners_ are deleted and SettingsDlg_ is deleted), also delete the time bar: `if ~isempty(obj.TimeBar_) && isvalid(obj.TimeBar_); delete(obj.TimeBar_); end; obj.TimeBar_ = [];`. The RangeChanged listener is already in Listeners_ so it is torn down with the rest.

    Keep every new callback wrapped in try/catch + guarded uialert. Do not change toolbar ColumnWidth (col 9 stays '1x').
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/PARA/10_Projects/FastPlot/.claude/worktrees/sleepy-grothendieck-1b8d2f && grep -qF "TimeRange_ = []" libs/FastSenseCompanion/FastSenseCompanion.m && grep -qF "CompanionTimeBar(hToolbarGrid, 9" libs/FastSenseCompanion/FastSenseCompanion.m && grep -qF "addlistener(obj.TimeRange_, 'RangeChanged'" libs/FastSenseCompanion/FastSenseCompanion.m && grep -qF "function onRangeChanged_(obj)" libs/FastSenseCompanion/FastSenseCompanion.m && grep -qF "getappdata(hf, 'DashboardEngine')" libs/FastSenseCompanion/FastSenseCompanion.m && grep -qF "setTimeWindow(t0, t1)" libs/FastSenseCompanion/FastSenseCompanion.m && grep -qF "function [t0, t1] = currentTimeWindow" libs/FastSenseCompanion/FastSenseCompanion.m && echo INTEGRATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - `libs/FastSenseCompanion/FastSenseCompanion.m` adds private `TimeRange_` and `TimeBar_`.
    - Constructs `CompanionTimeRange.fromStruct(p.timeRange)` when prefs has it, else `CompanionTimeRange()` (default Last 7 days).
    - Instantiates `CompanionTimeBar(hToolbarGrid, 9, obj.TimeRange_, obj.Theme_, obj)`.
    - Adds the RangeChanged listener to `Listeners_`: `grep -qF "addlistener(obj.TimeRange_, 'RangeChanged'" libs/FastSenseCompanion/FastSenseCompanion.m` (fixed-string — parens).
    - Defines `onRangeChanged_` which resolves, persists prefs, iterates `Engines_` with `setTimeWindow(t0, t1)`, calls `syncOpenedFigures_()`, and iterates `OpenedFigures_` recovering the engine via `getappdata(hf, 'DashboardEngine')`.
    - Defines the public helper `currentTimeWindow`: `grep -q "function \[t0, t1\] = currentTimeWindow" libs/FastSenseCompanion/FastSenseCompanion.m` (the checker's literal grep; or equivalently `grep -qF "function [t0, t1] = currentTimeWindow" libs/FastSenseCompanion/FastSenseCompanion.m`).
    - Open sites pass the window: `onOpenAdHocPlotRequested_` and `onOpenDashboardRequested_` apply the resolved window to the just-opened engine; a companion-opened sensor detail starts windowed (via `currentTimeWindow()` + setTimeWindow OR `'TimeWindow'` into SensorDetailPlot).
    - Theme switch restyles the bar (`obj.TimeBar_.setTheme(`); close path deletes the bar (`delete(obj.TimeBar_)`).
    - Toolbar ColumnWidth unchanged (col 9 still '1x'): the array `{110, 110, 110, 130, 70, 90, 70, 70, '1x', 36}` is still present.
    - Every new callback is try/catch-guarded.
    - Static check clean: `mcp__matlab__check_matlab_code` on FastSenseCompanion.m reports no errors.
  </acceptance_criteria>
  <done>The companion instantiates the global CompanionTimeRange (default Last 7 days from prefs) + the toolbar CompanionTimeBar, re-queries every managed and ad-hoc engine on RangeChanged, passes the current window to every newly opened view, persists the chosen range, and tears the bar + listener down on close.</done>
</task>

</tasks>

<verification>
- Static analysis (`mcp__matlab__check_matlab_code`) clean on CompanionTimeBar.m, FastSenseCompanion.m, openAdHocPlot.m, companionPrefs.m.
- `test_companion_open_ad_hoc_plot()` still green (the appdata stash is additive).
- `test_companion_time_range()` still green (CompanionTimeRange unchanged this plan).
- Toolbar ColumnWidth array unchanged; col 9 hosts the range button.
- Integration smoke (Plan 05 covers the full assertions): the companion constructs without error and the range button label reads "Last 7 days" by default.
</verification>

<success_criteria>
- A themed range button sits in the toolbar showing the current range; clicking it opens the 3-mode picker; committing edits the shared CompanionTimeRange.
- RangeChanged pushes the resolved window to every managed + ad-hoc engine (ad-hoc recovered via appdata).
- Newly opened views start windowed; the default is Last 7 days persisted via companionPrefs.
- No regressions to existing companion / ad-hoc tests; companion teardown is clean (no orphan listener/bar).
</success_criteria>

<output>
After completion, create `.planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-04-SUMMARY.md`.
</output>

---
phase: 1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading
plan: 05
type: execute
wave: 4
depends_on: ["1041-04"]
files_modified:
  - tests/suite/TestCompanionTimeBar.m
  - tests/suite/TestFastSenseCompanion.m
  - tests/SpyTimeWindowEngine.m
  - .planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-MANUAL-VERIFY.md
autonomous: false

requirements: []

must_haves:
  truths:
    - "A MATLAB-only class suite asserts the toolbar range button exists with Tag 'CompanionTimeRangeBtn' and label 'Last 7 days' by default"
    - "The suite asserts opening the picker creates one 400x280 popup (singleton) and a quick-preset click fires RangeChanged, updates the button label, and closes the popup"
    - "A companion integration test asserts RangeChanged re-queries managed Engines_ (setTimeWindow called with the resolved window) and ad-hoc engines recovered via appdata"
    - "A companion integration test asserts a newly opened view starts with the current resolved window applied"
    - "All new UI/integration tests are MATLAB-only (Octave-skipped via skipOnOctave)"
    - "The full MATLAB suite is green (no regressions) and the Octave-safe data-layer subset is green in Octave"
    - "A manual-verify checklist documents the picker visuals + relative-slide-on-live-tick steps a human runs"
  artifacts:
    - path: "tests/suite/TestCompanionTimeBar.m"
      provides: "MATLAB-only UI smoke suite for the range button + picker popup"
      contains: "classdef TestCompanionTimeBar"
    - path: "tests/suite/TestFastSenseCompanion.m"
      provides: "Added integration tests: range button presence, RangeChanged re-query, open-site windowing, teardown"
      contains: "testCompanionTimeRangeButtonDefaultLabel"
    - path: "tests/SpyTimeWindowEngine.m"
      provides: "Lightweight setTimeWindow-recording spy double for re-query assertions"
      contains: "classdef SpyTimeWindowEngine"
    - path: ".planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-MANUAL-VERIFY.md"
      provides: "Human verification checklist for picker visuals + relative slide"
      contains: "Manual Verification"
  key_links:
    - from: "tests/suite/TestCompanionTimeBar.m"
      to: "CompanionTimeBar + CompanionTimeRange"
      via: "construct a hidden uifigure host, build the bar, drive presets, assert label + event"
      pattern: "CompanionTimeBar\\("
    - from: "tests/suite/TestFastSenseCompanion.m"
      to: "DashboardEngine.setTimeWindow"
      via: "SpyTimeWindowEngine recording setTimeWindow calls on RangeChanged"
      pattern: "setTimeWindow"
---

<objective>
Lock the phase behind automated MATLAB integration/UI tests plus a human checklist. The UI smoke suite proves the picker renders and edits the spec; the companion integration tests prove the re-query and open-site windowing wiring; the manual checklist covers the two behaviors that cannot be headlessly asserted (picker visuals + relative slide on live tick).

Purpose: Convert the Plan 01-04 wiring into a green, regression-protected feature with a clear human sign-off path.
Output: `tests/suite/TestCompanionTimeBar.m`, added tests in `tests/suite/TestFastSenseCompanion.m`, `tests/SpyTimeWindowEngine.m`, and `1041-MANUAL-VERIFY.md`. This plan ends with a `checkpoint:human-verify`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-VALIDATION.md
@.planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-UI-SPEC.md
@.planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-RESEARCH.md

<interfaces>
<!-- Verified contracts. -->

CompanionTimeBar (Plan 04 Tasks 1a+1b):
  CompanionTimeBar(parentGrid, col, timeRange, theme, app) ; openPicker ; refreshButton ; setTheme ; close ; delete
  range button Tag = 'CompanionTimeRangeBtn' ; popup uifigure Name 'Time Range' 400x280

CompanionTimeRange (Plan 02):
  events RangeChanged ; setRelative/setAbsolute/setAll ; [t0,t1]=resolve ; lbl=label ; isDefault

FastSenseCompanion (Plan 04 Task 3):
  private TimeRange_, TimeBar_ ; onRangeChanged_ ; currentTimeWindow() (if added) ; toolbar range button col 9
  existing test seam: trackOpenedFigureForTest_(obj, hFig) at FastSenseCompanion.m:1386 (drives private trackOpenedFigure_)

DashboardEngine (Plan 03):
  setTimeWindow(t0, t1)

Test patterns (VERIFIED):
  * tests/suite/TestFastSenseCompanion.m : class suite; TestClassSetup `addPaths` (addpath + install); `skipOnOctave` in TestMethodSetup (skips whole suite on Octave); uifigure('Visible','off') hidden-figure pattern; drives state via public methods; findall/findobj for lookup. Recent toolbar tests assert Layout.Column (Wiki=7, gear=9 pre-1041; with the range button in col 9 the gear is col 10 — CONFIRM the live ColumnWidth array {110,110,110,130,70,90,70,70,'1x',36} and current button columns when writing assertions).
  * tests/CaptureNotificationService.m : an existing standalone test-double class file — the model for SpyTimeWindowEngine.m.
  * tests/test_companion_open_ad_hoc_plot.m : function-test Octave guard `if exist('OCTAVE_VERSION','builtin') ~= 0; ...; return; end`.

NOTE on toolbar column assertions: Plan 04 did NOT change ColumnWidth (col 9 stays '1x', now hosting the range button; gear stays col 10). If existing TestFastSenseCompanion toolbar-column tests assumed col 9 was empty, they remain valid (they assert specific buttons' columns, not emptiness). Only ADD assertions; do not loosen existing ones unless they genuinely conflict — if one does, fix the column literal to match the live source and note it in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TestCompanionTimeBar — MATLAB-only UI smoke suite</name>
  <files>tests/suite/TestCompanionTimeBar.m</files>
  <read_first>
    - tests/suite/TestFastSenseCompanion.m lines 1-60 (class-suite header, TestClassSetup addPaths, skipOnOctave in TestMethodSetup, hidden uifigure setup, teardown deleting figures)
    - libs/FastSenseCompanion/CompanionTimeBar.m (Plan 04 Tasks 1a+1b — the API + Tag names + the popup handle name hPopup_ to find/close in teardown)
    - libs/FastSenseCompanion/CompanionTimeRange.m (Plan 02 — construct the spec the bar edits; add a RangeChanged listener in the test)
    - libs/FastSenseCompanion/CompanionTheme.m (CompanionTheme.get('dark'/'light') struct)
    - .planning/phases/1041-.../1041-UI-SPEC.md "Surface 1" + "Surface 2" + "Copywriting Contract" (button label, preset texts, 'All data', Accent vs WidgetBorderColor)
    - .planning/phases/1041-.../1041-VALIDATION.md (TestCompanionTimeBar rows: testRangeButtonLabel, testPresetFiresEvent)
    - CLAUDE.md (Test prefix + PascalCase; testMethods camelCase verbs; Listeners_/timer cleanup; namespaced IDs)
  </read_first>
  <action>
    Create `tests/suite/TestCompanionTimeBar.m` as a `matlab.unittest.TestCase` class suite, MATLAB-only. Add `skipOnOctave` in `TestMethodSetup` exactly as TestFastSenseCompanion does. `TestClassSetup` method named `addPaths` calling `addpath` + `install()` per convention.

    Per-method setup: create a hidden host `uifigure('Visible','off')` + a `uigridlayout([1 10])` to mimic the toolbar, a `CompanionTimeRange`, a theme `CompanionTheme.get('dark')`, and a minimal `app` stand-in exposing `hFig_` (the CompanionTimeBar uses `app.hFig_` as the uialert parent + for theme-walker access). If the bar strictly requires a FastSenseCompanion handle (read the Plan 04 ctor), construct a hidden companion and read its `TimeBar_` instead — but prefer the lighter standalone host. Teardown (in TestMethodTeardown or per-test onCleanup): `bar.close()`, `delete(bar)`, delete the host figure, and assert no orphan figure named 'Time Range' remains.

    Test methods (camelCase, verb-first):
    - `testRangeButtonExists` — after constructing the bar, `findall(hostFig,'Tag','CompanionTimeRangeBtn')` returns exactly one uibutton with Layout.Column == 9.
    - `testRangeButtonDefaultLabel` — the button Text equals 'Last 7 days'.
    - `testOpenPickerCreatesOnePopup` — `bar.openPicker()`; assert exactly one uifigure named 'Time Range' with Position width 400 and height 280.
    - `testOpenPickerSingleton` — call `openPicker()` twice; assert still exactly one 'Time Range' figure.
    - `testPresetFiresEventAndUpdatesLabel` — add a RangeChanged listener counting fires; locate the 'Last 30 days' preset button (by Text) and invoke its ButtonPushedFcn (`cb = btn.ButtonPushedFcn; cb(btn, struct());`); assert counter incremented by 1, `[t0,t1]=range.resolve()` gives ~30-day span (`verifyEqual(t1-t0, 30, 'AbsTol', 1e-6)`), toolbar button Text == 'Last 30 days', and no 'Time Range' figure remains (popup closed).
    - `testAllDataPresetSetsAll` — invoke the 'All data' preset; assert `[t0,t1]=range.resolve()` returns `isempty(t0) && isempty(t1)` and button Text == 'All data'.
    - `testNonDefaultUsesAccent` — after a non-default preset, assert button BackgroundColor == theme.Accent (verifyEqual with AbsTol 1e-3); after a 'Last 7 days' preset, assert == theme.WidgetBorderColor.
    - `testThemeSwitchRestylesButton` — `bar.setTheme(CompanionTheme.get('light'))`; assert button FontColor/BackgroundColor changed to light-theme values, no error.

    To invoke a preset button programmatically: locate it (`findall(popupFig,'Type','uibutton')` filtered by `.Text`), then call its `ButtonPushedFcn`. Wrap teardown so already-deleted handles do not error.
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/PARA/10_Projects/FastPlot/.claude/worktrees/sleepy-grothendieck-1b8d2f && grep -q "classdef TestCompanionTimeBar" tests/suite/TestCompanionTimeBar.m && grep -q "skipOnOctave" tests/suite/TestCompanionTimeBar.m && grep -qE "testRangeButtonDefaultLabel|testPresetFiresEventAndUpdatesLabel|testOpenPickerSingleton" tests/suite/TestCompanionTimeBar.m && grep -q "CompanionTimeRangeBtn" tests/suite/TestCompanionTimeBar.m && echo SUITE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/suite/TestCompanionTimeBar.m` contains `classdef TestCompanionTimeBar` extending matlab.unittest.TestCase, with `skipOnOctave` in TestMethodSetup and an `addPaths` TestClassSetup.
    - Defines all eight test methods: testRangeButtonExists, testRangeButtonDefaultLabel, testOpenPickerCreatesOnePopup, testOpenPickerSingleton, testPresetFiresEventAndUpdatesLabel, testAllDataPresetSetsAll, testNonDefaultUsesAccent, testThemeSwitchRestylesButton (verify with grep).
    - References `'CompanionTimeRangeBtn'`, `'Last 7 days'`, `'Last 30 days'`, `'All data'`.
    - Static check clean: `mcp__matlab__check_matlab_code` on the file reports no errors.
    - In MATLAB via MCP (`run_matlab_test_file` on tests/suite/TestCompanionTimeBar.m): all methods pass (Octave-skipped). Confirm green in the user's MATLAB session.
  </acceptance_criteria>
  <done>A MATLAB-only UI smoke suite proves the range button renders with the right default label/color, the picker is a singleton 400x280 popup, presets commit + fire RangeChanged + update the label + close, 'All data' sets the all-spec, and theme switch restyles the button.</done>
</task>

<task type="auto">
  <name>Task 2: SpyTimeWindowEngine double + TestFastSenseCompanion integration tests</name>
  <files>tests/SpyTimeWindowEngine.m, tests/suite/TestFastSenseCompanion.m</files>
  <read_first>
    - tests/CaptureNotificationService.m (existing standalone test-double — the file shape for SpyTimeWindowEngine.m)
    - tests/suite/TestFastSenseCompanion.m (FULL skim — TestClassSetup, skipOnOctave, hidden-figure companion construction, how Engines_/OpenedFigures_ are populated, the latest toolbar-column tests, trackOpenedFigureForTest_ seam usage)
    - libs/FastSenseCompanion/FastSenseCompanion.m lines 1194-1210 (trackOpenedFigure), 1386-1392 (trackOpenedFigureForTest_), 2120-2160 (syncOpenedFigures_), plus the Plan-04 onRangeChanged_ + TimeRange_/TimeBar_
    - libs/Dashboard/DashboardEngine.m setTimeWindow (Plan 03)
    - .planning/phases/1041-.../1041-VALIDATION.md (re-query + open-site windowing validation targets)
    - CLAUDE.md (test conventions; handle-class inherit from handle; namespaced IDs; cleanup)
  </read_first>
  <action>
    A) Create `tests/SpyTimeWindowEngine.m` — a minimal `classdef SpyTimeWindowEngine < handle` test double (model on tests/CaptureNotificationService.m). Public properties: `hFigure = []`, `LastT0 = []`, `LastT1 = []`, `CallCount = 0`. Public method `setTimeWindow(obj, t0, t1)` that records `obj.LastT0 = t0; obj.LastT1 = t1; obj.CallCount = obj.CallCount + 1;`. Add no-op stubs for any method the companion's re-query path calls on an engine (read onRangeChanged_ + syncOpenedFigures_ — they call `isvalid`, read `.hFigure`, and `setTimeWindow`; isvalid works on any handle, so likely only `hFigure` + `setTimeWindow` are needed). Header comment per convention.

    B) Add integration test methods to the EXISTING `tests/suite/TestFastSenseCompanion.m` (they inherit skipOnOctave):
    - `testCompanionTimeRangeButtonDefaultLabel` — construct the hidden companion; `findall(obj.companion.hFig_,'Tag','CompanionTimeRangeBtn')` returns one button at Layout.Column 9 with Text 'Last 7 days'. Also assert the toolbar ColumnWidth array still equals `{110,110,110,130,70,90,70,70,'1x',36}` (regression guard against accidental grid changes).
    - `testRangeChangedRequeriesManagedEngines` — put a `SpyTimeWindowEngine` into the companion's `Engines_` (via the 'Dashboards' constructor arg or the suite's setProject path). Drive a range change: `companion.TimeRange_.setRelative(30,'days')`. Assert spy.CallCount >= 1 and `verifyEqual(spy.LastT1 - spy.LastT0, 30, 'AbsTol', 1/24)` (datenum days; tolerance for clock tick + the 30-day relative resolve).
    - `testRangeChangedRequeriesAdHocFigure` — create a hidden figure `hf = figure('Visible','off')`; `setappdata(hf,'DashboardEngine', spy2)` (a second SpyTimeWindowEngine); inject via `companion.trackOpenedFigureForTest_(hf)`. Fire a range change. Assert spy2.CallCount >= 1 with the resolved window. Clean up `delete(hf)`.
    - `testOpenSiteAppliesWindow` — assert the open-time windowing seam: drive (or mock) an open via the suite's existing openAdHocPlot DI/mock approach; assert after open `getappdata(hFig,'DashboardEngine')` is non-empty and (if a spy is injected through the suite's mock) its setTimeWindow was called with the resolved window. If the suite mocks openAdHocPlot via a constructor-injected callable, route a SpyTimeWindowEngine through that mock and assert CallCount >= 1. If no such mock exists, assert via the appdata seam (`~isempty(getappdata(hFig,'DashboardEngine'))`) and that `companion.currentTimeWindow()` (or `companion.TimeRange_.resolve()`) is non-empty for a non-'all' default — documenting that full end-to-end windowing of a real spawned engine is covered by the manual checklist.
    - `testTeardownDeletesTimeBar` — construct then close the companion; assert no figure named 'Time Range' remains and `close` raised no error (TimeBar_ + RangeChanged listener torn down).

    Use `verifyEqual`/`verifyNotEmpty`/`verifyTrue` with diagnostic strings; `'AbsTol', 1/24` on datenum comparisons; clean up every figure/handle each test creates.
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/PARA/10_Projects/FastPlot/.claude/worktrees/sleepy-grothendieck-1b8d2f && test -f tests/SpyTimeWindowEngine.m && grep -qF "classdef SpyTimeWindowEngine < handle" tests/SpyTimeWindowEngine.m && grep -qF "function setTimeWindow(obj, t0, t1)" tests/SpyTimeWindowEngine.m && grep -q "testCompanionTimeRangeButtonDefaultLabel" tests/suite/TestFastSenseCompanion.m && grep -qE "testRangeChangedRequeriesManagedEngines|testRangeChangedRequeriesAdHocFigure" tests/suite/TestFastSenseCompanion.m && grep -q "testTeardownDeletesTimeBar" tests/suite/TestFastSenseCompanion.m && echo INTEG_TESTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/SpyTimeWindowEngine.m` exists with `classdef SpyTimeWindowEngine < handle`, a public `setTimeWindow(obj, t0, t1)` recording LastT0/LastT1/CallCount, and a public `hFigure` property.
    - `tests/suite/TestFastSenseCompanion.m` adds: testCompanionTimeRangeButtonDefaultLabel, testRangeChangedRequeriesManagedEngines, testRangeChangedRequeriesAdHocFigure, testOpenSiteAppliesWindow, testTeardownDeletesTimeBar (verify with grep).
    - The managed-engine test asserts `setTimeWindow` was called with the resolved window (CallCount + LastT0/LastT1 with AbsTol 1/24).
    - The ad-hoc test uses `setappdata(hf,'DashboardEngine', spy)` + `trackOpenedFigureForTest_` and asserts the spy's setTimeWindow fired.
    - The default-label test re-asserts the toolbar ColumnWidth array unchanged.
    - Static checks clean: `mcp__matlab__check_matlab_code` on SpyTimeWindowEngine.m and TestFastSenseCompanion.m report no errors.
    - In MATLAB via MCP (`run_matlab_test_file` on tests/suite/TestFastSenseCompanion.m): all methods pass (Octave-skipped); confirm no regression vs the pre-1041 baseline count.
  </acceptance_criteria>
  <done>A setTimeWindow-recording spy + companion integration tests prove RangeChanged re-queries managed and ad-hoc engines with the resolved window, a newly opened view starts windowed (or the documented seam holds), the default toolbar button reads 'Last 7 days' in col 9, and teardown removes the bar + listener cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression gate + Octave data-layer pass + manual-verify checklist</name>
  <files>.planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-MANUAL-VERIFY.md, .planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-VALIDATION.md</files>
  <read_first>
    - tests/run_all_tests.m (how the full suite is discovered/run)
    - .planning/phases/1041-.../1041-VALIDATION.md "Manual-Only Verifications" table (the two human checks: picker visuals + relative slide) and "Validation Sign-Off"
    - .planning/phases/1041-.../1041-UI-SPEC.md "Interaction Contracts" (the exact behaviors a human verifies: mode switching, one-click quick apply, two-step Apply, label sync, Accent on non-default, live-tick relative slide)
    - CLAUDE.md "Running MATLAB code" (MCP test tools; prefer single-file runs; full run only when needed)
    - .claude memory note "Known env test failures" (the 14 pre-existing dashboard/companion failures in headless MCP runs are environmental — do NOT treat them as 1041 regressions; baseline them)
  </read_first>
  <action>
    A) Run the affected single-file tests in MATLAB via MCP and confirm green:
       - `test_sensor_tag_range()` (also confirm it passes in Octave — it is the Octave-safe data-layer file)
       - `test_companion_time_range()` (MATLAB green; Octave runs with event-test self-skipped)
       - `test_dashboard_time_window()` (MATLAB green; pure fan-out sub-tests also Octave-safe)
       - `run_matlab_test_file` on `tests/suite/TestCompanionTimeBar.m`
       - `run_matlab_test_file` on `tests/suite/TestFastSenseCompanion.m`
       Then run the FULL suite once (`tests/run_all_tests.m`) and record the result. Compare failures against the documented pre-existing environmental failures (the 14 known headless dashboard/companion failures + any MEX-gated skips) — any NEW failure attributable to 1041 must be fixed before the checkpoint. Capture the green/known-fail tally for the SUMMARY.

    B) Create `.planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-MANUAL-VERIFY.md` — a "## Manual Verification" checklist a human runs in MATLAB (these cannot be headlessly asserted per VALIDATION.md). Include numbered, concrete steps with expected outcomes:
       1. Open the companion on the industrial plant demo (or any project with a disk-backed sensor): `c = FastSenseCompanion('Dashboards', {...}, 'Registry', reg);`. Expect the toolbar range button to read "Last 7 days" with the default (WidgetBorderColor) background.
       2. Click the range button -> the Time Range popup opens (400x280, near the toolbar). Verify the three tabs (Quick / Relative / Absolute), Quick active by default, "Last 7 days" preset highlighted in Accent.
       3. Click "Last 30 days" -> popup closes immediately (one-click), the toolbar button updates to "Last 30 days" and turns Accent (non-default). Any open dashboard/ad-hoc plot re-queries (lines reflect the 30-day window; a disk-backed sensor visibly loads less).
       4. Reopen -> Relative tab: set N=2, unit=weeks; the preview label shows the resolved YYYY-MM-DD to YYYY-MM-DD; click Apply -> button reads "Last 2 weeks", views re-query.
       5. Reopen -> Absolute tab: pick a start and an end; preview shows "N days"; set start after end -> Apply disabled + "Invalid: start must be before end" in red; fix it -> Apply -> button shows "YYYY-MM-DD to YYYY-MM-DD", views load that fixed window.
       6. Select a tag whose data is entirely outside the active window (e.g. a 2018-only .mat sensor under Last 7 days) and open it -> the widget shows the centered "No data in selected range" empty-state (14pt bold), no crash.
       7. Relative-slide on live tick: with a live-to-today sensor and "Last 7 days" active, start Live; let several ticks elapse -> the window slides so the right edge tracks "now" (tail data streams in) WITHOUT a popup re-open or a full rerender flicker. With an Absolute window, confirm it stays fixed.
       8. Theme: flip the companion theme (gear -> Theme) -> the range button restyles to the new theme; if the popup is open it restyles too.
       9. Persistence: change the range, close + reopen the companion -> the button restores the last-used range (companionPrefs).
       Add a "Sign-off" line: tester name + date + pass/fail per step, mirroring the existing manual-verify docs in the repo's quick/ directories if one exists.

    C) Update `.planning/phases/1041-.../1041-VALIDATION.md`: fill the "Per-Task Verification Map" table rows with the actual test files/commands created across Plans 01-05 (test_sensor_tag_range, test_companion_time_range, test_dashboard_time_window, TestCompanionTimeBar, TestFastSenseCompanion additions), and check the two "Manual-Only Verifications" rows against 1041-MANUAL-VERIFY.md. Set `nyquist_compliant: true` in its frontmatter only if every task across the phase maps to an automated verify or a Wave-0 dependency (they do).
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/PARA/10_Projects/FastPlot/.claude/worktrees/sleepy-grothendieck-1b8d2f && test -f .planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-MANUAL-VERIFY.md && grep -q "Manual Verification" .planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-MANUAL-VERIFY.md && grep -q "No data in selected range" .planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-MANUAL-VERIFY.md && grep -q "Last 7 days" .planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-MANUAL-VERIFY.md && echo MANUAL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test_sensor_tag_range()`, `test_companion_time_range()`, `test_dashboard_time_window()` print "All N tests passed" in MATLAB (via MCP); `test_sensor_tag_range` also passes in Octave.
    - `TestCompanionTimeBar` and the new `TestFastSenseCompanion` methods pass in MATLAB (via `run_matlab_test_file`).
    - The full `tests/run_all_tests.m` run shows no NEW failures attributable to 1041 (only the documented pre-existing environmental failures/skips remain); the tally is recorded for the SUMMARY.
    - `.planning/phases/1041-.../1041-MANUAL-VERIFY.md` exists with a "## Manual Verification" heading and the 9 numbered steps incl. the "No data in selected range" empty-state and the relative-slide-on-live-tick check, plus a sign-off line.
    - `1041-VALIDATION.md` Per-Task Verification Map is populated with the real test files/commands and `nyquist_compliant: true` is set.
  </acceptance_criteria>
  <done>All phase tests are green in MATLAB (data-layer subset green in Octave) with no new regressions, and a concrete human checklist documents the picker visuals + relative-slide behaviors that require eyes-on verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of the time-range picker</name>
  <files>.planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-MANUAL-VERIFY.md</files>
  <action>
    CHECKPOINT — no code changes. The executor pauses and hands control to the human. Present the contents of `1041-MANUAL-VERIFY.md` and ask the user to run every numbered step in MATLAB, then wait for the resume signal. See `<what-built>` / `<how-to-verify>` / `<resume-signal>` for the human-facing detail.
  </action>
  <what-built>
    The global Kibana-style time-range picker: a toolbar range button + a 3-mode popup (Quick / Relative / Absolute) editing a shared CompanionTimeRange; on change, every companion-opened dashboard, ad-hoc plot, and sensor-detail view re-queries to load only the selected window (disk-backed sensors read fewer chunks); relative windows slide on live ticks; out-of-window widgets show a "No data in selected range" empty-state; the default is Last 7 days, persisted via companionPrefs.
  </what-built>
  <how-to-verify>
    Open the companion in MATLAB and work through every step in
    `.planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-MANUAL-VERIFY.md`
    (default label, picker open/singleton, one-click quick apply + Accent, relative builder + preview, absolute + validation, empty-state, relative slide on live tick, theme restyle, persistence across reopen). The MATLAB figures appear on your screen directly.
  </how-to-verify>
  <verify>
    <automated>MANUAL — human runs 1041-MANUAL-VERIFY.md in MATLAB; no headless command. Gated on the resume signal.</automated>
  </verify>
  <resume-signal>Type "approved" if every checklist step passes, or describe the steps that failed (with what you saw vs expected).</resume-signal>
  <done>Human has run the full manual-verify checklist in MATLAB and approved (or filed specific failures for gap closure).</done>
</task>

</tasks>

<verification>
- All Plan 01-05 automated tests green in MATLAB; data-layer subset green in Octave.
- `tests/run_all_tests.m` shows no new 1041 regressions (only documented environmental pre-existing failures/skips).
- Static analysis clean on all new test files (`mcp__matlab__check_matlab_code`).
- Manual-verify checklist exists and is exercised at the checkpoint.
</verification>

<success_criteria>
- The picker renders, is a singleton, and edits the shared CompanionTimeRange (asserted headlessly).
- RangeChanged re-queries managed + ad-hoc engines with the resolved window (asserted via spy).
- Newly opened views start windowed; teardown removes the bar + listener cleanly.
- The full suite is green (modulo documented environmental failures); the human checklist confirms the visual + live-slide behaviors.
</success_criteria>

<output>
After completion, create `.planning/phases/1041-global-kibana-style-time-range-for-the-companion-with-windowed-sensor-loading/1041-05-SUMMARY.md`.
</output>

# 999.2-05 Summary — Tag.exceedance (#316)

**Status:** Complete — 12/12 headless assertions pass, static analysis clean.

## Shipped
- `Tag.exceedance(level, 'Direction', above|below, 'Range', [t0 t1])` on `libs/SensorThreshold/Tag.m`. Returns struct totalTime/fraction/count/longest/peak. Continuous tags interpolate exact crossing boundaries; discrete tags use ZOH. NaN-bounded segments treated as gaps.
- `tests/test_tag_exceedance.m`.

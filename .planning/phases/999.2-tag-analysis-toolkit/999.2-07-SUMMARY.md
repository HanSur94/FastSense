# 999.2-07 Summary — Tag.cumulativeIntegral (#327)

**Status:** Complete — 8/8 headless assertions pass, static analysis clean.

## Shipped
- `Tag.cumulativeIntegral('Range', [t0 t1])` on `libs/SensorThreshold/Tag.m`. 2-out [t, cum] running trapezoid; 1-out scalar grand total. NaN-bounded segments contribute zero (no poisoning). Warns `Tag:integralOnDiscrete` on discrete kinds.
- `tests/test_tag_cumulativeintegral.m`.

## Notes
- Octave-safe: hand-rolled NaN-safe trapezoid (no cumtrapz).

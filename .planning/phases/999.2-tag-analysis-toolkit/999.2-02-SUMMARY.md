# 999.2-02 Summary — Tag.resampleUniform (#308)

**Status:** Complete — 10/10 headless assertions pass, static analysis clean.

## Shipped
- `Tag.resampleUniform(dt, 'Range', [t0 t1], 'Method', m, 'MaxGap', g)` on `libs/SensorThreshold/Tag.m`. Uniform grid via `interp1`; kind-aware default method (continuous→linear, discrete→previous/ZOH); Range clips the grid to the window (padded slice still bounds interpolation); optional MaxGap NaN-fills across dropouts.
- `tests/test_tag_resampleuniform.m`.

## Notes
- Toolbox-free (interp1 linear/previous). Honours the ZOH-only invariant for state/monitor channels.

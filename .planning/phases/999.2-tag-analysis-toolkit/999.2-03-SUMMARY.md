# 999.2-03 Summary — Tag.derivative (#326)

**Status:** Complete — 13/13 headless assertions pass, static analysis clean.

## Shipped
- `Tag.derivative('Method', m, 'Range', [t0 t1])` on `libs/SensorThreshold/Tag.m`. Central (default, non-uniform-aware via `gradient`), forward, backward. NaN-safe. Warns `Tag:derivativeOnDiscrete` on state/monitor.
- `tests/test_tag_derivative.m`.

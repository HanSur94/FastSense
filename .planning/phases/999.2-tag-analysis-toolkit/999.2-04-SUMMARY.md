# 999.2-04 Summary — Tag.movingStat (#312)

**Status:** Complete — 14/14 headless assertions pass, static analysis clean.

## Shipped
- `Tag.movingStat(window, type)` on `libs/SensorThreshold/Tag.m`. type ∈ mean|std|max|min|rms|median over a centered sample window (shrinking at edges). NaNs excluded per window. Errors `Tag:movingStatNotContinuous` on discrete kinds.
- `tests/test_tag_movingstat.m`.

## Notes
- Octave-safe: hand-rolled window loop (no movmean/movstd dependency).

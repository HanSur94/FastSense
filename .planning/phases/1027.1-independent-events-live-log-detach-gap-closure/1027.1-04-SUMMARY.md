---
phase: 1027.1-independent-events-live-log-detach-gap-closure
plan: 04
subsystem: FastSenseCompanion
tags: [test, livelogpane, port, class-based-test, detach, theme]
dependency-graph:
  requires: [LiveLogPane (Plan 02), CompanionTheme, install]
  provides: [TestLiveLogPane class-based unit suite — 13 test methods covering attach/detach lifecycle, buffer preservation, 500-row cap, clearLiveLog, theme propagation, DetachRequested event firing]
  affects: [Plan 05 (companion integration — TestLiveLogPane is a regression baseline), Plan 07 (companion tests — must keep this suite green)]
tech-stack:
  added: []
  patterns:
    - "Class-based matlab.unittest.TestCase suite (TestClassSetup addPaths + TestMethodSetup skipOnOctave)"
    - "Hidden uifigure + addTeardown(@() delete(...)) for handle cleanup"
    - "Test-helper makePane_ private method returns [pane, hFig]"
    - "containers.Map closure-bag pattern for event-fire counting (DetachRequested)"
    - "verbatim-port discipline: live-half tests retargeted from LogPane to LiveLogPane with bufferSize() arity reduction"
key-files:
  created:
    - "tests/suite/TestLiveLogPane.m"
  modified: []
decisions:
  - "Single-arg bufferSize() throughout — LiveLogPane has one buffer, no 'log'/'live' selector"
  - "Two new tests added beyond the LogPane port: testDetachPreservesLiveBuffer (live-equivalent of testDetachPreservesBuffers), testAddLiveLogEntryWhileDetached (live-equivalent of testAddLogEntryWhileDetached)"
  - "Events-half tests (testLogBufferOrdering, testSetLastUpdatedNoOpWhenDetached, testFiveHundredRowCapLog) DROPPED — they belong to TestEventsLogPane (Plan 03)"
  - "testFiveHundredRowCapLive renamed to testFiveHundredRowCap (no 'Live' suffix needed since this suite is live-only)"
  - "DetachRequested firing test ports verbatim — tests the new pop-out icon's event without referencing the private hPopoutBtn_ handle"
  - "TestClassSetup addPaths and bumpBag helper ported verbatim from TestLogPane.m (same install() bootstrap, same closure bag pattern)"
metrics:
  duration: "~5m"
  completed: "2026-05-08"
  tasks_completed: 1
  files_created: 1
  files_modified: 0
  total_lines: 192
---

# Phase 1027.1 Plan 04: Test LiveLogPane Summary

Class-based unit suite for `LiveLogPane` (built in Plan 02) — 13 tests covering attach/detach lifecycle, buffer preservation across re-attach round-trips, 500-row cap, clearLiveLog, dark<->light theme propagation, and DetachRequested event firing for the new per-pane pop-out icon.

## Test Inventory (13 tests)

| Test | Source | Adaptation |
| ---- | ------ | ---------- |
| `testConstructorRequiresTheme` | TestLogPane:27 | Error ID `LogPane:invalidTheme` -> `LiveLogPane:invalidTheme` |
| `testConstructDoesNotBuildUI` | TestLogPane:33 | LogPane -> LiveLogPane |
| `testAttachToUifigure` | TestLogPane:41 | LogPane -> LiveLogPane |
| `testAttachToUipanel` | TestLogPane:49 | LogPane -> LiveLogPane |
| `testDetachClearsHandles` | TestLogPane:62 | `addLogEntry('info', ...)` -> `addLiveLogEntry('tag.x', 1, 1.0)`; `bufferSize('log')` -> `bufferSize()` |
| `testDetachPreservesLiveBuffer` | NEW (live equivalent of TestLogPane:72 testDetachPreservesBuffers) | Uses `addLiveLogEntry`, `bufferSize()` arity 0 |
| `testAddLiveLogEntryWhileDetached` | NEW (live equivalent of TestLogPane:93 testAddLogEntryWhileDetached) | Uses `addLiveLogEntry`, `bufferSize()` arity 0 |
| `testFiveHundredRowCap` | TestLogPane:117 testFiveHundredRowCapLive | Renamed (no 'Live' suffix); `bufferSize('live')` -> `bufferSize()` |
| `testClearLiveLog` | TestLogPane:127 | `bufferSize('live')` -> `bufferSize()` |
| `testApplyThemeDarkToLight` | TestLogPane:138 | LogPane -> LiveLogPane |
| `testApplyThemeLightToDark` | TestLogPane:147 | LogPane -> LiveLogPane |
| `testApplyThemeNoOpWhenDetached` | TestLogPane:156 | LogPane -> LiveLogPane |
| `testDetachRequestedEventFires` | TestLogPane:165 | LogPane -> LiveLogPane; verifies the new per-pane pop-out icon's DetachRequested event |

**Tests dropped (events-half, belong to Plan 03's TestEventsLogPane):**
- `testFiveHundredRowCapLog`
- `testLogBufferOrdering`
- `testSetLastUpdatedNoOpWhenDetached`

## What Was Ported Verbatim

- `methods (TestClassSetup) addPaths` block — `addpath(...) + install()` bootstrap
- `methods (TestMethodSetup) skipOnOctave` — `assumeFalse(exist('OCTAVE_VERSION', 'builtin') ~= 0, ...)` Octave gate
- `private makePane_(testCase, themeName)` helper — hidden uifigure + LiveLogPane + attach + double teardown
- Top-level `bumpBag(b)` helper — closure-bag mutation workaround for class-based test methods

## Acceptance Criteria — Verified

| Check | Required | Actual |
| ----- | -------- | ------ |
| `^classdef TestLiveLogPane < matlab.unittest.TestCase` | 1 | 1 |
| `function test[A-Z]` test method count | >=12 | 13 |
| `LiveLogPane:invalidTheme` references | >=1 | 3 |
| `bufferSize('log')\|bufferSize('live')` (must be 0) | 0 | 0 |
| `function testDetachPreservesLiveBuffer` | 1 | 1 |
| `function testAddLiveLogEntryWhileDetached` | 1 | 1 |
| `function testClearLiveLog` | 1 | 1 |
| `function testFiveHundredRowCap` | 1 | 1 |
| Events-half symbols (`addLogEntry`, `setLastUpdated`, `peekLogRow`, `hLogTable_`, `hLogSearch_`) | 0 | 0 |
| `LiveLogPane(` occurrences | >=5 | 8 |
| `mh_lint tests/suite/TestLiveLogPane.m` exit | 0 | 0 (everything seems fine) |
| `mh_style tests/suite/TestLiveLogPane.m` exit | 0 | 0 (everything seems fine) |

## Pop-Out Icon Coverage

The `testDetachRequestedEventFires` test verifies the new per-pane pop-out icon's event-firing contract by listening on `DetachRequested` and calling `requestDetach()` twice (the public wrapper for the button's `notify(obj, 'DetachRequested')` callback). Two clicks -> two listener hits. This is the core behavioural test for the Phase 1027.1 architectural change (each log gets its OWN pop-out icon, vs Phase 1027's single shared icon).

## Deviations from Plan

None — plan executed exactly as written. All 13 test methods specified in the plan are present, all acceptance criteria pass, both lint and style exit clean.

## Lint Status

- `mh_lint tests/suite/TestLiveLogPane.m` -> "1 file(s) analysed, everything seems fine" (exit 0)
- `mh_style tests/suite/TestLiveLogPane.m` -> "1 file(s) analysed, everything seems fine" (exit 0)

## Pass/Fail Run Output

The matlab MCP is parent-only in this parallel-executor context — actual `runtests('tests/suite/TestLiveLogPane.m')` execution is deferred to the orchestrator's verifier pass. All static checks (grep acceptance criteria, MISS_HIT lint, MISS_HIT style) pass on this end.

## Self-Check: PASSED

- File exists: `tests/suite/TestLiveLogPane.m` -> FOUND (192 lines)
- Commit exists: `8a61dc0` -> FOUND

---
phase: 1016-examples-05-events-rewrite-live-demos-ci-grep-seal
plan: 03
type: execute
wave: 2
depends_on: [1016-01, 1016-02]
files_modified:
  - .github/workflows/tests.yml
autonomous: true
requirements: [DIFF-01]
must_haves:
  truths:
    - ".github/workflows/tests.yml lint job fails CI on any new reference to the 8 Phase-1011 deleted classes (Threshold, CompositeThreshold, StateChannel, ThresholdRule, Sensor, SensorRegistry, ThresholdRegistry, ExternalSensorRegistry) within libs/, tests/, examples/, benchmarks/ (DIFF-01)"
    - "fp.addThreshold( and obj.addThreshold( surviving FastSense plot-annotation API are NOT flagged (allow-list via word-boundary regex)"
    - "SensorTag(, MonitorTag(, EventStore(, StateTag(, CompositeTag( surviving v2.0 API surface is NOT flagged"
    - "Local hit-case dry run (write a temp file with `Threshold(` and run the step over it) returns exit-non-zero — the seal actually catches re-introductions"
    - "Local miss-case dry run (run the step over the v2.1 tip post-1016-01/-02) returns exit 0 — clean repo passes"
  artifacts:
    - path: ".github/workflows/tests.yml"
      provides: "Lint job step 'Phase-1011 deleted-class regression seal (DIFF-01)' alongside the existing skip-list parity gate"
      contains: "DIFF-01"
  key_links:
    - from: ".github/workflows/tests.yml lint job"
      to: "8 Phase-1011 deleted classes"
      via: "grep -rE pattern with word-boundary lookbehind"
      pattern: "\\(\\^\\|\\[\\^\\.a-zA-Z_\\]\\)"
---

<objective>
Add the **CI grep seal** that locks the v2.1 cleanup forever: a step in `.github/workflows/tests.yml` `lint` job that fails CI on any new reference to the 8 classes deleted in Phase 1011 (`Threshold`, `CompositeThreshold`, `StateChannel`, `ThresholdRule`, `Sensor`, `SensorRegistry`, `ThresholdRegistry`, `ExternalSensorRegistry`) inside `libs/`, `tests/`, `examples/`, `benchmarks/`.

The regex must use the established Phase 1015 idiom `(^|[^.a-zA-Z_])(ClassName)\(` with word-boundary lookbehind, so:
- `Sensor(...)` HITS (legacy constructor — re-introduction blocked)
- `SensorTag(...)` does NOT hit (surviving v2.0 API — `[^.a-zA-Z_]` lookbehind matches `S` in `SensorTag`? — re-verify below)
- `fp.addThreshold(...)` does NOT hit (surviving FastSense plot-annotation API — `.` in lookbehind blocks)
- `obj.addThreshold(...)` does NOT hit (same — `.` in lookbehind)
- `MonitorTag(...)`, `EventStore(...)`, `StateTag(...)`, `CompositeTag(...)` do NOT hit

**Re-verify regex:** `(^|[^.a-zA-Z_])(Sensor)\(` matches the literal text `Sensor(` ONLY when the character before `S` is start-of-line OR a non-alphanumeric/non-underscore/non-dot character. In `SensorTag(`, the character before `T` is `r` — but the regex is anchored on `(Sensor)\(` not `(Sensor)Tag\(`. So the regex tries to match `Sensor(` inside `SensorTag(` — but `SensorTag(` does NOT contain the substring `Sensor(` (the `(` is after `Tag`, not after `Sensor`). The regex never matches `SensorTag(` at all because there's no `(` directly after `Sensor`. ✅ Correct.

Same logic for `Threshold(` vs `fp.addThreshold(`: the literal text `Threshold(` appears inside both. But `(^|[^.a-zA-Z_])` lookbehind requires the char before `T` to be `^` or `[^.a-zA-Z_]`. In `fp.addThreshold(`, the char before `T` is `.` — REJECTED by `[^.a-zA-Z_]` (the dot is excluded). ✅ Correct.

In `addThreshold(`, the char before `T` is `d` — REJECTED by `[^.a-zA-Z_]` (lowercase letter is excluded). ✅ Correct (catches `obj.addThreshold` indirectly via the `d` not the dot).

In `MonitorTag(`: the literal text `Threshold(` does not appear, so no match anyway. ✅.

In standalone `Threshold(...)` (legacy constructor): preceded by start-of-line, whitespace, `(`, etc. — all match `(^|[^.a-zA-Z_])`. ✅ HIT.

Final regex per class: `(^|[^.a-zA-Z_])(Threshold|CompositeThreshold|StateChannel|ThresholdRule|Sensor|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\(`

This is single-pass — one grep invocation across all 8 classes via alternation. Implementation: a single `grep -rE` over `libs tests examples benchmarks` with the alternation; non-zero exit means a hit was found → fail CI.

Output: One commit on `.github/workflows/tests.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1016-examples-05-events-rewrite-live-demos-ci-grep-seal/1016-CONTEXT.md
@.github/workflows/tests.yml
@scripts/check_skip_list_parity.sh

<interfaces>
<!-- The lint job in tests.yml currently has 4 steps: -->
<!--   - actions/checkout@v6 -->
<!--   - Install MISS_HIT (pip install miss_hit) -->
<!--   - Run style checker (mh_style libs/ tests/ examples/) -->
<!--   - Run linter (mh_lint libs/ tests/ examples/) -->
<!--   - Run complexity metrics (mh_metric --ci libs/ tests/ examples/) -->
<!--   - Skip-list parity gate (DIFF-04) (bash scripts/check_skip_list_parity.sh) -->
<!-- -->
<!-- Add the new step BETWEEN "Run complexity metrics" and "Skip-list parity gate" -->
<!-- so failure ordering is: lint → metrics → DIFF-01 grep seal → DIFF-04 parity. -->
<!-- DIFF-01 fires faster (single grep) and gives clearer signal than DIFF-04. -->
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Phase-1011 deleted-class regression seal step to .github/workflows/tests.yml lint job + self-test the regex against hit and miss cases</name>
  <files>.github/workflows/tests.yml</files>
  <read_first>
    - .github/workflows/tests.yml lines 125-148 (current lint job structure — 4 mh_* steps + skip-list parity gate)
    - scripts/check_skip_list_parity.sh (precedent for an inline-bash CI step)
    - .planning/REQUIREMENTS.md DIFF-01 entry (allow-list rules)
  </read_first>
  <action>
    Edit `.github/workflows/tests.yml` to add ONE new step to the `lint` job, inserted between the existing "Run complexity metrics" step (line 143-144) and "Skip-list parity gate (DIFF-04)" step (line 146-147).

    The new step uses an inline bash script. The grep alternation pattern must be EXACTLY this (8 classes, `|`-separated):
    ```
    (^|[^.a-zA-Z_])(Threshold|CompositeThreshold|StateChannel|ThresholdRule|Sensor|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\(
    ```

    The step:
    ```yaml
          - name: Phase-1011 deleted-class regression seal (DIFF-01)
            run: |
              # DIFF-01 — fail CI on any new reference to the 8 classes deleted in
              # Phase 1011 within libs/ tests/ examples/ benchmarks/. The regex uses
              # word-boundary lookbehind so:
              #   - `Sensor(`, `Threshold(`, etc. (legacy constructors) HIT and fail
              #   - `SensorTag(`, `MonitorTag(`, `EventStore(`, `StateTag(`,
              #     `CompositeTag(` (surviving v2.0 API) DO NOT hit
              #   - `fp.addThreshold(` and `obj.addThreshold(` (surviving
              #     FastSense plot-annotation API) DO NOT hit (`.` and `d` in
              #     the lookbehind position are both rejected)
              #
              # Self-test: matches the Phase 1015 P01 idiom for the test-suite
              # cleanup grep gate (TEST-10).
              set -euo pipefail
              PATTERN='(^|[^.a-zA-Z_])(Threshold|CompositeThreshold|StateChannel|ThresholdRule|Sensor|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\('
              echo "Searching for Phase-1011 deleted-class references..."
              echo "Pattern: ${PATTERN}"
              echo "Scope:   libs/ tests/ examples/ benchmarks/"
              echo ""

              # Self-test (sanity): write a temp file with a known hit + a known
              # miss and confirm the regex's discrimination before scanning the repo.
              tmpdir="$(mktemp -d)"
              trap 'rm -rf "${tmpdir}"' EXIT
              printf 's = Sensor(...);\n'           > "${tmpdir}/should_hit.m"
              printf 'fp.addThreshold(50);\n'        > "${tmpdir}/should_miss_addThreshold.m"
              printf 's = SensorTag(...);\n'         > "${tmpdir}/should_miss_sensortag.m"
              printf 'm = MonitorTag(...);\n'        > "${tmpdir}/should_miss_monitortag.m"

              if ! grep -rE "${PATTERN}" "${tmpdir}/should_hit.m" > /dev/null; then
                  echo "REGEX SELF-TEST FAILED: should_hit.m did NOT match — regex is broken."
                  exit 2
              fi
              for f in should_miss_addThreshold should_miss_sensortag should_miss_monitortag; do
                  if grep -rE "${PATTERN}" "${tmpdir}/${f}.m" > /dev/null; then
                      echo "REGEX SELF-TEST FAILED: ${f}.m DID match — false positive on surviving API."
                      grep -rE "${PATTERN}" "${tmpdir}/${f}.m" || true
                      exit 2
                  fi
              done
              echo "Regex self-test passed (1 hit + 3 misses)."
              echo ""

              # Real scan
              if grep -rE "${PATTERN}" libs tests examples benchmarks; then
                  echo ""
                  echo "DIFF-01 GATE FAILED: re-introduction of a Phase-1011 deleted class."
                  echo "Allowed APIs: SensorTag, MonitorTag, StateTag, CompositeTag, EventStore,"
                  echo "              fp.addThreshold(...), obj.addThreshold(...)"
                  echo "Re-introducing any of: Threshold, CompositeThreshold, StateChannel,"
                  echo "                       ThresholdRule, Sensor, SensorRegistry,"
                  echo "                       ThresholdRegistry, ExternalSensorRegistry"
                  echo "is a DIFF-01 regression. Migrate to the v2.0 Tag API."
                  exit 1
              fi
              echo "DIFF-01 GATE PASSED: no Phase-1011 deleted-class re-introductions."
    ```

    Indentation in YAML must match the surrounding lint-job step indentation (6 spaces leading `- name:` to align with `- name: Run complexity metrics` at line 143).

    **Order in lint job after edit:**
    1. Checkout
    2. Install MISS_HIT
    3. Run style checker
    4. Run linter
    5. Run complexity metrics
    6. **Phase-1011 deleted-class regression seal (DIFF-01)** ← new
    7. Skip-list parity gate (DIFF-04)

    **Verification commands** (executor MUST run these locally before commit):
    1. **Hit-case dry run:** Create a temp file with the literal text `Sensor(`, run the regex against it, expect exit-non-zero:
       ```bash
       echo 's = Sensor(1, 2, 3);' > /tmp/hit_test.m
       grep -rE '(^|[^.a-zA-Z_])(Threshold|CompositeThreshold|StateChannel|ThresholdRule|Sensor|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\(' /tmp/hit_test.m
       echo "exit=$?"   # expect: 0 (grep found a match)
       ```
    2. **Miss-case dry run** over the v2.1 tip (post-1016-01/-02 commits):
       ```bash
       grep -rE '(^|[^.a-zA-Z_])(Threshold|CompositeThreshold|StateChannel|ThresholdRule|Sensor|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\(' libs tests examples benchmarks
       echo "exit=$?"   # expect: 1 (no matches → grep returns 1)
       ```
    3. **YAML lint:** `yamllint .github/workflows/tests.yml` (if installed) or visually verify indentation.

    If the miss-case dry run finds matches in the current repo, ESCALATE to the user — there are remaining un-migrated references that must be cleaned before the seal lands. (Phase 1015 P03 should have covered tests/; Phase 1012 should have covered examples/. Any hits at this stage are unexpected and need diagnosis, not a seal that immediately fails.)

    **Commit:** Stage and commit ONLY `.github/workflows/tests.yml`. Message:
    `ci(lint): add Phase-1011 deleted-class regression seal (DIFF-01)`
  </action>
  <verify>
    <automated>
      grep -F 'Phase-1011 deleted-class regression seal (DIFF-01)' .github/workflows/tests.yml
      grep -F 'ExternalSensorRegistry' .github/workflows/tests.yml
      grep -F '(Threshold|CompositeThreshold|StateChannel|ThresholdRule|Sensor|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)' .github/workflows/tests.yml
      # Hit-case test: a file with Sensor( triggers the regex (exit 0 from grep)
      echo 's = Sensor(1, 2, 3);' > /tmp/diff01_hit_test.m
      grep -rE '(^|[^.a-zA-Z_])(Threshold|CompositeThreshold|StateChannel|ThresholdRule|Sensor|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\(' /tmp/diff01_hit_test.m
      rm -f /tmp/diff01_hit_test.m
      # Miss-case test: real repo passes (grep returns 1, expected — wrap in negation)
      ! grep -rE '(^|[^.a-zA-Z_])(Threshold|CompositeThreshold|StateChannel|ThresholdRule|Sensor|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\(' libs tests examples benchmarks
    </automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/tests.yml` contains a new step named `Phase-1011 deleted-class regression seal (DIFF-01)` inside the `lint` job
    - The step's regex includes all 8 class names exactly: `Threshold`, `CompositeThreshold`, `StateChannel`, `ThresholdRule`, `Sensor`, `SensorRegistry`, `ThresholdRegistry`, `ExternalSensorRegistry`
    - The step uses `(^|[^.a-zA-Z_])(...)\(` word-boundary lookbehind idiom (Phase 1015 precedent)
    - Hit-case self-test in the inline script verifies the regex flags `Sensor(` and does NOT flag `SensorTag(`, `MonitorTag(`, `fp.addThreshold(`
    - Miss-case dry run on the current repo (post-1016-01 + 1016-02 wave-1 plans) finds zero matches across `libs/ tests/ examples/ benchmarks/`
    - The new step is positioned between "Run complexity metrics" and "Skip-list parity gate (DIFF-04)" — adjacent to the existing DIFF-04 gate for cohesion
    - Single git commit, only `.github/workflows/tests.yml` touched
  </acceptance_criteria>
  <done>
    `.github/workflows/tests.yml` lint job runs the DIFF-01 grep seal on every PR and push. Regex hits any of the 8 deleted-class constructors and fails CI; allow-listed surviving APIs (SensorTag, MonitorTag, fp.addThreshold, obj.addThreshold) pass cleanly. Local hit-case test fires (exit 0 from grep), miss-case test passes (exit 1 from grep, negated to exit 0). Committed as a single per-file commit; v2.1 milestone is sealed against re-introductions.
  </done>
</task>

</tasks>

<verification>
**Phase-wide verification (Phase 1016 exit gate — Gate C in ROADMAP § 1016 verification gates):**

```bash
# DIFF-01 grep seal locally (mirrors the CI step)
grep -rE '(^|[^.a-zA-Z_])(Threshold|CompositeThreshold|StateChannel|ThresholdRule|Sensor|SensorRegistry|ThresholdRegistry|ExternalSensorRegistry)\(' libs tests examples benchmarks
# Expect: exit 1 (no matches) — the seal passes locally on the v2.1 tip

# Confirm the surviving APIs are NOT flagged
grep -rE '(SensorTag|MonitorTag|StateTag|CompositeTag|EventStore)\(' libs/SensorThreshold/ libs/EventDetection/ examples/05-events/ | head -5
# Expect: many hits (these are the v2.0 surface) — NOT flagged by the DIFF-01 regex

grep -rnE 'fp\.addThreshold\(|obj\.addThreshold\(' libs/FastSense/ examples/ 2>/dev/null | head -3
# Expect: hits (FastSense plot-annotation API survives) — NOT flagged by the DIFF-01 regex

# YAML structural sanity
grep -A 2 'name: MATLAB Lint' .github/workflows/tests.yml | head -5
grep -B 1 'Phase-1011 deleted-class regression seal' .github/workflows/tests.yml
```

**Commit count:** exactly 1 commit from this plan.

**Phase-wide commit count after Plans 1016-01 + 1016-02 + 1016-03:**
- 1016-01: 2 commits (one per demo)
- 1016-02: 2 commits (live_pipeline + run_all_examples skip-list)
- 1016-03: 1 commit (CI seal)
- **Total: 5 commits** (matches Phase 1015 per-file-commit-discipline cadence)
</verification>

<success_criteria>
- [x] CI grep seal step in `.github/workflows/tests.yml` lint job (DIFF-01)
- [x] Regex covers all 8 Phase-1011 deleted classes
- [x] Word-boundary lookbehind allow-lists surviving APIs (SensorTag, MonitorTag, fp.addThreshold, obj.addThreshold, etc.)
- [x] Inline self-test in the CI step verifies regex discrimination on every CI run
- [x] Local hit-case dry run flags re-introductions; local miss-case dry run passes on v2.1 tip
- [x] Step placed adjacent to the existing skip-list parity gate (DIFF-04) for cohesion
- [x] Single per-file commit
</success_criteria>

<output>
After completion, the orchestrator will create `.planning/phases/1016-examples-05-events-rewrite-live-demos-ci-grep-seal/1016-03-SUMMARY.md` followed by the **phase-wide 1016-SUMMARY.md** documenting:
- All 5 commit SHAs across the 3 plans
- The 6 ROADMAP success criteria with PASS/FAIL verdicts
- Confirmation of the 6-gate exit pattern (Gate A scope, Gate B golden untouched, Gate C grep seal, Gate D Octave smoke, Gate E MATLAB CI, Gate F skip-list parity)
- v2.1 milestone close-out — the CI grep seal is the canonical Gate C for the cumulative cleanup
</output>

---
phase: 1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api
plan: 03
type: execute
wave: 3
depends_on:
  - 1015-01
  - 1015-02
files_modified:
  - .planning/phases/1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api/1015-SUMMARY.md
autonomous: false
requirements:
  - TEST-11

must_haves:
  truths:
    - "User running tests/run_all_tests.m on MATLAB R2020b sees a green suite (Gate E satisfied)"
    - "User running tests/run_all_tests.m via Octave sees a green suite (Gate D satisfied — surviving Octave-flat tests pass; deleted/migrated sidecars no longer crash)"
    - "User running scripts/check_skip_list_parity.sh sees exit 0 (Gate F satisfied)"
    - "User running grep -rE '(^|[^.a-zA-Z_])(Threshold|CompositeThreshold|StateChannel|ThresholdRule)\\(' tests/ sees zero hits (Gate C re-confirmed at plan close)"
    - "User opening 1015-SUMMARY.md sees the test-method-count baseline drop documented with explicit counts (TEST-11)"
    - "User running git diff against the phase's first commit sees only files in the union of all 3 plans' files_modified (Gate A re-confirmed)"
    - "User running git diff against the phase's first commit on tests/**/*olden* sees only the single banner-addition commit's 2-line/2-file diff (Gate B re-confirmed)"
  artifacts:
    - path: ".planning/phases/1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api/1015-SUMMARY.md"
      provides: "Phase-level summary documenting all 6 gate verdicts + test-count baseline drop + commit roster"
      contains: "## Gate Verdicts"
      min_lines: 80
  key_links:
    - from: "1015-SUMMARY.md"
      to: "1015-01-SUMMARY.md, 1015-02-SUMMARY.md"
      via: "consolidates plan-level summaries"
      pattern: "1015-0[12]-SUMMARY"
---

<objective>
Phase 1015 Plan 03 — close-out verification + 6-gate exit + test-count baseline drop documentation.

Purpose: Run all 6 gates (A/B/C/D/E/F) against the phase's tip commit, document the test-method baseline drop from Plans 01-02, and emit the phase-level SUMMARY.md.

Output:
- 1015-SUMMARY.md with explicit verdict for each of the 6 gates
- Test-method baseline drop captured with pre/post counts and a one-line "why" per deletion
- Final commit list for the phase
- A single human-verify checkpoint pausing for the user to confirm `tests/run_all_tests.m` is green on MATLAB R2020b (Gate E — only the user has the licensed MATLAB R2020b)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api/1015-CONTEXT.md
@.planning/phases/1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api/1015-01-PLAN.md
@.planning/phases/1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api/1015-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated gates A/B/C/F and capture verdicts</name>
  <files>/tmp/1015_gate_verdicts.txt</files>
  <read_first>
    - .planning/phases/1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api/1015-01-PLAN.md (frontmatter `files_modified` list — Gate A union)
    - .planning/phases/1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api/1015-02-PLAN.md (frontmatter `files_modified` list — Gate A union)
    - .planning/phases/1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api/1015-01-SUMMARY.md (per-plan close-out)
    - .planning/phases/1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api/1015-02-SUMMARY.md (per-plan close-out)
  </read_first>
  <action>
    Identify the phase's first commit:
      `PHASE_BASE="$(git log --format=%H --reverse --grep='1015-01' | head -1)~1"`
      (If the grep finds nothing, fall back to: `PHASE_BASE="$(git merge-base HEAD origin/main 2>/dev/null || git merge-base HEAD main)"`)

    **Gate A — scope:** confirm `git diff --name-only "$PHASE_BASE"..HEAD` is a SUBSET of the union of all 3 plans' `files_modified` frontmatter.
      ```bash
      EXPECTED=$(cat <<'EOF'
      tests/suite/TestEventConfig.m
      tests/suite/TestEventDetectorTag.m
      tests/test_event_config.m
      tests/test_event_detector.m
      tests/test_incremental_detector.m
      tests/suite/TestGoldenIntegration.m
      tests/test_golden_integration.m
      tests/suite/makeV21Fixtures.m
      scripts/check_skip_list_parity.sh
      .github/workflows/tests.yml
      tests/test_SensorDetailPlot.m
      tests/test_event_store.m
      tests/test_gauge_widget.m
      tests/test_icon_card_widget_tag.m
      tests/test_multistatus_widget_tag.m
      .planning/phases/1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api/
      EOF
      )
      ACTUAL=$(git diff --name-only "$PHASE_BASE"..HEAD)
      # Every line in $ACTUAL must be in $EXPECTED (or under a directory in $EXPECTED).
      ```
      Record PASS/FAIL with diff if FAIL.

    **Gate B — golden untouched:** confirm only the single banner-addition commit touched `tests/**/*olden*`.
      ```bash
      git log --oneline "$PHASE_BASE"..HEAD -- 'tests/**/*olden*' | wc -l
      ```
      Expected output: `1` (the banner commit). If the count is 0, Plan 01 Task 2 didn't run — block. If > 1, Gate B failed — investigate.

    **Gate C — Threshold-family grep clean:**
      ```bash
      grep -rE '(^|[^.a-zA-Z_])(Threshold|CompositeThreshold|StateChannel|ThresholdRule)\(' tests/ | wc -l
      ```
      Expected output: `0`. If > 0, list the offending lines and block.

    **Gate F — skip-list parity:**
      ```bash
      bash scripts/check_skip_list_parity.sh; echo "exit_code=$?"
      ```
      Expected: `exit_code=0`. If non-zero, list the diff output and block.

    Record each gate verdict in `/tmp/1015_gate_verdicts.txt` (PASS/FAIL plus relevant evidence). Do NOT commit yet — Task 4 collates these into 1015-SUMMARY.md.
  </action>
  <verify>
    <automated>bash -c 'set -e; grep -rE "(^|[^.a-zA-Z_])(Threshold|CompositeThreshold|StateChannel|ThresholdRule)\(" tests/ | wc -l | grep -q "^0$" && bash scripts/check_skip_list_parity.sh && test "$(git log --oneline HEAD~10..HEAD -- "tests/**/*olden*" | wc -l)" -le 1'</automated>
  </verify>
  <acceptance_criteria>
    - Gate C: `grep -rE '(^|[^.a-zA-Z_])(Threshold|CompositeThreshold|StateChannel|ThresholdRule)\(' tests/ | wc -l` returns 0
    - Gate F: `bash scripts/check_skip_list_parity.sh` exits 0
    - Gate B: `git log --oneline <phase-base>..HEAD -- 'tests/**/*olden*' | wc -l` returns at most 1
    - Gate A: `git diff --name-only <phase-base>..HEAD | grep -v -E '^(tests/|scripts/check_skip_list_parity\.sh|\.github/workflows/tests\.yml|\.planning/phases/1015-)' | wc -l` returns 0 (no out-of-scope files)
    - File `/tmp/1015_gate_verdicts.txt` exists with 4 PASS/FAIL lines
  </acceptance_criteria>
  <done>Gates A/B/C/F all PASS; verdicts captured in /tmp/1015_gate_verdicts.txt for Task 4 to consume.</done>
</task>

<task type="auto">
  <name>Task 2: Run Octave smoke (Gate D) — automated to the extent possible</name>
  <files>/tmp/1015_octave_run.txt</files>
  <read_first>
    - tests/run_all_tests.m (entry point — confirm the Octave-half subprocess-isolation pattern)
    - .planning/phases/1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api/1015-02-SUMMARY.md (Plan 02 outcome — confirms which sidecars survived migration vs. were deleted)
  </read_first>
  <action>
    Gate D — Octave smoke. Two parts:

    **Part 1 — Octave-half of run_all_tests.m:**
      ```bash
      if command -v octave >/dev/null; then
        cd tests && octave --no-gui --eval "run_all_tests()" 2>&1 | tail -30 > /tmp/1015_octave_run.txt
        OCT_RC=$?
        cd ..
      else
        echo "octave not on PATH — Gate D defers to user (logged as DEFERRED, not FAIL)" > /tmp/1015_octave_run.txt
        OCT_RC=0  # don't block the plan
      fi
      ```
      Record verdict in /tmp/1015_gate_verdicts.txt: PASS / FAIL / DEFERRED.

    **Part 2 — timer leak check** (CONTEXT.md Gate D second condition: "timerfindall() empty between examples"):
      ```bash
      if command -v octave >/dev/null; then
        octave --no-gui --eval "addpath(pwd); install(); fprintf('timerfindall: %d open\n', numel(timerfindall())); exit(numel(timerfindall()))"
      fi
      ```
      Expected exit 0 (zero open timers). Append result to /tmp/1015_gate_verdicts.txt.

    If Octave is unavailable in the executor environment (very common — only CI has the Octave runner), record Gate D as DEFERRED with note "Verify in CI: tests.yml octave-tests job must pass on the v2.1 tip commit." Do not block the phase on a DEFERRED Gate D — Gate E (MATLAB) carries the same risk vector.
  </action>
  <verify>
    <automated>bash -c 'if command -v octave >/dev/null; then cd tests && octave --no-gui --eval "run_all_tests()" >/dev/null 2>&1; else true; fi'</automated>
  </verify>
  <acceptance_criteria>
    - File `/tmp/1015_octave_run.txt` exists with last 30 lines of Octave run OR a DEFERRED note
    - If Octave was available locally: tail of `/tmp/1015_octave_run.txt` shows `0 failed` (or equivalent green-suite phrasing) AND `timerfindall: 0 open`
    - If Octave was unavailable: verdict marked DEFERRED with explicit pointer to CI tests.yml octave-tests job
    - /tmp/1015_gate_verdicts.txt has a Gate D entry (PASS/FAIL/DEFERRED)
  </acceptance_criteria>
  <done>Gate D verdict captured (locally PASS, or DEFERRED to CI octave-tests job).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — MATLAB R2020b suite green (Gate E)</name>
  <files>tests/run_all_tests.m</files>
  <read_first>
    - tests/run_all_tests.m (the user runs this; planner has no role beyond pausing)
  </read_first>
  <what-built>
    All Phase 1015 changes are committed (Plans 01 + 02 commits). The Threshold-family grep gate is clean. Octave smoke either passed locally or is deferred to CI. The remaining gate is Gate E — MATLAB R2020b suite green — which only the user can run because Claude does not have a MATLAB license.
  </what-built>
  <how-to-verify>
    1. Open MATLAB R2020b (must be R2020b, not a later release — Phase 1006 pinned CI to R2020b).
    2. `cd <repo>` and run:
       ```matlab
       cd tests
       run_all_tests
       ```
    3. Wait for the suite to finish.
    4. Confirm:
       - The final summary line shows `0 failed` (matlab.unittest output: "Totals: PASSED 0 FAILED 0 INCOMPLETE … of N tests").
       - The test-count `N` is approximately the pre-1015 baseline minus the ~40-100 methods deleted in Plan 01 zombie sweep (exact delta documented in Task 4's SUMMARY).
       - No `Threshold` / `EventDetector` / `IncrementalEventDetector` / `EventConfig` reference errors appear in the output.
    5. Report back one of:
       - `approved` — suite green, test-count delta plausible.
       - `failed: <one-line summary>` — list any failing test methods. The Phase 1015 fix path goes through `/gsd:plan-phase 1015 --gaps`.
       - `partial: <count> failed but they are pre-existing` — if failing tests are unrelated to Phase 1015 (e.g., R2025b drift catalogued in `.planning/debug/matlab-tests-failures-investigation.md` Phase 1006 territory), report the count + a 1-line diagnosis. The plan still ships if the failing tests are pre-existing and explicitly out-of-scope per CONTEXT.md.
  </how-to-verify>
  <action>
    Pause and surface the `<how-to-verify>` block to the user. Wait for the resume-signal. Capture the user's verdict (`approved` / `failed: ...` / `partial: ...`) into `/tmp/1015_gate_verdicts.txt` as the Gate E entry. If the verdict is `failed: ...`, block the plan and surface the failure to /gsd:plan-phase 1015 --gaps. If `approved` or `partial: ... pre-existing`, proceed to Task 4.
  </action>
  <verify>
    <automated>grep -q '^Gate E: ' /tmp/1015_gate_verdicts.txt</automated>
  </verify>
  <acceptance_criteria>
    - User has supplied a resume-signal matching `^(approved|failed:|partial:)`
    - /tmp/1015_gate_verdicts.txt has a `Gate E:` line with PASS / FAIL / PARTIAL verdict
    - If verdict is `failed: ...`, plan is blocked and orchestrator routes to gap-closure planning
  </acceptance_criteria>
  <resume-signal>Type `approved`, `failed: <summary>`, or `partial: <count> pre-existing failures: <one-line diagnosis>`</resume-signal>
  <done>Gate E verdict recorded in /tmp/1015_gate_verdicts.txt; plan unblocked iff verdict is approved or partial-pre-existing.</done>
</task>

<task type="auto">
  <name>Task 4: Write 1015-SUMMARY.md with all 6 gate verdicts + baseline drop documentation (TEST-11)</name>
  <files>.planning/phases/1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api/1015-SUMMARY.md</files>
  <read_first>
    - /tmp/1015_gate_verdicts.txt (Tasks 1-3 output)
    - /tmp/1015_pre_deletion_method_counts.txt (Plan 01 Task 1 output — pre-deletion counts of the 5 deleted files)
    - .planning/phases/1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api/1015-01-SUMMARY.md
    - .planning/phases/1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api/1015-02-SUMMARY.md
    - $HOME/.claude/get-shit-done/templates/summary.md (the SUMMARY template structure)
  </read_first>
  <action>
    Author `1015-SUMMARY.md` following the gsd `summary.md` template structure with these required sections:

    ```markdown
    ---
    phase: 1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api
    plans: [01, 02, 03]
    requirements_satisfied: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, TEST-07, TEST-08, TEST-09, TEST-10, TEST-11, TEST-12, DIFF-02, DIFF-04]
    completed: <today's date>
    ---

    # Phase 1015 Summary — Test Suite Cleanup

    ## Outcome
    <one-paragraph: what shipped, what's the user-facing change, what's the bisect-safe state>

    ## Gate Verdicts (6-gate exit pattern)

    | Gate | Description | Verdict | Evidence |
    |------|-------------|---------|----------|
    | A | scope discipline | PASS/FAIL | `git diff --name-only` ⊆ planned files_modified |
    | B | golden untouched | PASS/FAIL | <N> commits touched tests/**/*olden* (expected: 1) |
    | C | Threshold-family grep | PASS/FAIL | <N> hits in tests/ (expected: 0) |
    | D | Octave smoke | PASS/FAIL/DEFERRED | <local result OR "deferred to tests.yml octave-tests job"> |
    | E | MATLAB R2020b CI | PASS/FAIL/PARTIAL | <user's Task 3 verdict> |
    | F | skip-list parity | PASS | scripts/check_skip_list_parity.sh exit 0 |

    ## Test-Method Baseline Drop (TEST-11)

    Pre-1015 baseline (captured 2026-04-30):
    - tests/suite/* — <N1> total test methods
    - tests/test_*.m — <N2> total test functions

    Deletions (Plan 01 Task 1):
    | File | Test Methods | Why |
    |------|--------------|-----|
    | tests/suite/TestEventConfig.m | <count> | Tests EventConfig.addSensor — class deleted in Phase 1013 (DEAD-03) |
    | tests/suite/TestEventDetectorTag.m | <count> | Tests EventDetector.detect — class deleted in Phase 1013 (DEAD-01) |
    | tests/test_event_config.m | <count> | Octave sidecar of TestEventConfig.m |
    | tests/test_event_detector.m | <count> | Octave sidecar of long-deleted TestEventDetector.m |
    | tests/test_incremental_detector.m | <count> | Octave sidecar of long-deleted TestIncrementalDetector.m |
    | <Plan 02 Task 2 deletion if applicable> | <count> | <reason from Plan 02 Task 2 commit body> |

    Migrations (Plan 02 — net method count change):
    | File | Pre Methods | Post Methods | Delta |
    |------|-------------|--------------|-------|
    | tests/test_SensorDetailPlot.m | <pre> | <post> | <delta> |
    | tests/test_gauge_widget.m | <pre> | <post> | <delta> |
    | tests/test_icon_card_widget_tag.m | <pre> | <post> | <delta> |
    | tests/test_multistatus_widget_tag.m | <pre> | <post> | <delta> |

    Total baseline drop: <Σ deleted> test methods removed; <Σ migrated_delta> net change in surviving migrated files. Final v2.1 tip baseline: <N1 - drop> suite + <N2 - drop> sidecar test methods.

    ## Net Line Budget

    `git diff --shortstat <phase-base>..HEAD` output:
    <X files changed, Y insertions(+), Z deletions(-)>

    Net LOC: <Y - Z>. ROADMAP target: -500 to -1500. Verdict: <within range / over / under>.

    ## Commits in This Phase

    <git log --oneline <phase-base>..HEAD output, prefixed by "* ">

    ## Decisions Recorded

    - Plan 02 Task 2 outcome: <migrated test_event_store.m to MonitorTag+EventStore pattern OR deleted as zombie sidecar>. Justification: <one-line>.
    - Plan 02 Task 4 IconCardWidget Threshold NV pair: <survived as deprecation alias / removed in Phase 1011>.
    - Plan 02 Task 5 MultiStatusWidget item.threshold field: <survived / removed in Phase 1011>.

    ## Carry-Over to v2.1 Tip

    - scripts/check_skip_list_parity.sh exits 0 vacuously on this branch (smoke harness absent — Phase 1012 lands smoke harness on a different branch). Once branches converge, the parity script will exercise actual diff detection. Defensive script design ensures it never false-positives on branch divergence.
    - Test-count baseline drop must be reflected when STATE.md or PROJECT.md publishes the v2.1 tip metric.

    ## Next Phase

    Phase 1016 — Examples 05-events rewrite (DEMO-01..09 + DIFF-01). DIFF-04's parity script is now in place for DEMO-08 to exercise.
    ```

    Substitute `<...>` placeholders from the actual gate verdicts file, the baseline file, and `git log --oneline`. Do NOT commit a summary with unfilled `<...>` placeholders OUTSIDE of fenced code blocks.

    Commit message exactly:
      `docs(1015-03): write phase summary with 6-gate verdicts + baseline drop (TEST-11)`
  </action>
  <verify>
    <automated>test -f .planning/phases/1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api/1015-SUMMARY.md && grep -q '## Gate Verdicts' .planning/phases/1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api/1015-SUMMARY.md && grep -q 'Test-Method Baseline Drop' .planning/phases/1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api/1015-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/1015-test-suite-cleanup-delete-zombies-migrate-threshold-refs-to-tag-api/1015-SUMMARY.md` exits 0
    - File contains the literal heading `## Gate Verdicts (6-gate exit pattern)`
    - File contains the literal heading `## Test-Method Baseline Drop (TEST-11)`
    - File contains a row for each of Gates A-F
    - File contains pre/post counts for every deleted file (rows in the Deletions table)
    - File contains pre/post counts for every migrated file (rows in the Migrations table)
    - File min line count: 80 (per must_haves)
    - `git log -1 --format=%s` returns `docs(1015-03): write phase summary with 6-gate verdicts + baseline drop (TEST-11)`
  </acceptance_criteria>
  <done>1015-SUMMARY.md exists, fully populated, all 6 gate verdicts recorded, baseline drop documented with explicit counts, commit pushed.</done>
</task>

</tasks>

<verification>
- All 6 gates have a recorded verdict (A: scope, B: golden, C: grep, D: Octave, E: MATLAB R2020b, F: parity)
- Test-method baseline drop documented with explicit pre/post counts per file
- Net LOC budget recorded and compared against ROADMAP target -500 to -1500
- All requirement IDs in this phase (TEST-01..12 + DIFF-02 + DIFF-04) cross-referenced in 1015-SUMMARY.md frontmatter `requirements_satisfied`
- 1 commit expected (the SUMMARY commit; Tasks 1, 2, 3 are pure verification — no commits)
</verification>

<success_criteria>
- TEST-11: baseline drop documented with explicit numbers
- All 6 gates PASS or formally DEFERRED with traceability
- Phase ready for /gsd:complete-phase ratchet to advance v2.1 progress (50% → 75%)
</success_criteria>

<output>
The 1015-SUMMARY.md file IS this plan's output. After commit, the orchestrator's /gsd:complete-phase step will:
1. Update .planning/STATE.md progress (50% → 75%)
2. Mark Phase 1015 [x] in ROADMAP
3. Update REQUIREMENTS.md traceability table (TEST-01..12 + DIFF-02 + DIFF-04 → ✅)
4. Optionally tee the SUMMARY's Carry-Over section into PROJECT.md decisions log
</output>

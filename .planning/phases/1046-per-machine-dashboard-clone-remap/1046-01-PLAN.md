---
phase: 1046-per-machine-dashboard-clone-remap
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/Fleet/CanonicalMapper.m
  - tests/test_canonical_reverse.m
  - libs/Fleet/runCanonicalReverseTests.m
autonomous: true
requirements: [DASH-03]
must_haves:
  truths:
    - "CanonicalMapper.logicalIdFor(machineId, localKey) returns the logicalId of the entry matching (machineId, localKey), or '' when none matches (DASH-03 reverse seam)"
    - "It is a pure read — Entries_ and LastTagInfos_ are never mutated; no side effects"
    - "Octave-safe: plain keys()/loop/strcmp; no contains, no isa, no validateattributes"
  artifacts:
    - path: "libs/Fleet/CanonicalMapper.m"
      provides: "logicalIdFor reverse-lookup method"
      contains: "function lid = logicalIdFor"
    - path: "tests/test_canonical_reverse.m"
      provides: "flat Octave-safe test for logicalIdFor"
      contains: "test_canonical_reverse"
  key_links:
    - from: "libs/Fleet/CanonicalMapper.m"
      to: "Entries_"
      via: "bucket scan for machineId+localKey"
      pattern: "logicalIdFor"
---

<objective>
Add the reverse canonical lookup the clone/remap needs: `CanonicalMapper.logicalIdFor(machineId, localKey) -> logicalId | ''`. `resolve` goes logicalId→entry (forward); the cloner needs the other direction — given a source machine's local tag key, which logical sensor is it? This is a pure `Entries_` scan, the mirror of `resolve`, with a flat Octave-safe test.
Output: extended `libs/Fleet/CanonicalMapper.m` + `tests/test_canonical_reverse.m` (+ `libs/Fleet/runCanonicalReverseTests.m` wrapper).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/1046-per-machine-dashboard-clone-remap/1046-CONTEXT.md
@libs/Fleet/CanonicalMapper.m
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CanonicalMapper.logicalIdFor reverse lookup</name>
  <files>libs/Fleet/CanonicalMapper.m</files>
  <read_first>
    - libs/Fleet/CanonicalMapper.m lines 292-336 (resolve + isResolvable — the forward bucket-scan idiom to mirror; logicalIds at ~318 added in Phase 1045) and lines 53-56 (Entries_ shape: containers.Map(logicalId -> cell of entry structs; each entry has machineId, localKey, ...))
  </read_first>
  <behavior>
    - logicalIdFor('M01','temp_motor') returns the logicalId whose bucket holds an entry with machineId=='M01' AND localKey=='temp_motor'.
    - Returns '' when the machine/key pair is in no bucket (no mapping).
    - First match wins (a localKey maps to at most one logicalId per machine in practice); deterministic over keys() order.
    - No mutation of Entries_ / LastTagInfos_.
  </behavior>
  <action>
    Add a public method `logicalIdFor(obj, machineId, localKey)` to `CanonicalMapper`, placed immediately after `logicalIds` (and before `isResolvable`). Iterate `ks = keys(obj.Entries_)`; for each logicalId, scan its bucket cell; if an entry has `strcmp(e.machineId, machineId) && strcmp(e.localKey, localKey)`, return that logicalId. Default `lid = ''` before the loop; return on first match. Namespaced header comment mirroring `resolve`/`logicalIds`. Octave-safe (no contains/isa).
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/PARA/10_Projects/FastPlot/.claude/worktrees/friendly-leakey-0bc166 && grep -q 'function lid = logicalIdFor' libs/Fleet/CanonicalMapper.m && ! grep -q 'contains(' libs/Fleet/CanonicalMapper.m && echo PASS</automated>
    <automated>mcp__matlab__check_matlab_code on libs/Fleet/CanonicalMapper.m — clean.</automated>
  </verify>
  <acceptance_criteria>
    - check_matlab_code clean; grep PASS; pure read (no Entries_ assignment inside logicalIdFor).
  </acceptance_criteria>
  <done>logicalIdFor returns the right logicalId for a mapped pair and '' for an unmapped pair.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flat Octave-safe test for logicalIdFor</name>
  <files>tests/test_canonical_reverse.m, libs/Fleet/runCanonicalReverseTests.m</files>
  <read_first>
    - tests/test_compare_resolution.m (flat-test shape: install() at top, per-test try/catch, nPassed/nFailed, final fprintf + error on failure) and libs/FastSenseCompanion/runCompareResolutionTests.m (the run-wrapper pattern)
    - libs/Fleet/CanonicalMapper.m suggest() (to build a known map: pass tagInfos so M01/M02 cluster under a logicalId)
  </read_first>
  <behavior>
    - Build a CanonicalMapper, suggest tagInfos for M01='temp_motor'/M02='temp_mtor' (cluster), then: logicalIdFor('M01','temp_motor') == the cluster logicalId; logicalIdFor('M02','temp_mtor') == same logicalId; logicalIdFor('M01','nonexistent') == ''; logicalIdFor('M99','temp_motor') == ''.
    - An override entry (override(lid,'M03','x')) is also found by logicalIdFor('M03','x').
  </behavior>
  <action>
    Create `tests/test_canonical_reverse.m` (flat function `test_canonical_reverse()`), mirroring `test_compare_resolution.m`: `install()`, per-test try/catch, `nPassed`/`nFailed`, final `fprintf('    %d of %d tests passed.\n', ...)` + `error('test_canonical_reverse:failures', ...)` on any failure. Cover: forward/reverse consistency (resolve the cluster logicalId, assert logicalIdFor returns it for both machines), unmapped key → '', unknown machine → '', and an override entry found. Add `libs/Fleet/runCanonicalReverseTests.m` mirroring `runCompareResolutionTests.m` so the suite/CI can invoke it (Octave-safe: no matlab.unittest, no contains).
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/PARA/10_Projects/FastPlot/.claude/worktrees/friendly-leakey-0bc166 && grep -q 'test_canonical_reverse' tests/test_canonical_reverse.m && ! grep -qE 'contains\(|matlab\.unittest' tests/test_canonical_reverse.m</automated>
    <automated>mcp__matlab__evaluate_matlab_code: run test_canonical_reverse() — all tests pass (flat test_*.m runs via evaluate_matlab_code, not run_matlab_test_file).</automated>
  </verify>
  <acceptance_criteria>
    - test_canonical_reverse() prints all tests passed; check_matlab_code clean; no contains / matlab.unittest.
  </acceptance_criteria>
  <done>The reverse lookup is proven by a flat Octave-safe test (forward/reverse consistency + miss cases + override).</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-1046-01a | Tampering (wrong remap) | logicalIdFor match | mitigate | Exact `strcmp` on BOTH machineId and localKey; first-match deterministic; pure read. Covered by the flat test's forward/reverse-consistency + miss cases. |
| T-1046-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. |
Data-model surface only; no security boundary.
</threat_model>

<verification>
- check_matlab_code clean on CanonicalMapper.m; `grep contains( libs/Fleet/CanonicalMapper.m` → 0.
- test_canonical_reverse() green via evaluate_matlab_code.
</verification>

<success_criteria>
- logicalIdFor is the pure reverse mirror of resolve; '' on miss; Octave-safe; flat-tested.
</success_criteria>

<output>
Create `.planning/phases/1046-per-machine-dashboard-clone-remap/1046-01-SUMMARY.md` when done.
</output>

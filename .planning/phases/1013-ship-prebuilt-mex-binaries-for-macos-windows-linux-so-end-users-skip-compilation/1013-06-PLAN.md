---
phase: 1013-ship-prebuilt-mex-binaries-for-macos-windows-linux-so-end-users-skip-compilation
plan: 06
type: execute
wave: 4
depends_on: ['1013-01', '1013-02', '1013-03', '1013-04']
files_modified:
  - .github/workflows/_build-mex-octave.yml
  - .github/workflows/tests.yml
  - .github/workflows/benchmark.yml
  - .github/workflows/examples.yml
  - .github/workflows/release.yml
autonomous: false
requirements: [MEX-03, MEX-04, MEX-05]
must_haves:
  truths:
    - "tests.yml, benchmark.yml, examples.yml: if committed binaries exist for the runner's platform and stamp matches, CI jobs skip compilation and use the tracked binaries"
    - "If stamp mismatches (e.g. PR touches mex_src but binaries not yet refreshed), CI still compiles so tests can run"
    - "release.yml includes committed MEX binaries in the release tarball instead of deleting them"
    - "FASTSENSE_SKIP_BUILD=1 escape hatch continues to work in every CI job"
    - "_build-mex-octave.yml artifact paths updated to include octave-<platform> subdirs (needed for Plan 03 layout)"
  artifacts:
    - path: .github/workflows/_build-mex-octave.yml
      provides: "Reusable Octave build updated for platform-subdir layout"
    - path: .github/workflows/release.yml
      provides: "Release packaging no longer strips MEX files"
  key_links:
    - from: .github/workflows/tests.yml
      to: libs/FastSense/private/binary_search_mex.mexa64
      via: "committed binary present in checkout; build step becomes no-op or skipped by needs_build"
      pattern: "FASTSENSE_SKIP_BUILD|binary_search_mex"
    - from: .github/workflows/release.yml
      to: "libs/**/*.mex*"
      via: "removal of the find-delete block"
      pattern: "mexmaca64"
---

<objective>
Update the four existing CI workflows that currently compile MEX on every run, plus the release packaging, so they reuse committed binaries instead of always rebuilding. Preserve the full build-on-source-change path for PRs that touch MEX sources (those PRs intentionally get a from-scratch rebuild).

Purpose: makes the "fast clone / fast CI" behavior visible in every pipeline, not just the new refresh workflow. Also unblocks release tarball shipping committed binaries.

Output: edits to 5 workflow YAMLs. No new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1013-ship-prebuilt-mex-binaries-for-macos-windows-linux-so-end-users-skip-compilation/1013-CONTEXT.md
@.planning/phases/1013-ship-prebuilt-mex-binaries-for-macos-windows-linux-so-end-users-skip-compilation/1013-RESEARCH.md
@.github/workflows/_build-mex-octave.yml
@.github/workflows/tests.yml
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update _build-mex-octave.yml artifact paths for subdir layout</name>
  <files>.github/workflows/_build-mex-octave.yml</files>
  <action>
    1. Update the cache-mex `path` and the upload-artifact `path` blocks to capture files from the new Octave platform subdir locations in addition to the legacy flat `private/` paths (for backward compat during rollout):
       ```yaml
       path: |
         libs/FastSense/private/*.mex
         libs/FastSense/private/octave-linux-x86_64/*.mex
         libs/FastSense/octave-linux-x86_64/*.mex
         libs/SensorThreshold/private/*.mex
         libs/SensorThreshold/private/octave-linux-x86_64/*.mex
         libs/FastSense/mksqlite.mex
       ```
    2. Update the cache key to also hash `build_mex.m` + the stamp file so a stamp refresh invalidates cache:
       ```yaml
       key: mex-linux-${{ hashFiles('libs/FastSense/private/mex_src/**', 'libs/FastSense/build_mex.m', 'libs/FastSense/private/.mex-version') }}
       ```
    3. Leave the `octave --eval "install();"` compile step as-is. With Plan 01+03 shipped, install.m will skip compilation when the committed binary + stamp are present. If missing (e.g. after `paths-ignore`-filtered PR), it'll rebuild — desired.
  </action>
  <verify>
    <automated>actionlint .github/workflows/_build-mex-octave.yml</automated>
  </verify>
  <done>
    - Artifact glob includes octave-linux-x86_64/ subdirs.
    - Cache key hashes the stamp file.
    - No logic change that would break callers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tests.yml to trust committed binaries, keep FASTSENSE_SKIP_BUILD for test jobs</name>
  <files>.github/workflows/tests.yml</files>
  <action>
    1. `build-mex-matlab` job: update cache path and key to include `.mex-version`, same pattern as Task 1. Leave the `install();` command unchanged.
    2. `mex-build-macos` and `mex-build-windows` jobs: these are now "rebuild-on-source-change smoke tests" rather than artifact producers. Add a comment explaining that the authoritative artifacts come from `refresh-mex-binaries.yml`. Leave the jobs in place so a PR that breaks Octave compilation on mac/win is caught.
    3. The `octave` and `matlab` test jobs already set `FASTSENSE_SKIP_BUILD: "1"` after downloading MEX artifacts. Keep this. After Plan 04 lands committed binaries for ARM, the CI runners on Linux will get binaries from the artifact download; the check-ignore is to avoid calling install's build path even defensively.
    4. Download step for `octave` job: currently downloads artifact `mex-linux` (flat `.mex`). Update the upload in `_build-mex-octave.yml` (Task 1) so artifacts preserve the subdir layout; no change needed in the download step because download-artifact preserves paths. Verify via: `ls libs/FastSense/private/octave-linux-x86_64/` in the test job's first step on next CI run.
  </action>
  <verify>
    <automated>actionlint .github/workflows/tests.yml</automated>
  </verify>
  <done>
    - No logic change for the test jobs; only cache invalidation wiring adjusted.
    - macOS/Windows smoke-test jobs retained with comment pointing to refresh-mex-binaries.yml.
  </done>
</task>

<task type="auto">
  <name>Task 3: Same cache-key update on benchmark.yml + examples.yml</name>
  <files>.github/workflows/benchmark.yml, .github/workflows/examples.yml</files>
  <action>
    For each workflow, if it contains an `actions/cache` step keyed on mex_src+build_mex.m, append `libs/FastSense/private/.mex-version` to the `hashFiles(...)` call.
    If either workflow instead uses `_build-mex-octave.yml` via `workflow_call`, no local cache-key change is needed (inherited from Task 1).
    Open each file first, audit which path it uses, and make the minimal edit.
  </action>
  <verify>
    <automated>bash -c 'actionlint .github/workflows/benchmark.yml .github/workflows/examples.yml'</automated>
  </verify>
  <done>
    - Cache keys (if any) hash the stamp file.
    - No semantic change otherwise.
  </done>
</task>

<task type="auto">
  <name>Task 4: Stop deleting MEX files from release tarball</name>
  <files>.github/workflows/release.yml</files>
  <action>
    1. In the `Package release` step, DELETE the block that strips MEX files (lines 64-67 in the current file):
       ```yaml
       find "${DIRNAME}/libs" -type f \( \
         -name "*.mexmaca64" -o -name "*.mexmaci64" \
         -o -name "*.mexa64" -o -name "*.mexw64" \
         -o -name "*.mex" \) -delete
       ```
    2. Update the release body under `Create GitHub Release` → `body:` to reflect that prebuilt binaries ship in the archive:
       > Download the archive, extract it, and run `install` in MATLAB/Octave. Prebuilt MEX binaries are bundled — compilation only happens on unsupported platforms.
    3. Keep everything else (tar+zip, changelog, test gate) unchanged.
  </action>
  <verify>
    <automated>bash -c 'actionlint .github/workflows/release.yml &amp;&amp; ! grep -q "mexmaca64" .github/workflows/release.yml || (grep -n mexmaca64 .github/workflows/release.yml; echo "FAIL: mex-delete block still present"; exit 1)'</automated>
  </verify>
  <done>
    - No `-name "*.mex*" -delete` block in release.yml.
    - Release body text updated.
    - Test gate job unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human verifies CI green across tests + dry-run release</name>
  <what-built>Updated CI workflows that trust committed binaries and ship them in release tarballs.</what-built>
  <how-to-verify>
    1. Push the branch. Observe:
       - `tests.yml::build-mex` (Linux Octave): runs, uses cache or rebuilds; octave tests green.
       - `tests.yml::build-mex-matlab`: runs, matlab tests green.
       - `tests.yml::mex-build-macos` / `mex-build-windows`: run to completion (smoke test), green.
    2. Optionally push a tag `v99.99.99-test` to a feature branch to trigger `release.yml`. Download the resulting tarball. Verify: `tar tzf FastSense-v99.99.99-test.tar.gz | grep mex` lists the committed binaries. Delete the test release.
    3. Optionally push a trivial change under `libs/FastSense/private/mex_src/` to verify `refresh-mex-binaries.yml` (Plan 05) gets triggered correctly under the new layout.
    4. Confirm no test or benchmark job regresses vs. main.
  </how-to-verify>
  <resume-signal>Type "approved" with CI run URLs, or describe failing jobs</resume-signal>
</task>

</tasks>

<verification>
Static YAML validation via actionlint per task.
End-to-end CI green is gated by the human-verify checkpoint (Task 5).
</verification>

<success_criteria>
- All five YAMLs valid.
- Release tarball contains committed MEX binaries (verified in Task 5).
- Existing CI green across tests/benchmarks/examples jobs.
- FASTSENSE_SKIP_BUILD still honored everywhere it was before.
</success_criteria>

<output>
After completion, create `.planning/phases/1013-ship-prebuilt-mex-binaries-for-macos-windows-linux-so-end-users-skip-compilation/1013-06-SUMMARY.md` noting: test CI run URL, release dry-run tarball size, any workflow behavior changes observed.
</output>

---
id: 260423-s4s
mode: quick
created: 2026-04-23
tags: [ci, mex, workflow]
---

# Quick 260423-s4s: Auto-trigger refresh-mex-binaries.yml on MEX source changes

<objective>
Ensure `.github/workflows/refresh-mex-binaries.yml` auto-triggers on push to `main` whenever any file that contributes to `.mex-version` stamp computation changes, so prebuilt MEX binaries stay current for all 7 platforms without manual intervention.
</objective>

## Tasks

### Task 1: Add `libs/FastSense/mex_stamp.m` to paths filter

<read_first>
- `.github/workflows/refresh-mex-binaries.yml` (current trigger config)
- `libs/FastSense/mex_stamp.m` (stamp formula source of truth)
</read_first>

<action>
Edit `on.push.paths` in `.github/workflows/refresh-mex-binaries.yml`. Current paths cover the stamp's *inputs* (C/H sources, `build_mex.m`, `mksqlite.c`) but not the stamp helper itself. Add `libs/FastSense/mex_stamp.m` with an inline comment explaining it's defensive (the aggregator bash reimplementation can drift from the MATLAB helper if only one is edited).

Resulting paths:
```yaml
paths:
  - 'libs/FastSense/private/mex_src/**'
  - 'libs/FastSense/build_mex.m'
  - 'libs/FastSense/mksqlite.c'
  - 'libs/FastSense/mex_stamp.m'   # new
```
</action>

<acceptance_criteria>
- `grep -c "mex_stamp.m" .github/workflows/refresh-mex-binaries.yml` >= 1 (appears in paths)
- `actionlint .github/workflows/refresh-mex-binaries.yml` exits 0 (if installed)
- `yq '.on.push.paths | length' .github/workflows/refresh-mex-binaries.yml` returns 4 (if yq installed)
- No other trigger semantics changed — `workflow_dispatch`, `concurrency`, matrix definitions untouched
</acceptance_criteria>

<done>
Workflow still lints clean. Pushing a change to `libs/FastSense/mex_stamp.m` on main triggers the 7-platform refresh. Pushing unrelated changes does not trigger. Manual dispatch still works.
</done>

## Scope notes

**Not in scope** — already correct from Phase 1013-05:
- `push: branches: [main]` trigger
- `workflow_dispatch` manual fallback
- `peter-evans/create-pull-request@v7` auto-PR on `chore/refresh-mex-binaries` branch
- No auto-merge — user reviews each refresh PR
- Non-retrigger invariant (auto-PR only touches binaries + stamp, neither in paths → no loop)

**Rejected expansions:**
- `libs/**/*.c` / `libs/**/*.h` globs — there's only one `mex_src/` directory (`libs/FastSense/private/mex_src/`); broader globs add no coverage and risk matching unrelated future sources.
- `libs/SensorThreshold/private/mex_src/**` — no such directory exists; SensorThreshold binaries are built from FastSense sources via `build_mex.m` and written to both locations. Already covered by existing paths.

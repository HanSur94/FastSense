---
quick_id: 260608-c11
slug: readme-hero-banner-product-screenshots
date: 2026-06-08
status: complete
---

# Quick Task 260608-c11 — Summary

## What changed
- Added 3 images to `docs/images/`: `fastsense-banner.png` (2560×960 dark hero),
  `dashboard-overview.png` (3096×1240), `companion.png` (1318×799).
- Reworked `README.md` top: centered, clickable hero banner + badge row inside
  `<div align="center">`; removed the now-redundant `# FastSense` H1 and the
  tagline blockquote (the banner conveys both).
- Added `## See it in action`: dashboard screenshot inline + companion in a
  `<details>` collapsible. Every other section left unchanged.

## Provenance
- Banner exported from the user-approved Pencil landing design
  (`~/Documents/untitled.pen`, image-free "FastSense README Banner" frame —
  Pencil's node exporter rejects nodes with embedded image fills, so the full
  one-pager could not be exported directly).
- Dashboard + companion screenshots captured live from
  `demo/industrial_plant/run_demo.m`: dashboard via `DashboardEngine.exportImage`;
  companion via macOS window-capture auto-cropped to its client area (MATLAB's
  uifigure exporters — exportapp/getframe/print — all refused the companion).

## GitHub-README constraints honored
- No JS / CSS / iframes (GitHub sanitizes them out of markdown). The interactive
  surface is the clickable banner, image links, and `<details>` expand/collapse.

## Commits
- `f652e742` — docs(readme): lead with hero banner + product screenshots (README + images)
- docs commit (this task's `.planning/` artifacts) follows.

## Optional follow-up
- For a *fully* interactive page, stand up a GitHub Pages `index.html` (the
  approved landing design rebuilt as responsive HTML) and point the README
  banner link at it. Offered to the user; out of scope for this task.

## Not pushed
- Per instruction, commits stay local on `claude/amazing-darwin-33f98c`; user pushes manually.

## Follow-up tweaks (commit `14a26bae`)
Four README tweaks after the initial landing, per user request:
- Added a centered `# FastSense` H1 + tagline under the banner (accessibility/SEO + fallback if the banner image fails to load).
- Added a feature-chips row: `100M+ points · Tags · live event detection · 21 dashboard widgets · no toolboxes`.
- Swapped the dashboard screenshot Overview → **Reactor** page (`dashboard-reactor.png` added; `dashboard-overview.png` removed).
- Pulled the companion screenshot inline (removed the `<details>` collapsible).

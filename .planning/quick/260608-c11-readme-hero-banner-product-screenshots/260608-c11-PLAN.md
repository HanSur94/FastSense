---
quick_id: 260608-c11
slug: readme-hero-banner-product-screenshots
date: 2026-06-08
status: complete
---

# Quick Task 260608-c11: README hero banner + product screenshots

## Goal

Lead `README.md` with the FastSense dark hero banner (exported from the approved
Pencil landing-page design) and feature the real product screenshots — fully
GitHub-README-compatible. GitHub sanitizes JS/CSS/iframes out of markdown, so a
*live* interactive page cannot live in the README; the realistic interactive
surface is a clickable banner, image links, and `<details>` expand/collapse.

## Tasks

1. Add image assets to `docs/images/`:
   - `fastsense-banner.png` — dark hero banner, 2560×960 (exported from Pencil)
   - `dashboard-overview.png` — industrial-plant dashboard screenshot, 3096×1240
   - `companion.png` — FastSense Companion client area, 1318×799
2. Edit `README.md` (keep ALL existing sections):
   - Replace the `# FastSense` H1 + tagline blockquote with a centered, clickable
     banner image + the existing badge row, wrapped in `<div align="center">`
     (the banner conveys the title + tagline).
   - Add a `## See it in action` section after the intro: dashboard screenshot
     inline + companion in a `<details>` collapsible.
3. Commit atomically. Do NOT push (user pushes manually).

## Verify

- Banner + screenshots render; `<details>` expands on GitHub.
- All original sections intact (30 seconds in, Tags, Build a dashboard,
  Performance, What's in the box, Install, Examples & docs, Citation/License).
- `docs/images/*.png` committed so paths resolve.

## Notes

- The banner is a purpose-built **image-free** Pencil frame: Pencil's node
  exporter fails on nodes containing embedded image fills, so the full one-pager
  (which embeds the screenshots) could not be exported directly.
- Screenshots captured live from `demo/industrial_plant/run_demo.m`: dashboard
  via `DashboardEngine.exportImage`; companion via macOS window-capture
  auto-cropped to its client area (MATLAB's uifigure exporters refused it).
- Source design lives in `~/Documents/untitled.pen` (frames "FastSense Landing"
  + "FastSense README Banner").
